# plan — restfit

앞으로 할 일. 완료 기록은 [changeLog.md](changeLog.md), 함정은 [claude-docs/troubleshooting.md](claude-docs/troubleshooting.md).
설계는 [docs/2026-08-25-design.md](docs/2026-08-25-design.md), 아이템 선정 경위는 [docs/ideas.md](docs/ideas.md).
화면 시안은 [assets/mockups/](assets/mockups/README.md) — 단일 출처는 Figma 파일이고 PNG는 산출물이다.

**범례** ✅완료 / 🔜다음 / ⬜예정 / ⏸의도적 보류 / ⚠️리스크·전제

---

## ⚠️ 전제

- ✅ **앱인토스 콘솔에 앱 등록** — 워크스페이스 `69821`(Booktimer) / 미니앱 `68739`(홈트가어렵나), 상태 `PREPARE`. `releaseDecision`·`reviewRequestDecision` 둘 다 `ALLOWED`
- ✅ **배포 경로 확보 — 콘솔 MCP**(`mcp.toss.im`, user scope). **API 키가 아예 불필요해졌다** — 번들 업로드·빌드 폴링·테스트 발송을 Claude가 직접 한다
  - CLI로 갈 거면 API 키는 **워크스페이스 왼쪽 「키」 메뉴**에 있다(앱 안 개발 섹션이 아니다)
- ✅ **광고 그룹 생성** (2026-08-26) — `INTERSTITIAL` · 「세트 사이 휴식 전면광고」 · `groupId` **`ait.v2.live.f491615603f04220`**
  - 전면 광고는 `categoryId`가 **필수**다. 미니앱에 이미 붙어 있던 **생활 › 건강**(`3804`)을 그대로 썼다 — `miniapp_get`의 `impression.categoryPaths[].category.id`
  - **문서가 예고한 `REGISTERING` 2시간을 안 거치고 즉시 `ENABLED`로 떴다.** 대기를 전제로 일정을 잡을 필요가 없다
  - ⚠️ **23일 연속 노출이 없으면 삭제 예정**으로 넘어간다 — 만든 순간부터 그 시계가 돈다
- ⬜ 쉐어링크 Open API 신청 *(사용자 진행)* — 기구 링크 50개뿐이라 **급하지 않다**
- ✅ 미니앱 이름 = **홈트가어렵나** / `appName` = **`home-workout-hard`** (⚠️ "토스" 미포함)
  - 둘은 **다른 값이다** — 표시 이름은 콘솔에서 고칠 수 있고, `appName`은 **케밥-케이스 고유 ID라 수정 불가**다. 한글을 넣으면 `ait build`만 통과하고 deploy에서 깨진다(T-218)
- ✅ **앱 아이콘** — [`assets/icon/icon.svg`](assets/icon/icon.svg) 단일 출처 + `npm`-무관 렌더 스크립트. 콘솔 「앱 로고」에는 **`png/icon-600.png`**(600×600 요구)를 올린다. 다크모드 로고는 비워 둔다 — 배경이 자체 색이다
  - AI 생성(HiggsField)은 **접었다** — 출력 PNG에 워터마크가 박혀 나오고, 가격 페이지 어디에도 유료 전환 시 제거된다는 말이 없다

---

## Phase 0 — 프로젝트 셋업 ✅

- ✅ 작업 추적 3종 + troubleshooting 분할 시스템 (pre-commit 훅 동작 실측)
- ✅ 미니앱 스캐폴드 (Vite + React 18 + `@apps-in-toss/web-framework`)
- ✅ 쉐어링크 복귀 검증 도구 (`returnTracker` · `verifyLog` — 15건 + 돌연변이 4종)
- ✅ 아이템 확정 및 경위 기록 — 3회의 전환 과정 전부 `docs/ideas.md`에 보존
- ✅ 설계 문서 재작성 (휴식 시간 광고 모델)
- ✅ 레포·폴더 `restfit`으로 정리

## Phase 0.5 — ⚠️ 실기기 실측 ✅ **완료**

**한 번의 실기기 테스트로 둘을 잰다.** 기존 검증 앱에 얹는다.

> **①의 답: 8/8.** 수익 모델이 섰고, 이 결과로 Phase 3를 그대로 진행했다.
> **②의 답: 복귀된다.** 토스쇼핑에 다녀와도 미니앱이 재시작되지 않는다.

### ① 광고를 세션당 몇 번 틀 수 있는가 ★★★

**이 아이템의 수익 전체가 이 가정 위에 있다.** 5회에서 막히면 모델이 절반 이하로 줄어든다.

- ✅ 검증 앱에 광고 연속 요청 테스트 추가 (`awaitAdEvent` — Phase 3에서 그대로 재사용)
- ✅ **앱인토스 광고 정책에 노출 빈도 제한이 있는지 확인** → **수치 상한 없음.** [인앱 광고 가이드](https://developers-apps-in-toss.toss.im/guide/monetization/in-app-ad)에 *"광고가 너무 자주 노출되지 않도록 주의"* 라는 권고만 있고 하루·세션당 횟수가 없다. **정책이 모델을 죽이지는 않는다** — 실제 상한은 fill rate라, 실측이 여전히 유일한 답이다
  - 지켜야 할 금지 조항(우리 설계는 전부 통과): 결제·계좌 개설 흐름 중 삽입 금지 / 종료·뒤로가기 방해 금지 / 필수 CTA 가리기 금지. 제재는 **30일~영구 이용 제한 + 수익 환수**
  - 앱인토스 인앱 광고는 **구글 애드몹 정책**을 따른다
- ✅ 측정 도구가 쓰는 API가 현행인지 확인 — `loadFullScreenAd`/`showFullScreenAd`는 현행이다(deprecated는 `TossAds.attach`와 `GoogleAdMob` v2 별칭)
- ✅ **실기기 실행 (2026-08-26) — 8회 시도, 8회 전부 노출 성공.** 막히는 지점이 없었다
  - **연속으로 눌러댄 최악 조건에서 나온 수치다.** 실사용은 45~150초 간격이라 더 불리할 이유가 없다
  - frequency cap이 있었다면 애드몹 통상값인 3~5회에서 걸렸어야 한다 — **안 걸렸다**
  - 근육 키우기·건강 유지(둘 다 휴식 8회)는 **세션 전체가 커버된다.** 체지방 감량은 11회라 3회가 미검증이지만, 8회까지 무손실이라 막힐 근거가 없다

### ② 미니앱에서 쉐어링크 복귀가 되는가

- ✅ 수동 쉐어링크 발급 (2026-08-26) — 토스쇼핑 앱에서 상품 공유 → "쉐어링크 공유하기". **Open API 승인 불필요**
- ✅ **앱 상태 유지 검증 (2026-08-26) — 통과.** 기구 탭에서 `Device.openURL`로 토스쇼핑이 열리고, **돌아오니 기구 탭 그대로였다**(재시작되면 오늘 탭으로 돌아간다)
  - 검증을 Probe 화면이 아니라 **실사용 경로(기구 탭)에서** 했다 — 실제로 쓸 흐름이 그대로 통과했다는 뜻이라 더 강한 증거다
- ❌ **뒤로 가기로는 안 돌아온다 (2026-08-27 실기기 제보)** — 토스쇼핑에서 뒤로 가기를 누르면 미니앱이 아니라 **토스쇼핑 안에서** 움직인다. [T-228](claude-docs/troubleshooting/T-228.md)
  - ⚠️ **위 8/26 검증이 「복귀 통과」로 읽혀 이 문제가 한 번 걸러졌다.** 둘은 다른 동작이다 — 8/26은 **돌아왔을 때 상태가 유지되는가**를 봤고, 이건 **뒤로 가기가 어디로 가는가**다. 설계 때 달아 둔 「복귀가 불안정하다」는 제보는 **우리 앱에도 해당한다**(옛 기록의 「해당하지 않는다」는 틀렸다)
  - **고칠 방법이 우리에게 없다** — `Device.openURL`은 URL 문자열 하나만 받고(복귀 경로 인자 없음), 외부 주소를 미니앱 안에서 여는 SDK도 없다(웹뷰 API는 `openPDFViewer` 하나, PDF 전용). 토스 공식 답변도 *"토스 내 다른 서비스로 이동하는 별도의 SDK / API는 제공해드리고 있지 않습니다"*
  - **토스가 내부 개선 중**(일정 미정, 2026-08-18 기준). 그때까지 상품 목록 위 `RETURN_NOTICE` 한 줄로 길을 알려 준다 — 고쳐지면 그 상수와 쓰는 자리를 지운다
  - ⚠️ 토스가 한때 **「쉐어링크를 제거하라」고 권고**했다(2026-08-05). 다만 우리는 링크가 든 채로 **검수를 세 번 통과**해서 심사 차단 사유는 아니다. 신고가 쌓이거나 반려 사유로 나오면 다시 판단한다
- ⚠️ **다만 외부 브라우저로 열린다.** 미니앱 안에 토스쇼핑을 띄우는 SDK·딥링크·WebView API가 **플랫폼에 없다**(앱인토스 개발자 커뮤니티 [공식 답변](https://techchat-apps-in-toss.toss.im/t/topic/4102): *"토스 내 다른 서비스로 이동하는 별도의 SDK / API는 제공해드리고 있지 않습니다"*). 우리 구현의 한계가 아니다

## Phase 1 — 데이터 + 순수 로직 ✅

- ✅ **운동 데이터 정제 파이프라인** — `npm run data` (873 → **647 채택**)
  - 장비를 **단일 값 → `requires` 집합**으로. 덤벨 벤치프레스가 벤치를 요구한다는 사실이 원본에는 없다
  - `other` 122개는 **제외가 기본값**, 홈트 가능한 33개만 골라 담음. 머신·케이블 148개는 통째로 제외
  - `equipment: null` 77개는 맨몸으로 합치되 해금 수치는 **근력만** 센다(안 그러면 배수가 무너진다)
  - 한글화 — 토큰 사전 + 이름 단위 예외. **미매핑 토큰이 있으면 빌드 실패**
  - → `src/data/exercises.json` 커밋 (런타임 네트워크 0)
  - 📌 해금 실측: 근력 415개 기준 **맨몸 81 → 덤벨 176 (+95, 2.17배)** — 설계 가정 유지
- ✅ **`filterByEquipment()` TDD** — `requires` 전부를 보유해야 통과
- ✅ **`unlockGain()` TDD** — 해금 수치. 쉐어링크의 근거
- ✅ **`pickRoutine()` TDD** — 부위 로테이션(가장 오래된 부위) · 근육별 라운드로빈 · 복합 우선. seed는 날짜
- ✅ **`adPlan()` TDD** ★ — 40초 미만 안 틈 / 연속 미충전만큼 백오프 / 3회 연속이면 세션 포기
- ✅ 테스트 84건 · **돌연변이 10종 전부 사살**(생존 0)
- ⏸ `instructions`(운동 설명문)는 데이터에서 뺐다 — 화면에 자리가 없고 이미지 2장이 동작을 보여준다

## Phase 2 — 화면 (광고 없이 먼저) ✅

- ✅ 보유 기구 등록 — 체크할 때마다 **"+96개"** 해금 수치가 붙는다. Phase 4 쉐어링크의 자리
- ✅ 오늘의 루틴 — 부위·개수·예상 시간. 날짜로 고정되고 **오늘 기록은 계산에서 뺀다**(끝내는 순간 루틴이 바뀌면 안 된다)
- ✅ **운동 진행 화면** — 세트 기록 + 휴식 타이머. **광고 자리는 비워둔 채로 완성**
- ✅ 기록 — 직전 기록 자동 입력(*"지난번 20kg × 10"*)
- ✅ 운동 이미지 — jsDelivr CDN. **실기기에서 정상 표시 확인**(2026-08-26) — CSP가 막을지 모른다는 리스크가 해소됐다. 번들에 넣을 이유가 없다(900장)
- ✅ 한글 이름 화면 확인 — 데이터 오분류 3건을 여기서 잡았다(아래)
- ✅ **온보딩 2단계** — 첫 진입 시 기구 → 목적. 목적이 없으면(`loadGoal() === null`) 온보딩이 뜬다
- ✅ **운동 목적** — 체지방 감량 / 근육 키우기 / 건강 유지. 반복·휴식·종목 수를 함께 정하고 기구 탭에서 바꾼다
- ✅ **온보딩 다시 보기**(기록 탭 개발자용 줄) — 폰에서는 localStorage를 손댈 수 없어, **이게 없으면 온보딩을 고쳐도 실기기에서 두 번 볼 수 없다.** 목적·기구를 지우고 기록은 남긴다
- ✅ **기구 상세** — 무게 구간(기구마다 다른 범위) · 벤치 등판 · 밴드 강도. **「모름」이 1급 선택지다.** 무게는 운동 화면 첫 값을 채우고, 벤치 등판은 루틴을 가른다(모름 165개 → 조절식 187개)
- ✅ `StrictMode` 재검토 → **켰다**. 실측 판정은 `restartedAfterOpen`이라 이중 마운트에 영향받지 않고, 타이머·광고 effect의 정리 누락을 잡아야 한다
- ✅ **기구 선택을 2열 정사각 버튼으로 (2026-08-27)** — 한 줄짜리 체크 항목이라 무엇을 고르는지 눈에 안 들어왔다. 170×170 칸에 44px 아이콘 + 이름 + 해금 수치를 넣고, 상세 칩은 **그 줄 아래 전폭으로** 펼친다(정사각 칸 안에 넣으면 글자가 뭉갠다)
  - 고른 칸은 **테두리 두께·바탕색·체크 세 가지가 같이** 바뀐다 — 색 하나에만 기대면 색각 이상인 사람에게는 표시가 없는 것과 같다
  - ⚠️ **온보딩과 「보유 기구」 화면이 같은 컴포넌트다** — 둘이 갈라지면 온보딩에서 고른 것과 나중에 보이는 것이 달라 보여서 일부러 하나로 뒀다. 양쪽이 함께 바뀐다
  - ✅ **「다음」 하단 고정** — 칸 10개면 세로 약 900px이라 CTA가 두 화면 아래로 밀린다. **온보딩에서 다음 버튼을 못 찾는 건 그대로 이탈이고, 이탈은 광고 슬롯이 통째로 사라진다는 뜻이다**
- ✅ **기구 아이콘 8종 (2026-08-27)** — 바벨·밴드·풀업바·벤치·짐볼·폼롤러·메디신볼·복근롤러. 기존 6종과 같은 언어(24 그리드·2px 라운드 스트로크)
  - ⚠️ **밴드만 면으로 채운다**([T-225](claude-docs/troubleshooting/T-225.md)) — 꼬리로 갈수록 가늘어 보이려면 **잉크가 실제로 얇아져야** 하는데 선으로는 불가능하다(굵기는 path 하나에 하나뿐). 대신 채우면 굽은 데 안쪽이 제 살을 파고들어, path는 손으로 못 고치고 [`gen-band-icon.mjs`](miniapp/scripts/gen-band-icon.mjs)로 뽑는다

**화면에서 드러난 데이터 결함** — 로직 테스트로는 절대 안 잡혔다

- 원본이 **풀업·행잉을 「맨몸」으로 분류**(자기 체중을 든다는 뜻) → 기구 없는 사용자 첫 화면에 풀업 3종. 9개를 `pullupBar`로 옮기고 회귀 테스트를 걸었다
- **목 아이소메트릭**이 어깨 운동 3개 중 2개를 차지 → 제외. 홈트 루틴에 목 운동을 넣는 앱은 없다
- 맨몸 팔 루틴이 **삼두 5종목** → 한 근육 상한 3을 넣었다. 한 근육에 15세트는 과훈련이고, **그런 루틴은 사람을 떠나게 해서 광고 슬롯도 함께 사라진다**
- **인클라인·디클라인 34개가 평벤치와 한 덩어리**였다 → 평벤치만 가진 사람에게 인클라인 벤치프레스가 나왔다. `benchAdjustable`로 갈랐다(2026-08-26)

**측정된 광고 슬롯**: 맨몸 3종목 = 9세트 → **휴식 8회**. 덤벨 5종목 = 15세트 → **휴식 14회**

## Phase 3 — 광고 통합 ✅ *(2026-08-26 실기기 확인 완료)*

- ✅ 휴식 시작 시 load → show. **`adPlan`이 유일한 판단자다** — 화면에서 조건을 덧붙이면 규칙이 두 곳으로 갈라진다
  - ⏸ **preload는 안 넣었다 — 실기기에서 확정.** 휴식 화면이 뜨고 **약 2초 뒤** 광고가 덮는다. 미리 받아 두면 마지막 세트처럼 **쓰지 않고 버리는 로드**가 생기는데(애드몹이 싫어하는 패턴), 2초를 줄이자고 치를 값이 아니다
- ✅ 광고 재생 중에도 **휴식 타이머는 계속 흐른다** — `restEndsAt`이 절대 시각이라 별도 처리가 필요 없었다. 전면 광고가 웹뷰를 가려 타이머가 스로틀돼도 같은 이유로 안전하다
- ✅ 실패·미충전은 **조용히 넘어간다.** `awaitAdEvent`가 throw하지 않고 결과를 돌려주고, 바깥은 `try/catch`로 한 번 더 덮었다
- ✅ 상태 전이(`nextAdState`) — 미충전이어도 `slotsSinceLastTry`는 0으로 되돌린다(시도는 했다). 안 그러면 백오프가 즉시 풀려 다음 휴식에 또 두드린다
- ⏸ 배너(`TossAds.attachBanner`) 병행 — **조건이 성립하지 않았다.** 「전면 광고 빈도가 낮게 나왔을 때만」이었는데 8/8이다
- ✅ **실기기 확인 (2026-08-26)** — 휴식에 광고가 뜨고, **광고를 닫으면 남은 휴식이 그만큼 줄어 있다.** 「광고가 시간을 늘리지 않는다」는 전제가 실기기에서 확인됐다

## Phase 4 — 장비 해금 + 쉐어링크 *(링크 대기)*

- ✅ **기구 탭 = 추천 화면**. 보유 기구·목적은 홈의 목적 칩에서 여는 전체화면(`Settings`)으로 갈랐다 — 전에는 **두 진입점이 같은 화면으로 갔다**
- ✅ **`recommend()` TDD** — 두 겹으로 거른다: **후보 목록**(사람이 정한다)과 **최소 해금 수**(데이터가 정한다)
  - ⚠️ **바벨은 후보에서 뺐다.** 운동 수 2위(+95)지만 원룸에 랙과 200cm 봉을 놓으라는 말이 된다. **숫자가 못 잡는 판단**이라 목록으로 박았다
  - ⚠️ **폼롤러는 근력 운동이 +0이다** — 사도 루틴이 하나도 안 바뀐다. 짐볼(+9)·메디신볼(+2)·복근롤러(+1)와 함께 뺐다
  - **`MIN_GAIN = 10`이 벤치를 자동으로 가른다** — 맨몸이면 +4라 안 뜨고, 덤벨을 가진 순간 +12가 되어 나타난다. 조건문 없이 「지금 뭘 가졌나」가 추천을 바꾼다
  - ⚠️ **근력만 센다.** 스트레칭까지 넣으면 맨몸 기준이 부풀어 배수가 주저앉는다 — `unlockGain`은 이 필터를 호출부에 맡기지만 `recommend`는 안에서 한다(첫 테스트가 그 함정에 그대로 걸렸다)
  - 배수는 **1.5배 이상일 때만** 표시. 1.1배가 셋씩 늘어서면 「겨우 1.1배」로 읽혀 +18개라는 사실까지 깎아먹는다
- ✅ ⚠️ **운동 중에는 상품을 절대 띄우지 않는다** — 세션이 시작되면 탭이 통째로 감춰져 구조적으로 불가능하다
- ✅ **2단 구조** — 1단은 「몇 개가 열리는지」, 2단(펼치면)은 「무엇을 사면 되는지」. 상품을 곧바로 늘어놓으면 **「+96개」가 상품 사이에 묻혀 사야 하는 이유를 잃는다**
  - 한 번에 하나만 펼친다 — 다 열면 다시 「쫘라락」이 되어 나눈 뜻이 없다
  - ⚠️ **가격은 넣지 않는다.** 정적 파일이라 바뀌어도 못 따라가고, 틀린 가격은 없는 것만 못하다
- ✅ **쉐어링크 채우기 (2026-08-27) — 5종 98건.** 작업은 [**쉐어링크 작업대**](docs/sharelinks/README.md)에서 한다. **기구마다 파일 하나**라 오늘 덤벨만 볼 거면 [dumbbell.md](docs/sharelinks/dumbbell.md)만 연다(검색어·그 기구의 함정·구간 경계·수집함이 거기 있다). 산출물은 [`src/data/shareLinks.ts`](miniapp/src/data/shareLinks.ts). 개수의 단일 출처는 [작업대 README](docs/sharelinks/README.md)의 표다
  - 여기부터는 **새로 뽑는 일이 아니라 살아 있는지 보는 일**이다 — 분기 1회, 다음은 2026-11 무렵
  - ⚠️ **필요한 건 5종뿐이다** — `SHOP_CANDIDATES`(덤벨·케틀벨·벤치·밴드·풀업바). 나머지 기구는 링크를 넣어도 추천에 안 떠서 화면에 안 나온다
  - ✅ **개수 상한을 두지 않는다 — 무게 구간 칩으로 좁힌다**(2026-08-27). 처음엔 기구당 3~5개만 두려 했는데, 그러면 고르는 일을 우리가 대신해 버려 **「내 무게에 맞는 게 없는」 사람**이 생긴다. 많이 담고 **거를 수단을 주는 쪽**이 낫다
    - 기준은 개수가 아니라 **구간이 비지 않았는가**다. 어느 구간이 0이면 그 구간을 고른 사람에게는 목록이 없는 것과 같다
    - 칩은 **구간이 둘 이상일 때만** 뜬다 — 하나뿐이면 「전체」와 결과가 같아 누를 이유가 없다. 덤벨·케틀벨은 네 구간이 다 차서 칩이 뜨고, **벤치·밴드는 무게 축이 없어 칩이 아예 없다**(목록이 통째로 나열되므로 그 둘만은 개수를 늘리는 대신 종류를 갖춘다)
    - ⚠️ 무게를 모르는 상품은 `weight`를 **비운다.** 칩으로는 못 찾고 「전체」에서만 보인다 — 모르는 것에 구간을 붙이면 그게 거짓이다
  - **링크 없는 기구는 버튼만 안 뜨고 추천은 그대로 보인다** — 파일만 채우면 살아난다
  - ⚠️ **죽은 링크는 앱이 못 잡는다** — 주소가 있으면 버튼도 그대로 뜨고 404를 열어 준다. 수익만 조용히 0이 되므로 **주기적으로 사람이 본다**(작업대의 「살아 있는지 점검」 — 본 날짜는 그 기구 파일의 「점검 기록」에 남긴다). 자동 HTTP 확인은 무효 클릭 정책 때문에 **하지 않는다**
  - **발급은 어드민 웹에서 한다** — [`sharelink.toss.im`](https://sharelink.toss.im) → 「링크」 → **상품 조회** → 상품 카드의 **「링크 발급」**. 토스쇼핑 앱을 켤 필요가 없다(옛 절차였다)
  - ⏸ **Open API는 안 쓴다** (2026-08-27 조사 결론 — 옛 「승인 후 자동 수집」 계획을 접었다). [문서](https://sharelink-docs.toss.im/)·명세는 **공개돼 있다**(예전에 「명세가 없다」고 적어 둔 것이 낡았다). 그런데도 접는 이유가 넷이다:
    1. **심사제다.** 어드민 「API 키 발급」이 「신청 전」 상태이고 **연동 신청서**(서비스 유형·URL·사용 목적)를 넣어 검수를 받는다. 즉시 발급이 아니다
    2. **서비스 URL을 댈 수 없다.** 신청서가 *"검수 담당자가 직접 열어보니, 접속할 수 있는 주소"* 를 요구하는데 우리 `serviceLink`는 `intoss://home-workout-hard`라 **브라우저로 안 열린다**(`homePageUri`도 비어 있다)
    3. ⚠️ **키워드 검색 API가 없다.** 조회는 카테고리 베스트 / 카테고리 / 하루특가 / 베스트 / 상품 상세뿐 — **「덤벨」로 찾을 수가 없다.** 카테고리 베스트를 훑어 이름으로 거르는 수밖에 없고, 원하는 기구가 베스트에 없으면 API로도 못 찾는다
    4. ⚠️ **운영정책이 우리 용도를 제한 대상으로 집는다** — *"상품 피드 또는 Open API 데이터를 이용해 **별도 상품 DB를 구성하는 경우**"*. `shareLinks.ts`가 정확히 그것이다
    - **API의 대상은 「매일 갈리는 편성」이다** — 문서가 *"상품 목록을 받아 게시글·구좌를 구성"* 이라 쓰고 랭킹이 시간마다 바뀌니 캐싱하라고 안내한다. 딜 카페·오픈채팅방 봇처럼 **손 복사가 물리적으로 불가능한 곳**을 위한 것이지, 고정 40건을 한 번 박는 우리와는 맞지 않는다. 실사용 사례는 웹에서 하나도 찾지 못했다(출시 공지조차 없다)
    - 되살릴 조건: 상품이 자주 갈려 **정기 갱신이 필요해질 때**. 그때는 신청서의 「연동할 수 없어요 — 상품을 나열하거나 가격을 비교해서 보여주는 서비스」 조항을 우리 기구 탭이 어떻게 읽히는지부터 따진다
  - ⚠️ 조회 API의 `productUrl`은 **추적되지 않는다.** 반드시 발급 API의 `shortUrl`(또는 어드민·토스쇼핑 앱의 「링크 발급」으로 받은 주소 — `https://toss.im/_m/...` 꼴)
  - ⚠️ **대가성 문구는 토스가 지정한 문장을 글자 그대로 쓴다**(`DISCLOSURE`). 뜻이 같아도 내가 지은 문장은 규정 위반이다 — 실제로 임의 문구로 만들어 뒀다가 발급받고서야 알았다
  - 링크가 하나도 없으면 문구도 안 뜬다(`HAS_ANY_LINK`) — 받지도 않는 대가를 고지하면 그게 거짓이다
  - ⚠️ **상품 고를 때**: 「풀업바」로 검색하면 **푸쉬업바**가 섞여 나온다. 푸쉬업바를 요구하는 운동은 데이터에 **0개**라(폼롤러와 같다) 넣어도 안 보인다. 검색어는 「철봉」·「문틀철봉」 — 이대로 뽑으니 11건에 푸쉬업바가 **한 건도 안 섞였다**(2026-08-27 실측)
  - ⛔ **풀업바의 설치 방식은 비워 둔 채로 끝낸다** (2026-08-27 사용자 결정) — 상품명이 방식을 안 말해서(10건 중 3건만 짐작 가능) 아는 것만 적었는데, **설치 방식은 상품에 딸려 오는 설명서가 말한다.** 사진을 봐서까지 채우면 상품이 이미 하는 말을 한 번 더 하는 것이고, 틀리면 우리 쪽에만 책임이 남는다
  - 벤치는 ⚠️ **반드시 각도 조절식** — 평벤치 165개 vs 조절식 187개(+22)
  - 밴드는 ⚠️ **긴 밴드(튜빙·평밴드)를 앞에** — 밴드 운동 23개 중 **20개가 긴 밴드**를 쓰고 짧은 루프(힙밴드)로 되는 것은 **3개뿐**인데, 검색이 주는 것은 정반대다(실측 47건: 긴 밴드 19 : 루프 22). 앱은 셋을 `band` 하나로 봐서 이 차이를 **모른다**
- ⬜ 실기기에서 `Device.openURL` 동작 확인 — Phase 0.5 ②와 같은 검증이다

## Phase 5 — 배포·심사

- ✅ **`npm run release`** = `npm run build && ait build` — T-150(수동 3단계가 스테일 번들을 심사에 올린 사고) 대응. 두 단계를 묶어 **dist를 안 만들고 아티팩트만 다시 싸는 경로를 없앴다**
- ✅ **콘솔 MCP 배포** — `bundle_upload`(deploymentId는 `ait build`가 번들 헤더에 심은 값을 그대로) → 발급된 S3 URL에 `curl -X PUT -H 'Content-Type: application/zip'` → `bundle_upload_complete` → `bundle_build_status` 폴링 → `bundle_test_push`
  - `bundle_test_push`가 **테스트 푸시를 보내고 `privateLink`(`intoss-private://`)를 준다.** 심사 없이 실기기에서 열린다 — Phase 0.5 실측은 이 경로다
  - ⚠️ 폴링 종료 조건은 **내 `deploymentId`가 `builds`에서 빠지는 것**이다. `builds`가 전부 비기를 기다리면 안 된다(`upload_complete`를 안 한 번들이 `PREPARE`로 영영 남는다)
  - ⚠️ **라이브 전환(출시하기)은 MCP로 불가** — 검수 승인 후 콘솔 웹에서 눌러야 한다
- ✅ **콘솔 이미지 등록** (2026-08-26) — 세로 스크린샷 4장(636×1048) + 가로 썸네일 1장(1932×828). 재생성은 [`scripts/render-screenshots.ps1`](scripts/render-screenshots.ps1), 경위는 [`assets/screenshots/README.md`](assets/screenshots/README.md)
  - 상세 설명도 함께 고쳤다 — 기구 탭이 추천 화면이 되면서 옛 설명("기구 탭에서 체크해요")이 실제와 어긋나 있었다
  - ⚠️ **올린 이미지는 검토 승인 뒤에야 조회에 보인다**([T-220](claude-docs/troubleshooting/T-220.md)) — 빈 배열을 보고 실패로 판단하면 중복 업로드하게 된다
- ✅ **앱인토스 심사 제출 (2026-08-26)** — `20260826-7` · `reviewStatus REVIEWING`
  - `requiresFeatureRegistration`이 true지만 **주요기능은 비필수**다. `featureList`를 비우면 기존 등록분이 그대로 실려 콘솔 웹과 같은 결과가 된다
  - ⚠️ **`review_cancel`로 한 번 취소하면 영구히 재요청 불가** — 번들을 새로 빌드해 올려야 한다
- ✅ **1차 반려 → 탭바 수정 (2026-08-26)** — 사유는 **탭바 하나**였다([T-222](claude-docs/troubleshooting/T-222.md)). 밑변에 붙은 형태가 토스 자체 하단 탭과 겹쳐 금지 — 좌우·아래를 띄운 흰 캡슐로 고쳤다
  - ⚠️ **반려 사유는 `review_get_feedback`에 있다.** `review_get`의 `rejectMessages`는 빈 배열이라 그것만 보면 오판한다
  - 옛 탭바가 찍힌 **스토어 스크린샷 4장도 다시 찍어** 올렸다 — UI를 고치면 `assets/screenshots/raw/`를 함께 의심한다
- ✅ **재심사 제출 (2026-08-26)** — `20260826-8` · `reviewStatus REVIEWING`. 콘솔 이미지 5장도 새로 올려 앱정보 검토가 함께 돌고 있다(`IN_REVIEW`)
  - 두 검토는 **별개 트랙**이다 — 앱정보가 `IN_REVIEW`여도 번들 검수 요청은 막히지 않았다(`reviewRequestDecision: ALLOWED`)
- ✅ **2차 반려 → 탭바 여백에 바닥값 (2026-08-26)** — **1차와 글자 하나 다르지 않은 같은 사유**였다([T-224](claude-docs/troubleshooting/T-224.md)). 원인은 `safe-area`에 **더하기만 한 것** — 값이 0으로 오는 환경에서는 12px밖에 안 떠서 여전히 「붙은 바」로 읽힌다. `max()`로 바닥값(24px)을 주고 좌우도 가이드 비율(30px)에 맞췄다
  - ⚠️ **「수정했는데 또 반려」면 그 번들에 수정이 들어갔는지부터 확인한다** — 커밋 머지 시각과 `bundle_list`의 `regTs`를 대조한다(이번엔 1분 22초 차로 들어가 있었다). 릴리즈 노트의 "수정했습니다"는 사람이 쓴 문장이라 근거가 못 된다
  - ⚠️ 가이드에 **수치 스펙은 없다.** 그림(`Mobile_Tabbar`) 한 장뿐이라 프레임 폭을 화면 폭으로 놓고 **비율로 환산**해야 한다. 문서 페이지는 목차만 렌더되니 `.md` 확장자로 받는다
  - SDK에 **탭바 컴포넌트는 없다** — `@apps-in-toss/*`에 `TabBar`류 export가 없고 `@toss/tds-mobile`은 설치돼 있지도 않다(README가 낡았다). 「제공하는 형태」란 그림에 맞춰 직접 구현하라는 뜻이다
- ✅ **3차 제출 (2026-08-26)** — `20260826-9` · `reviewStatus REVIEWING`. 앱정보 검토는 그 사이 `APPROVED`가 됐고 스크린샷 5장도 새 화면으로 교체 완료
  - ⚠️ 3차도 같은 문구로 반려되면 **추측을 접고 토스에 문의한다.** 수치가 가이드 그림 환산값과 맞는데도(캡슐 폭 실측 330px vs 환산 330.6px) 막힌다면, 우리가 못 읽는 기준이 따로 있다는 뜻이다
- ✅ **3차 검수 승인 (2026-08-27)** — `reviewRequestId 159653` · `status APPROVED`. **`max()` 바닥값이 정답이었다** — 세 번째 시도에서 통과했으니 문제는 끝까지 「플로팅 형태」 하나였다
- ✅ **첫 출시 (2026-08-27 12:28)** — `20260826-9`가 라이브로 갔다 🎉 사용자가 콘솔 웹에서 눌렀고, `bundle_get_live_version`으로 `deployed: true` 확인
  - ⚠️ **MCP로 불가.** 콘솔 웹 「앱 출시」 화면에서 눌러야 하고, 승인돼도 자동 출시되지 않는다
  - 근거는 두 도구 설명이 같이 말한다 — `miniapp_release_status`의 `releaseDecision: ALLOWED`는 **「버튼이 눌리는 상태」라는 뜻일 뿐**이고, `bundle_rollback`은 *"신규 출시에 해당하는 MCP tool 은 없다"*고 못 박는다
  - ⚠️ `bundle_rollback`은 **과거에 라이브였던 번들로만** 되돌린다. 첫 출시에는 못 쓴다 — 이 앱은 `liveBundleVersionName: null`이라 대상 자체가 없었다
  - 출시 확인은 `bundle_get_live_version`(null이면 아직) · `miniapp_get_status`(`PREPARE` → `OPEN`)
- ✅ **기구 선택 화면 번들 제출 (2026-08-27)** — `20260827-10` · `reviewStatus REVIEWING`. 첫 출시 뒤 곧바로 UPDATE 검수를 넣었다. 라이브는 `20260826-9` 그대로라 사용자 영향 없음
  - ✅ **올리기 전에 번들에 수정이 들어갔는지 확인했다** — [T-224](claude-docs/troubleshooting/T-224.md)에서 이 확인을 건너뛸 뻔했다. 마커는 **소스에 따옴표째 실린 리터럴만** 골랐다(밴드 path 863자·「밴드 강도」·「벤치 등판」·`1fr 1fr`·`sticky`) — 런타임 조립값은 번들에 리터럴로 없어서 마커가 못 된다
  - 업로드 검증은 HTTP 200 + **보낸 바이트 = 로컬 파일 크기**(115,234) 대조
  - ⚠️ **온보딩이 바뀌었으니 스토어 스크린샷을 다시 볼 것** — 이번엔 콘솔에 올린 4장이 홈·휴식·기구 추천·기록이라 온보딩이 안 찍혀 있어 **교체 불필요**하다(확인함). 다음에 홈·기구 탭을 건드리면 그때는 다시 찍어야 한다
- ✅ **개발용 입구를 배포 번들에서 걷어냈다 (2026-08-27)** — 실측이 끝난 `Probe` 화면(+ `returnTracker`·`verifyLog`)은 **삭제**하고, 「온보딩 다시 보기」는 `import.meta.env.MODE !== 'production'` 한 줄 뒤로 넣었다. 릴리스에서는 상수로 접혀 **트리셰이킹으로 사라진다** — 가리는 게 아니라 번들에 없다
  - 실기기 확인이 필요하면 `npm run release:dev`(`--mode development`)로 뽑으면 남는다. ⚠️ **둘이 같은 `dist/`를 쓴다** — 개발 번들을 만든 뒤 그대로 `ait deploy` 하면 개발용 입구가 딸려 나간다. 심사 번들은 반드시 `npm run release`로 다시 뽑는다
  - 검증은 양방향으로 했다 — production 번들 0건 / development 번들 1건 / **같은 파일에서 일반 사용자 문구는 1건**(계측기가 살아 있다는 대조군). 0건만으로는 검사 문자열이 틀린 경우와 구분되지 않는다
  - ⚠️ **검수 중인 `20260827-10`에는 아직 개발용 입구가 들어 있다.** 이 변경은 다음 번들부터다
- ✅ **쉐어링크 98건 테스트 번들 (2026-08-27)** — `20260827-13` · `CREATED` · `isTested`. 실기기 확인용 테스트 푸시까지만 하고 **심사는 안 넣었다**(라이브 `20260826-9` 유지, `20260827-10`이 검수 중)
  - 번들 검증은 마커 8종으로 했다 — `toss.im/_m/` **98건** + 한글 7종 존재, 대조군으로 개발용 입구 `온보딩 다시 보기`는 소스 1건 / 번들 **0건**. **0건 하나만으로는 검사 문자열이 틀린 경우와 구분되지 않아** 같은 검색이 다른 한글을 찾는 것까지 봐야 한다
  - 🔜 실기기에서 상품 버튼을 눌러 **쉐어링크가 열리는지·복귀가 되는지** 본다. 이상 없으면 다음 검수에 함께 올린다
- ⚠️ **「미니앱 주소가 바뀌었으니 서버에 허용해 달라」는 콘솔 안내는 우리와 무관하다** — 파트너가 **자기 백엔드**를 둔 경우 CORS 등을 열라는 뜻인데 이 앱은 서버가 0대다. 소스에 `tossmini.com` 주소가 한 곳도 없고(있는 건 `apps-in-toss.config.ts`의 `appName`뿐 — URL이 아니라 앱 ID라 토스가 주소를 만든다), 바깥 호출은 운동 이미지 CDN(`cdn.jsdelivr.net`)과 쉐어링크(`toss.im/_m/…`) 둘뿐이라 둘 다 우리 서버가 아니다. **읽고 넘어가면 된다**(2026-08-27 확인)
- ⚠️ **광고 지면 23일 무노출 시계** — 2026-08-26 생성이라 **9월 18일경 「삭제 예정」**으로 넘어간다. 심사 왕복이 길어지면 지면을 새로 만들고 `adPlan.ts`의 `AD_GROUP_ID`를 갈아야 한다
- ⏸ **`minAge` 19는 우리가 못 바꾼다**([T-221](claude-docs/troubleshooting/T-221.md)) — 연령은 토스가 정하고 파트너는 콘솔에서도 설정할 수 없다. 14세 노출을 원하면 콘솔 채널톡 문의가 유일한 경로다. **출시를 막지는 않는다**(북타이머도 19인 채로 `OPEN`)

---

## Phase 6 — 운동 추천 개인화

> 설계 전문은 [docs/2026-08-27-workout-reco-design.md](docs/2026-08-27-workout-reco-design.md).

지금 루틴은 **기구·목적·부위 로테이션**만 본다. 몸 상태를 안 물어서 무릎이 아픈 사람에게도 스쿼트가 나가고, 처음 하는 사람에게 상급 동작이 나간다. **다섯 PR**로 쪼개 나간다(설계 §7 개정판).

- ✅ **PR 1 — 프로필 모델 + 온보딩·설정 UI** *(알고리즘 미소비)*
  - ✅ `logic/profile.ts` — `Experience`·`AvoidArea`·`Profile` 타입과 어휘·라벨 상수. 로직 함수(`isAvoided`·`nextExperience`)는 PR 3·4 몫
  - ✅ `storage.ts` — `restfit.profile` 키 · `loadProfile`/`saveProfile` · `clearOnboarding`에 삭제 추가
    - ⚠️ **`experience`가 어휘 밖이면 프로필 전체를 `null`로 버린다** — 기구 상세처럼 필드별로 걸러 반쪽을 남기면 「부위는 먹는데 난이도는 안 먹는」 상태가 조용히 생긴다. `avoid`는 어휘 밖 값만 걸러낸다
  - ✅ 온보딩 **2단계 → 3단계** (기구 → 몸 상태 → 목적). 경험 예/아니요는 필수, 불편 부위는 「없음」 기본
  - ✅ 설정에 「운동 경험」 3단계 + 「불편한 부위」 — 상급은 질문으로 못 얻지만 **승급으로 도달**하므로 셋 다 그린다
  - ✅ 면책 두 줄을 `AvoidPicker` 한 곳에 묶었다 — 한쪽에만 붙으면 다른 쪽이 치료 효능을 주장하는 화면이 된다
  - ✅ **`pickRoutine`에 아직 안 넘긴다** — 이 PR만 배포해도 루틴은 한 톨도 안 바뀐다
  - ✅ 화면 테스트용 `jsdom` + `@testing-library/react` 도입. ⚠️ **jsdom을 전역으로 켜면 안 된다** — `import.meta.url`이 `file:`이 아니게 되어 `shareLinks` 문서 대조 테스트가 통째로 죽는다. 파일 머리 `// @vitest-environment jsdom`으로만 켠다
- ✅ **PR 2 — 상·하체 2분할 로테이션** — 부위 로테이션을 2분할로 바꿨다. 개인화와 **독립**이라 먼저 떼어 냈다(프로필 없이도 값을 낸다)
  - ✅ `exercises.ts` — `Unit`·`UNIT_KEYS`·`UNIT_OF`·`unitOf`. 상체 = 가슴·등·어깨·팔 / 하체 = 하체·코어
  - ✅ `routine.ts` — 로테이션 단위가 부위 → **유닛**(staleness·MIN_POOL 메커니즘은 유지). `Routine`이 `{unit, exercises}`
    - ✅ 유닛 안은 **부위→근육 2단 라운드로빈** — 근육을 평평하게 섞으면 상체 버킷이 9개라 "사실상 등 데이"가 나서 부위당 빈도가 보장이 아니라 확률이 된다
    - ✅ **바깥 큐의 부위 하한 2**(리뷰 지적으로 추가) — 첫 라운드가 부위마다 주는 것은 「기회」가 아니라 **확정 슬롯**이다. 맨몸 어깨는 쓸 수 있는 종목이 **상급 핸드스탠드 푸시업 1종뿐**(등은 0종)이라 초보 맨몸 사용자의 상체 세션에 **60/60 확정 배급**됐다 — 구 MIN_POOL이 부위 단위로 막던 자리가 로테이션 단위가 유닛으로 올라가며 빈 것. 전부 걸러지면 있는 것 중에서 고른다(금지가 아니라 선호)
    - ⚠️ **회복 게이트를 여기 덧붙이지 마라** — 유닛이 둘뿐이라 「가장 오래된 것 우선」이 격일 교대를 구조적으로 강제한다(어제 유닛 재선정 불가 = 24h 하드 컷 상회). 발동 케이스가 없어 상태만 늘고 「오늘은 쉬는 날」 화면이 딸려 온다. 근거는 `routine.ts` 주석과 「어제 한 유닛은 절대 다시 고르지 않는다」 테스트
  - ✅ `goal.ts` — `exerciseCount` 셋 다 4로 통일(근육·건강 3→4). **근거는 광고가 아니라 상체 4부위 커버리지**(설계 §3.8.4) — 슬롯 8→11은 부수 효과다
  - ✅ `storage.ts` — 기록 `group` 어휘를 **부위 ∪ 유닛**으로 넓히고(구 레코드가 계속 통과해야 기록이 안 사라진다) `recentGroups` → `recentUnits`(사상 후 중복 제거)
    - ⚠️ **사상 → 중복 제거 순서를 뒤집지 마라** — 뒤집으면 `'chest'`와 `'upper'`가 따로 남아 어제 가슴을 한 사람에게 오늘 상체가 다시 나간다
  - ✅ 화면 — 홈 헤드라인 "오늘은 상체/하체", `GROUP_KO`에 유닛 라벨 추가. 기록 화면은 **저장된 이름 그대로**(구 「가슴」 · 신 「상체」 혼재 허용 — 사실 그대로다)
    - ⚠️ **`legs`는 「다리」다**(리뷰 지적) — `lower`가 「하체」를 가져갔으므로 둘 다 「하체」로 두면 기록 화면에서 구 부위 기록과 새 유닛 기록이 구별되지 않는다
  - ✅ 테스트 248 → 270. 돌연변이 9종으로 판별력 실측(유닛 통과 제거 · staleness 제거 · 2단 평탄화 · 구 어휘 제거 · 어휘 검사 제거 · 사상 순서 뒤집기 · 종목 수 되돌리기 · 부위 하한 무력화 · 하한 폴백 제거 — 전부 사살)
- ✅ **PR 3 — `pickRoutine` 개인화** (부상 제외 + 티어 선호 정렬) + 홈 안내 칩
  - ✅ `profile.ts` — `AVOID_MUSCLES`·`isAvoided`. 매핑이 부위마다 다른 이유(실측 수치)를 상수 주석에 박았다
    - ⚠️ `shoulder`는 primary만 본다 — secondary까지 걸면 가슴 47종 중 43종이 전멸한다(설계 §3.2 실측). 반대로 `lowerBack`은 secondary까지 봐야 한다(primary만으로는 3종뿐이라 필터가 아무 일도 안 한다)
  - ✅ `routine.ts` — 풀 구성에서 `isAvoided` **하드 제외**(기구·부위 하한과 달리 전부 걸러져도 폴백으로 안 되살린다 — 안전 문제다) + 근육 큐를 `TIER_RANK`로 **안정 정렬**(셔플 뒤에 얹으므로 티어 안은 그대로 시드가 정한 무작위)
    - ⚠️ 난이도는 **제외가 아니라 선발 순서**다 — 하드 필터로 만들면 맨몸 어깨(상급 1종뿐)처럼 풀이 비어 폴백 규칙을 따로 지어야 한다. 선호 정렬은 폴백이 규칙에 내장된다
  - ✅ **핵심 보증**: `profile`이 `null`이면 개인화를 통째로 건너뛴다. 검증은 **2분할 시점의 실제 출력을 고정값으로 박아** 대조한다 — 「profile 없이 호출한 것과 `null`을 넘긴 것이 같다」는 자기 자신과의 비교라, 개인화가 프로필과 무관하게 늘 켜져 있어도 통과한다(돌연변이 실측으로 확인)
  - ✅ 홈 칩: 프로필 미설정 + 기록 있음이면 「경험을 알려주시면 난이도를 맞춰드려요」 → 설정 열기. 프로필이 채워지면 자연 소멸이라 dismiss 상태가 필요 없다. 빈 루틴 화면 문구에 「불편 부위」 추가
  - ✅ 설정의 「바꾸면 오늘의 루틴에 바로 반영됩니다」를 이제 쓴다 — PR 1 시점에는 거짓이라 뺐던 문구다
  - ✅ 테스트 270 → 291. 돌연변이 6종 실측(허리 secondary 제거 · 어깨 secondary 추가 · 티어 정렬 제거 · null 가드 제거 · 하드 제외 제거 · 칩 조건에서 프로필 제거 — 전부 사살)
- ⬜ **PR 4 — 세션 피드백 → 승급/강등** (`nextExperience` · 운동 완료 화면의 체감 입력)
  - 상급은 **여기서만** 도달한다 — 온보딩 질문은 초급·중급까지가 정직한 상한이다
- ⬜ **PR 5 — 졸업 프리필** — 쌓인 기록으로 프로필을 미리 채워 준다

---

## ⏸ 의도적 보류

- ⏸ **기록 클라우드 동기화** — localStorage만 쓰므로 기기를 바꾸면 날아간다. 사용자가 요구하면 토스 로그인 + 서버를 붙인다
- ⏸ 구독·AI 코칭·운동 영상·소셜/랭킹
- ⏸ **자취·이사 플래너**, **육아/반려동물 케어** — 별도 프로젝트. 경위는 [docs/ideas.md](docs/ideas.md)

---

## ⚠️ 상시 주의

- **규율이 곧 수익 구조다** — 광고는 휴식 **안**에서만. 휴식보다 길거나 운동 중에 끼어들면 이탈하고, 그러면 노출 밀도 자체가 사라진다
- 휴식 길이는 **운동이 정한다.** 광고가 정하지 않는다 — 목적별 값도 근거 권장 구간 **안에서** 고른다(체지방 감량 45초가 그 예다)
- 미니앱 React는 **18**로 핀한다 (TDS peer 제약)
- 대가성 문구를 쉐어링크가 노출되는 모든 화면에 표시
- 조회 API의 `productUrl`은 **추적되지 않는다.** 반드시 발급 API의 `shortUrl`
- 미니앱 이름·로고에 **"토스" 사용 불가**
