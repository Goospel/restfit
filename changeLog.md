# changeLog

완료 기록 (역순). 앞으로 할 일은 [plan.md](plan.md), 함정은 [claude-docs/troubleshooting.md](claude-docs/troubleshooting.md).

코드 세부는 PR·커밋에 있으니 여기엔 **왜 / 무엇을**만 적는다.
⚠️ PR 번호는 적지 않는다 — 미래에 확정되는 값이라 stale이 쌓인다. PR을 찾으려면 제목으로 `git log --grep`.

---

## 2026-08-27 · 쉐어링크 Open API는 안 쓰기로 — 조사하고 접었다

**의도**: 상품 링크를 손으로 채우는 대신 API로 일괄 수집할 수 있는지 알아봤다.

**결과**: **API는 있고 명세도 공개돼 있다**([sharelink-docs.toss.im](https://sharelink-docs.toss.im/)) — plan.md에 적어 뒀던 *"공개 문서에 명세가 없다"* 는 낡은 서술이었다. 그런데도 **안 쓰기로 했다.** 심사제라 연동 신청서를 넣어야 하고, 신청서가 요구하는 「브라우저로 열리는 서비스 URL」을 미니앱(`intoss://home-workout-hard`)은 댈 수 없다. 결정적으로 **키워드 검색 API가 없어서** 「덤벨」로 상품을 찾을 수가 없고 — 조회는 카테고리 베스트·하루특가·베스트·상품 상세뿐이다 — 운영정책은 *"Open API 데이터로 별도 상품 DB를 구성하는 경우"* 를 제한 대상으로 집는데 `shareLinks.ts`가 정확히 그것이다.

**이 API의 대상은 「매일 갈리는 편성」이다.** 문서가 *"상품 목록을 받아 게시글·구좌를 구성"* 이라 쓰고 랭킹이 시간마다 바뀌니 캐싱하라고 안내한다 — 딜 카페·오픈채팅방 봇처럼 손 복사가 물리적으로 불가능한 곳을 위한 것이다. 고정 40건을 한 번 박는 우리는 애초에 대상이 아니었다. 실사용 사례는 웹에서 하나도 못 찾았다(출시 공지조차 없다).

**대신 발급 경로가 하나 짧아졌다.** 어드민 웹(`sharelink.toss.im`)의 「링크 → 상품 조회」에서 상품 카드의 「링크 발급」을 누르면 된다 — **토스쇼핑 앱을 켤 필요가 없다.** 옛 절차를 그대로 따르고 있었다.

## 2026-08-27 · 개발용 입구를 배포 번들에서 걷어냄

**의도**: 출시된 앱의 기록 탭 아래에 개발자용 버튼 두 개가 그대로 노출돼 있었다. 배포판에서는 진입 경로를 막아야 한다.

**결과**: 둘을 다르게 처리했다. **`개발자용 · Phase 0.5 실측`은 삭제** — 실측은 2026-08-26에 끝났고(광고 8/8 · 쉐어링크 복귀 정상) 코드 주석에도 "실측이 끝나면 걷는다"고 적어 뒀던 것이라, 조건부로 살려 두는 것보다 지우는 쪽이 싸다. `Probe.tsx`·`returnTracker`·`verifyLog`가 함께 나갔다(테스트 22건 감소, 219 → 197). **`온보딩 다시 보기`는 남기되 `import.meta.env.MODE !== 'production'` 뒤로 넣었다** — 폰에서는 localStorage를 손댈 방법이 없어 이 버튼 없이는 온보딩을 실기기에서 두 번 볼 수 없다.

**숨긴 게 아니라 번들에 없다.** `vite build`의 기본 모드가 `production`이라 조건이 상수로 접히고 블록이 트리셰이킹으로 사라진다. 새 config·env 파일·런타임 플래그는 하나도 늘지 않았다 — Vite가 이미 주는 값 한 줄이다.

**검증은 양방향으로 했다.** production 번들에서 「온보딩 다시 보기」 0건, development 번들에서 1건, 그리고 **같은 production 파일에서 일반 사용자 문구는 1건**이 잡혔다. 세 번째가 대조군이다 — 0건만으로는 「제거됐다」와 「검사 문자열이 틀렸다」가 구분되지 않는다.

**⚠️ 검수 중인 `20260827-10`에는 아직 개발용 입구가 들어 있다.** 이 변경은 다음 번들부터 적용된다. 실기기 확인용으로는 `npm run release:dev`를 새로 뒀는데, **둘이 같은 `dist/`를 쓰므로** 개발 번들을 만든 뒤 그대로 `ait deploy` 하면 개발용 입구가 딸려 나간다.

## 2026-08-27 · 첫 출시 🎉 그리고 기구 선택 화면 번들 제출 (`20260827-10`)

**의도**: 승인된 번들을 라이브로 올리고, 곧바로 새 기구 선택 화면을 다음 버전으로 넣는다.

**결과**: `20260826-9`가 **2026-08-27 12:28에 출시**됐다. 사용자가 콘솔 웹에서 눌렀고 `bundle_get_live_version`으로 `deployed: true`를 확인했다. 등록 후 첫 출시까지 검수 3회 왕복이 걸렸고, 세 번 모두 사유는 **탭바 하나**였다.

이어서 `20260827-10`(2열 정사각 기구 그리드 + 아이콘 8종 + 「다음」 하단 고정)을 올려 UPDATE 검수를 넣었다 — `REVIEWING`. 라이브는 `20260826-9` 그대로라 쓰는 사람에게는 영향이 없다.

**올리기 전에 번들에 수정이 실제로 들어갔는지 확인했다.** 2차 반려 때 이 확인을 건너뛸 뻔했던 경험(T-224) 때문인데, 마커는 **소스에 따옴표째 실린 리터럴만** 골랐다 — 런타임에 조립되는 값은 번들에 리터럴로 없어서 마커 구실을 못 한다. 업로드는 HTTP 200과 함께 **보낸 바이트가 로컬 파일 크기와 같은지**로 검증했다.

**콘솔의 「미니앱 주소가 바뀌었으니 서버에 허용해 달라」 안내는 우리와 무관하다.** 파트너가 자기 백엔드를 둔 경우의 이야기인데 이 앱은 서버가 0대다. 소스에 `tossmini.com` 주소가 한 곳도 없고, 바깥 호출은 운동 이미지 CDN과 쉐어링크뿐이라 둘 다 우리 서버가 아니다.

## 2026-08-27 · 3차 검수 승인 — 탭바 사유가 세 번 만에 풀렸다

**의도**: 검수 결과를 확인하고 출시까지 진행하려 했다.

**결과**: 3차 검수(`reviewRequestId 159653` · 번들 `20260826-9`)가 **APPROVED**. 1·2차를 막았던 「플로팅 형태」 사유는 `safe-area`에 바닥값(`max()`)을 주고 좌우를 가이드 비율(30px)에 맞춘 것으로 풀렸다 — **문제는 끝까지 탭바 하나였다.**

**출시는 MCP로 못 한다.** `releaseDecision: ALLOWED`는 「콘솔 웹 출시하기 버튼이 눌리는 상태」라는 뜻일 뿐이고, 라이브 전환 도구가 아예 없다. `bundle_rollback`은 과거에 라이브였던 번들로만 되돌리는 것이라 첫 출시에는 못 쓴다(이 앱은 `liveBundleVersionName: null`). 콘솔 웹에서 사용자가 눌러야 한다.

**승인된 번들에 오늘 만든 기구 선택 화면은 없다.** 반려 3번을 지나온 만큼 일단 출시해 앱을 살리고, 새 화면은 다음 번들로 UPDATE 검수를 넣기로 했다.

## 2026-08-27 · 기구 선택을 2열 정사각 버튼으로 — 아이콘 8종 추가

**의도**: 기구 목록이 한 줄짜리 체크 항목이라 무엇을 고르는지 눈에 안 들어왔다. 칸을 크게 만들고 기구마다 앱 전용 아이콘을 붙였다.

**결과**: 170×170 정사각 2열. 칸 안에 44px 아이콘 + 이름 + 해금 수치가 들어가고, 상세 칩은 그 줄 아래 전폭으로 펼친다. 고른 칸은 테두리·바탕·체크 셋이 같이 바뀐다 — 색 하나에만 기대지 않기 위해서다. 온보딩과 「보유 기구」가 같은 컴포넌트라 양쪽이 함께 바뀌었다.

칸이 10개면 세로가 약 900px이라 「다음」이 두 화면 아래로 밀린다. **온보딩에서 다음을 못 찾는 건 그대로 이탈**이라 하단에 고정했다.

아이콘은 바벨·밴드·풀업바·벤치·짐볼·폼롤러·메디신볼·복근롤러 8종. **밴드만 면으로 채웠다** — 꼬리로 갈수록 가늘어 보이려면 잉크가 실제로 얇아져야 하는데 선으로는 안 된다(굵기는 path 하나에 하나뿐). 채우면 굽은 데 안쪽이 제 살을 파고들어서, 겹침 지수를 재며 굵기를 찾는 생성기를 함께 넣었다. 경위는 [T-225](claude-docs/troubleshooting/T-225.md).

## 2026-08-26 · 3차 검수 제출 (`20260826-9`)

**의도**: 탭바 여백·화면 아이콘·스토어 스크린샷이 모두 반영된 번들로 다시 심사를 넣었다.

**결과**: 앱정보 검토는 `APPROVED`, 번들은 `REVIEWING`. 3차도 같은 문구로 반려되면 추측을 접고 토스에 문의한다 — 수치가 가이드 그림 환산값과 맞는데도 막힌다면 우리가 못 읽는 기준이 따로 있다는 뜻이다.

## 2026-08-26 · 스토어 스크린샷 재촬영 (새 탭바 + 새 아이콘)

**의도**: 탭바 여백과 화면 아이콘이 연달아 바뀌어 콘솔에 올린 이미지가 전부 낡았다. 옛 화면이 찍힌 채로 재제출하면 심사에서 보는 것과 스토어에 뜨는 것이 다르다.

**결과**: `raw/` 4장을 다시 찍고 `render-screenshots.ps1`로 `store/` 5장(세로 636×1048 ×4, 가로 썸네일 1932×828)을 다시 냈다. 콘솔 「앱 정보」에도 반영했다. 문구는 그대로 두고 화면만 갈았다.

## 2026-08-26 · 2차 반려 — 탭바 여백에 바닥값을 줬다

**의도**: 플로팅으로 고쳐 재제출했는데 **글자 하나 다르지 않은 같은 사유로 또 반려**됐다. 수정이 번들에 들어갔는지부터 확인했고(머지 22:23 → 번들 업로드 22:24:25), 들어가 있었다 — 즉 고친 화면을 보고도 미달로 판정한 것이다.

**결과**: 원인은 `safe-area`에 **더하기만 한** 것이었다. `env(safe-area-inset-bottom)`이 0으로 오는 환경에서는 `+12px`이 그대로 띄운 거리가 되어 「밑변에 붙은 바」로 읽힌다. 개발 중 보던 브라우저가 정확히 그 환경이라 끝까지 안 보였다.

`max()`로 바닥값을 보장하고(`--tab-lift`), 좌우 여백은 가이드 그림에서 비율로 환산해 30px로 넓혔다(`--tab-side`). 실측 캡슐 폭 **330px** — 가이드 그림 환산값 330.6px과 맞는다. 경위는 [T-224](claude-docs/troubleshooting/T-224.md).

**탭 아이콘은 일부러 안 건드렸다** — 사유가 「플로팅 형태」라 아이콘과 무관하고, 둘을 같이 바꾸면 3차에서 또 반려됐을 때 원인을 못 가른다.

## 2026-08-26 · 이모지를 걷어내고 앱 전용 아이콘 6종을 그렸다

**의도**: 화면의 이모지가 기기·OS마다 다른 그림으로 뜨는 데다, 어느 앱에나 있는 남의 그림이라 화면이 「아무거나 갖다 붙인 것」처럼 보였다.

**결과**: 앱 아이콘([`assets/icon/icon.svg`](assets/icon/icon.svg))과 같은 언어 — **24 그리드 · 2px 라운드 스트로크** — 로 6종을 그려 `components/Icon.tsx` 한 곳에 모았다. 색은 `currentColor`라 탭이 활성이면 파랑, 아니면 회색으로 **부모가 정한다**(이모지로는 못 하던 것이다).

바뀐 자리는 일곱이다 — 하단 탭 3(20px) · 목적 카드 3(28px) · 홈 목적 칩(14px) · 운동 완료(48px).

**모티프를 하나 바꿨다**: 근육 키우기는 💪가 아니라 **원판**이다. 이두박근 실루엣은 20px에서 반드시 뭉개지고, 원판은 앱 아이콘에서 쏟아지는 그 흰 원판과 같은 물건이라 계보가 이어진다. `✓`·`＋`(기구 선택)는 컬러 이모지가 아니라 문자 기호라 그대로 뒀다.

시안은 세 안(가는 선 / 채운 면 / 두툼한 획)을 실제 렌더 크기로 놓고 골랐다. **40px 시안에서만 판정했다가 세 개가 20px에서 무너진** 경위는 [T-223](claude-docs/troubleshooting/T-223.md).

## 2026-08-26 · 심사 반려 — 하단 탭을 플로팅 캡슐로 고쳤다

**의도**: 첫 심사가 반려됐다. 사유는 **하나뿐**이었다 — 탭바가 토스 브랜딩 가이드의 플로팅 형태가 아니라는 것. 기능·광고·쉐어링크는 지적이 없었다.

**결과**: 탭바를 좌우·아래로 띄운 흰 캡슐로 바꾸고, 옛 탭바가 찍힌 스토어 스크린샷도 다시 찍었다.

- **원인은 「모바일 앱의 상식」이었다**([T-222](claude-docs/troubleshooting/T-222.md)). 밑변에 붙이고 윗선을 긋는 건 네이티브 앱의 표준이지만, 미니앱은 토스 앱 **안에** 얹히는 화면이라 토스 자체 하단 탭과 형태가 겹친다 — 사용자가 지금 어디에 있는지 헷갈리는 것이 가이드가 든 이유다
- ⚠️ **수치 스펙은 문서에 없다.** 가이드가 주는 건 그림 한 장뿐이라 거기서 형태를 읽어 냈다 — 흰 캡슐 + 그림자 + 사방 여백
- ⚠️ **반려 사유는 `review_get_feedback`에 있다.** `review_get`의 `rejectMessages`는 빈 배열이었다 — 그것만 보면 "사유가 안 왔다"고 오판한다
- **스토어 스크린샷 4장 중 3장이 같이 낡았다.** UI를 고치면 `assets/screenshots/raw/`도 함께 의심한다. 겸사겸사 기구 추천 장은 스크롤 끝에서 찍어 **대가성 문구가 탭에 가리지 않게** 했다
- **재제출까지 마쳤다** — 번들 `20260826-8`(`REVIEWING`) + 콘솔 이미지 5장 교체(`IN_REVIEW`). 번들 검수와 앱정보 검토는 **별개 트랙**이라 앱정보가 검토중이어도 번들 요청이 막히지 않는다

---

## 2026-08-26 · 심사 제출 — 스크린샷·썸네일을 만들어 올렸다

**의도**: 실측·광고 통합·실기기 확인이 모두 끝나 심사 전제가 풀렸다. 남은 건 콘솔에 올릴 이미지였다.

**결과**: 세로 스크린샷 4장(636×1048) + 가로 썸네일 1장(1932×828)을 만들어 등록하고, 번들 `20260826-7`을 심사에 넣었다(`REVIEWING`).

- **재생성 스크립트로 남겼다**([`render-screenshots.ps1`](scripts/render-screenshots.ps1)). ⚠️ 콘솔은 **1px만 달라도 거부**하는데 **Chrome은 실패해도 종료코드가 0**이라, 성공 판정을 산출 파일의 실제 픽셀로 한다
- ⚠️ **636×1048은 폰보다 뭉뚱하다**(1.65:1 vs 2.16:1). 앱 화면을 그대로 넣으면 아래가 텅 비어 앱이 빈약해 보인다 — 캔버스에 문구를 얹고 화면은 비율을 지켜 배치했다
- **기구 추천 장만 맨몸 상태로 찍는다.** 덤벨을 가진 상태면 덤벨이 추천에서 빠져 **이 화면의 핵심인 「+96개(2.7배)」가 안 보인다**
- **썸네일은 기능이 아니라 태도로 연다** — 「루틴을 대신 짜준다」는 흔한 약속이라 구미를 못 당긴다는 지적을 받아 *"네, 어렵습니다. / 그래서 만들어 봤습니다. 같이 해봅시다."* 로 바꿨다
- **상세 설명도 함께 고쳤다.** 기구 탭이 추천 화면이 되면서 옛 설명("기구 탭에서 체크해요")이 실제와 어긋나 있었다 — 그대로 올렸으면 심사에서 걸릴 자리였다
- ⚠️ **올린 이미지는 검토 승인 뒤에야 조회에 보인다**([T-220](claude-docs/troubleshooting/T-220.md)). 같은 요청의 텍스트는 즉시 반영되는데 이미지만 빈 배열이라 실패로 오인하기 쉽다
- ⚠️ **연령(`minAge` 19)은 우리가 못 바꾼다**([T-221](claude-docs/troubleshooting/T-221.md)). 딥시그널이 14세인 걸 근거로 「설정 가능한 값」이라 추론했는데, 그건 **게임이라 등급분류를 받은 것**이었다. 비게임은 토스가 정한다 — **조작 방법을 확인하기 전에 「바꾸세요」라고 안내한 것**이 실수였다

## 2026-08-26 · 기구 추천을 2단으로 + Phase 0.5 ② 통과

**의도**: 기구 카드를 누르면 특정 상품 하나로 곧장 나가 버려서, 앱 안에서 **고를 수가 없었다.** 상품 목록을 앱에 두되 해금 수치를 잃지 않는 구조가 필요했다.

**결과**: 1단은 「몇 개가 열리는지」, 펼치면 2단으로 「무엇을 사면 되는지」. 상품을 곧바로 늘어놓으면 **「+96개」가 상품 사이에 묻혀 사야 하는 이유를 잃는다** — 숫자로 이유를 먼저 말하고 그다음에 살 것을 보여준다.

- 한 번에 **하나만 펼친다** — 다 열면 다시 「쫘라락」이 되어 나눈 뜻이 없다
- ⚠️ **가격은 안 넣는다.** 정적 파일이라 바뀌어도 못 따라가고, **틀린 가격은 없는 것만 못하다**
- ⚠️ **미니앱 안에 토스쇼핑을 띄우는 방법은 플랫폼에 없다.** SDK도 딥링크도 WebView API도 없다는 게 앱인토스 개발자 커뮤니티의 [공식 답변](https://techchat-apps-in-toss.toss.im/t/topic/4102)이다 — 링크는 외부 브라우저로 열린다. **우리 구현의 한계가 아니라 플랫폼 제약**이라, 이걸 모르면 고치려다 시간을 버린다

**그리고 Phase 0.5 ②가 통과했다.** 기구 탭에서 토스쇼핑을 열고 돌아왔는데 **기구 탭 그대로였다** — 미니앱이 재시작되지 않았다는 뜻이다. 설계 때 「쉐어링크를 열면 복귀가 불안정하다」는 제보를 리스크로 달아 뒀는데 우리 앱에는 해당하지 않는다. 검증을 Probe 화면이 아니라 **실사용 경로에서** 한 것이라 더 강한 증거다. **이로써 Phase 0.5가 완전히 끝났다.**

## 2026-08-26 · 첫 쉐어링크(덤벨) + 대가성 문구를 공식 문장으로

**의도**: 추천 화면은 만들어 뒀는데 링크가 없어 버튼이 안 떴다. 덤벨 링크 하나를 받아 붙였다.

**결과**: 덤벨 카드에 「토스쇼핑에서 보기」가 뜬다. 나머지 넷은 링크가 없어 **버튼만 안 뜨고 추천은 그대로 보인다** — 설계한 대로다.

- ⚠️ **대가성 문구를 임의로 지어 놨던 것이 드러났다.** 토스가 쉐어링크를 발급할 때 **정확한 문장을 함께 준다**(*"✱ 이 포스팅은 토스쇼핑 쉐어링크 활동의 일환으로…"*). 뜻이 같아도 내가 쓴 문장은 규정 위반이라 `DISCLOSURE` 상수로 박고 화면은 그것만 참조한다
- ⚠️ **「풀업바」로 검색하면 푸쉬업바가 섞여 나온다** — 실제로 첫 링크가 그랬다. 푸쉬업바를 요구하는 운동은 데이터에 **0개**(푸시업 계열이 전부 맨몸으로 분류돼 있다)라 폼롤러와 같은 처지다. **손목을 편하게 해줄 뿐 할 수 있는 운동을 늘리지 않는다** — 앱의 추천 기준이 그대로 걸러낸 셈이다
- 5kg 고정 중량이지만 **`requires`는 무게를 안 보므로 덤벨 운동 123개가 전부 열린다.** +96개는 그대로다
- 문구가 하단 탭에 가려 보였는데 **실측하니 24px 여유가 있었다** — `fullPage` 스크린샷이 fixed 탭을 스크롤 끝에 그려서 생긴 착시였다(관측 경로를 먼저 의심하라는 그 경우다)

## 2026-08-26 · 기구 탭을 추천 화면으로 — 「막무가내로 보여주지 않는다」

**의도**: 홈의 목적 칩과 하단 「기구」 탭이 **같은 화면으로 갔다.** 진입점이 둘인데 목적지가 하나면 탭 하나가 낭비다. 기구 탭을 쉐어링크가 붙을 추천 화면으로 돌리고, 보유 기구·목적은 목적 칩에서 여는 전체화면으로 갈랐다.

**결과**: 안 가진 기구 중 **지금 못 하는 운동을 많이 열어주는 것**만 순서대로 보여준다. 설득은 "이거 사세요"가 아니라 **「+96개」라는 숫자**가 한다.

- **선별을 두 겹으로 했다** — 후보 목록(사람이 정한다)과 최소 해금 수(데이터가 정한다)
- ⚠️ **바벨을 뺀 건 데이터를 거스른 판단이다.** 운동 수 2위(+95)로 덤벨 바로 다음인데, 숫자만 믿으면 **원룸 사용자에게 200cm 봉과 랙을 권하게 된다.** 안전 문제도 혼자 드는 사람에겐 다르게 걸린다
- ⚠️ **폼롤러는 근력 운동이 +0이었다** — 사도 루틴이 한 개도 안 바뀐다. 스트레칭 도구지 근력 기구가 아니다. 메디신볼(+2)·복근롤러(+1)·짐볼(+9)과 함께 뺐다
- **`MIN_GAIN = 10` 하나가 벤치를 가른다** — 맨몸이면 +4라 안 뜨고, 덤벨을 가진 순간 +12가 되어 나타난다. 조건문 없이 「지금 뭘 가졌나」가 추천을 바꾸는 자리라 실제 데이터로 양쪽을 테스트했다
- ⚠️ **`recommend`는 근력 필터를 안에서 한다.** `unlockGain`은 호출부에 맡기는데, **이 함수의 첫 테스트가 그 함정에 그대로 걸렸다** — 맡기면 잊는다는 증거라 안으로 넣고 테스트로 못 박았다
- 배수는 **1.5배 이상일 때만** 띄운다. 화면에 「1.1배」가 셋씩 늘어서니 정보가 아니라 소음이었고, 「겨우 1.1배」로 읽혀 +18개라는 사실까지 깎아먹었다
- **쉐어링크는 아직 비어 있다**([`shareLinks.ts`](miniapp/src/data/shareLinks.ts)). 링크 없는 기구는 버튼만 안 뜨고 추천은 그대로 보인다 — 파일만 채우면 살아난다. 대가성 문구도 **링크가 하나라도 있을 때만** 뜬다
- 테스트 204 → **216건**

## 2026-08-26 · 휴식에 광고를 붙였다 — 8/8 실측 위에서

**의도**: Phase 0.5 ①의 답이 나왔다. **8회 시도, 8회 전부 노출 성공.** 이 아이템의 수익 전체가 걸려 있던 가정이 통과했으므로 광고를 실제 휴식에 붙인다.

**결과**: 휴식이 시작되면 `adPlan`이 판단하고, 틀기로 하면 load → show 한다. 광고는 **수익이지 기능이 아니라서**, 실패해도 사용자에게 아무것도 안 보인다.

- **실측 조건이 실사용보다 가혹했다** — 연속으로 눌러댄 8회다. 실제 휴식은 45~150초 간격이라 더 불리할 이유가 없다. frequency cap이 있었다면 애드몹 통상값인 3~5회에서 걸렸어야 하는데 안 걸렸다
- **배너 병행은 접었다.** plan에 「전면 광고 빈도가 낮게 나왔을 때만」이라는 조건부 항목이었고, 조건이 성립하지 않았다
- ⏸ **preload도 안 넣었다.** 순차(load→show)로 8회가 무손실이었고, 미리 받아 두면 마지막 세트처럼 **쓰지 않고 버리는 로드**가 생긴다. 광고가 눈에 띄게 늦게 뜨면 그때 넣는다
- **휴식 타이머는 한 줄도 안 고쳤다** — `restEndsAt`이 절대 시각이라 광고가 웹뷰를 가려 타이머가 스로틀돼도 남은 시간이 정확하다. 광고 자리를 비워 둔 채로 완성해 둔 설계가 값을 했다
- ⚠️ **미충전이어도 `slotsSinceLastTry`는 0으로 되돌린다** — 시도는 했기 때문이다. 안 그러면 백오프가 즉시 풀려 다음 휴식에 또 광고 서버를 두드린다. 이 전이는 테스트로 못 박았다
- 광고 상태는 `useRef` — 화면에 그릴 값이 아니라 state로 두면 광고 판단이 리렌더를 부른다
- **실기기 확인 완료**: 휴식에 광고가 뜨고, **닫으면 남은 휴식이 그만큼 줄어 있다.** 광고는 휴식 화면이 뜨고 **약 2초 뒤** 덮는다 — preload를 접은 판단이 여기서 확정됐다
- 테스트 198 → **204건**

## 2026-08-26 · 광고 지면 개설 + 이미지 CSP 리스크 해소

**의도**: 수익 모델 전체가 「휴식마다 광고를 틀 수 있는가」 하나에 걸려 있는데, 잴 지면이 없어 못 재고 있었다.

**결과**: 전면 광고 지면을 만들었다 — `groupId` **`ait.v2.live.f491615603f04220`**. 실측 도구는 이미 `adGroupId`를 입력받게 돼 있어 **코드 변경도 재배포도 필요 없었다.**

- 전면 광고는 `categoryId`가 **필수**다. 미니앱에 이미 붙어 있던 **생활 › 건강**(`3804`)을 그대로 썼다
- ⚠️ **문서가 예고한 `REGISTERING` 2시간 대기가 실제로는 없었다** — 생성 응답이 곧바로 `ENABLED`에 `groupId`까지 채워서 왔다. 「2시간 뒤에 재조회」를 전제로 일정을 잡을 필요가 없다
- ⚠️ 만든 순간부터 **23일 무노출 = 삭제 예정** 시계가 돈다
- **운동 이미지가 실기기에서 정상 표시됐다.** 설계 때부터 「미니앱 CSP가 막을 수 있다」고 달아 둔 미확인 리스크였고, 막혔으면 900장을 번들에 넣거나 호스트를 옮겨야 했다. jsDelivr CDN 그대로 간다

## 2026-08-26 · 기구 상세(무게·벤치 종류) — 그리고 평벤치 사용자에게 인클라인을 시키던 버그

**의도**: 기구를 「있다/없다」로만 물어서, 덤벨이 2kg인지 20kg인지 앱이 몰랐다. **「모름」을 1급 선택지로** 두는 게 전제였다 — 기준을 모르는 사람이 많고(밴드가 대표적이다), 모른다고 답해도 앱이 그대로 돌아야 한다.

**결과**: 기구를 고르면 그 아래 상세가 펼쳐진다. 무게는 구간(가벼움·보통·무거움·모름)으로 받고, 벤치는 평벤치/조절식/모름으로 받는다.

- ⚠️ **조사 중에 실제 버그가 드러났다.** 인클라인 28개 중 23개, 디클라인 12개 중 11개가 벤치를 요구하는데 데이터가 이걸 평벤치와 한 덩어리로 묶고 있었다 — **평벤치만 가진 사람에게 인클라인 벤치프레스가 나왔다.** 실제로 그날 첫 루틴이 그거였다
- 데이터 빌드에서 `benchAdjustable`을 갈랐다(`NEEDS_ADJUSTABLE = /incline|decline/i`). 승격은 `requiresOf` **끝**에서 한 번에 걸어 수동 예외(`Decline_Crunch` 등)까지 덮는다
- 조절식을 가진 사람은 평벤치 운동도 되므로 `effectiveOwned()`가 두 키를 함께 세운다. **「모름」은 안전하게 평벤치로 친다** — 모른다고 답한 사람이 못 하는 운동을 받는 쪽이 더 나쁘다
- **실측**: 벤치 「모름」 165개 → 「각도 조절」 **187개**(+22). 덤벨 「보통」을 고르면 운동 화면 첫 무게가 **0 → 10kg**으로 채워진다
- ⚠️ **무게 구간은 기구마다 다르다** — 덤벨 15kg은 무겁고 바벨 15kg은 가볍다. 한 테이블로 뭉치면 기본값이 엉뚱해진다
- **밴드 강도는 받되 앱이 쓰지 않는다.** 밴드 운동 23개는 강도와 무관하게 전부 된다 — 「여러 개 있어요」를 맨 위에 뒀다
- 풀업바·짐볼·폼롤러·복근롤러는 **아예 묻지 않는다.** 물어볼 스펙이 없고, 묻는 만큼 온보딩이 길어진다
- 테스트 168 → **198건**

## 2026-08-26 · 온보딩을 다시 볼 길을 냈다 + 콘솔 MCP로 배포 자동화

**의도**: 온보딩은 **한 번 끝내면 실기기에서 두 번 볼 수 없었다.** 폰에서는 localStorage를 손댈 방법이 없으니, 온보딩 화면을 고쳐도 확인할 길이 없다는 뜻이다. 배포도 매번 수동 웹 업로드였다.

**결과**: 기록 탭 개발자용 줄에 「온보딩 다시 보기」를 넣어 첫 진입 상태로 되돌린다. 그리고 앱인토스 콘솔 MCP를 붙여 번들 업로드·빌드 대기·테스트 발송을 Claude가 직접 한다.

- **`clearOnboarding()`은 목적만이 아니라 기구도 지운다** — 목적만 지우면 온보딩 1단계에 고른 기구가 남아 **첫 진입 경험이 재현되지 않는다.** 재현이 이 버튼의 존재 이유라 둘 다 지운다
- **운동 기록은 건드리지 않는다** — 온보딩을 다시 보려다 그동안 쌓은 기록이 날아가면 안 된다. 테스트로 못 박았다
- 배포 경로 확정: `bundle_upload` → 발급된 S3 URL에 PUT → `bundle_upload_complete` → 빌드 폴링 → `bundle_test_push`(테스트 푸시 + `privateLink`)
- ⚠️ **CLI용 API 키는 워크스페이스 왼쪽 「키」 메뉴에 있다** — 앱 안 개발 섹션이 아니라서 한참 못 찾았다. 결국 콘솔 MCP로 가서 키 자체가 불필요해졌다
- 테스트 164 → **168건**

## 2026-08-26 · 온보딩과 운동 목적 — 반복·휴식·종목 수를 목적이 정한다

**의도**: 첫 진입 사용자가 아무 맥락 없이 루틴부터 보게 돼 있었다. **기구 → 목적** 두 단계를 앞에 세우고, 고른 목적이 실제로 루틴을 바꾸게 했다.

**결과**: 체지방 감량 / 근육 키우기 / 건강 유지 세 목적이 **반복 횟수 · 휴식 길이 · 종목 수 상한**을 함께 정한다. 기구 탭에서 바꾸면 오늘의 루틴에 바로 반영된다(실측: 12~20회·60초·15분 ↔ 6~12회·150초·29분).

- ⚠️ **체지방 감량 휴식을 45초로 고른 이유** — 근거 권장은 30~60초인데 아래쪽을 쓰면 `MIN_REST_SECONDS`(40초)에 걸려 **광고가 통째로 사라진다.** 광고 때문에 늘리는 것도 금지라(「휴식은 운동이 정한다」), **권장 구간 안에서 40초를 넘는 값**을 골랐다. 이 불변식은 `goal.test`와 `session.test` 양쪽에서 못 박았다
- 짧게 쉬는 목적은 종목을 늘려 세션 길이를 지킨다 → **휴식 횟수는 체지방 감량이 더 많다**
- ⚠️ 종목 수는 **상한일 뿐이다.** `pickRoutine`이 근육 수와 `MAX_PER_MUSCLE`로 한 번 더 조여서, 가슴처럼 근육이 하나인 부위는 4를 넘겨도 3종목이 최대다. 화면 문구를 「최대 N종목」으로 쓴 이유
- 기구 선택 UI를 `EquipmentPicker`로 뽑아 온보딩과 기구 탭이 공유한다 — 갈라지면 고른 것과 보이는 것이 달라 보인다
- 테스트 140 → **164건**

## 2026-08-26 · 화면 시안을 Figma로 옮기고 PNG를 레포에 남겼다

**의도**: 화면을 코드 밖에서 눈으로 놓고 고칠 자리가 없었다. Figma 원격 MCP를 붙여 5개 화면을 옮기고, 리뷰·PR에서 바로 보이도록 PNG를 [`assets/mockups/`](assets/mockups/README.md)에 커밋했다.

**결과**: 아트보드 6장(오늘의 루틴 / 운동 진행 / 휴식 / **휴식·배너 광고안** / 기록 / 보유 기구). 색·간격은 `index.css` 변수를, 기구 해금 수치는 `exercises.json`에서 계산한 실제 값을 넣어 앱 화면과 같은 값이다.

- **배너 광고안 한 장만 코드에 없는 안**이다. ⚠️ 전면 광고는 시스템이 화면을 덮으므로 **레이아웃을 바꾸지 않는다** — 자리를 비워 둘 필요가 없다는 뜻이고, 배너를 실제로 쓸지는 Phase 0.5 실측에 달렸다
- 시안은 **코드의 복제본이지 단일 출처가 아니다.** 코드를 고쳐도 따라오지 않으니 그때마다 다시 뽑는다
- 폰트 함정 하나를 [T-219](claude-docs/troubleshooting/T-219.md)에 남겼다 — 목록에 있고 로드도 성공하는데 실재하지 않아 타이머가 통째로 안 그려졌다

## 2026-08-25 · 광고 정책 확인 — 수치 상한이 없다

**의도**: plan에 *"정책이 막으면 실측 이전에 끝이다"* 로 걸어 둔 항목이다. 콘솔 등록 신청이 들어가 심사를 기다리는 동안, **키 없이 확인되는 것**부터 치웠다.

**정책에 횟수 상한이 없다.** [인앱 광고 가이드](https://developers-apps-in-toss.toss.im/guide/monetization/in-app-ad)에는 *"사용자에게 광고가 너무 자주 노출되지 않도록 주의해 주세요"* 라는 권고만 있고 하루 몇 회·세션당 몇 회 같은 수치가 없다. **모델이 정책으로 죽지는 않는다** — 그러나 이건 *상한이 없다*는 뜻이 아니라 **상한이 정책이 아닌 다른 곳(fill rate)에 있다**는 뜻이다. Phase 0.5 ①의 실기기 실측은 여전히 유일한 답이고, 여전히 결정적이다.

**금지 조항은 우리 설계가 전부 통과한다.** 결제·계좌 개설 흐름 중 삽입 금지 / 종료·뒤로가기 방해 금지 / 필수 CTA 가리기 금지. 우리는 휴식 중에만 틀고, 광고가 도는 동안에도 휴식 타이머는 그대로 흐르며, 실패는 조용히 넘어간다. 제재가 **30일~영구 이용 제한 + 수익 환수**라 지킬 값이 크다. 앱인토스 인앱 광고는 **구글 애드몹 정책**을 따른다.

**측정 도구가 현행 API를 쓰는지도 확인했다.** `loadFullScreenAd`/`showFullScreenAd`는 현행이다(deprecated는 `TossAds.attach` → `attachBanner`, 그리고 `GoogleAdMob` v2 호환 별칭). **재는 도구가 낡았으면 측정값이 아니라 측정 자체가 틀린다** — 결정적 실측을 앞두고 확인할 값이었다.

## 2026-08-25 · appName은 표시 이름이 아니다 — `home-workout-hard`로 정정

**의도**: 콘솔 등록 화면이 `appName`을 *"수정할 수 없는 고유 ID"* 라고 표시했다. 우리 설정에는 한글 `홈트가어렵나`가 들어 있었으니, 되돌릴 수 없는 값을 넣기 전에 근거를 확인해야 했다.

**CLI 번들이 답을 갖고 있었다.** 검증 문구가 *"앱 이름은 케밥-케이스 형식이어야 합니다"* 이고, 무엇보다 `appName`이 **URL에 그대로 박힌다** — `intoss-private://<appName>?_deploymentId=…` 와 `…/bundles/<appName>/upload-start`. 한글이면 스킴의 host 자리에 한글이 들어간다.

**「빌드 통과」를 유효성의 증거로 삼은 게 틀렸다.** 이전 작업에서 한글 `appName`으로 `ait build`가 성공하는 걸 보고 "한글이 통과한다"고 적었는데, 그 검증은 `문자열인가 && 빈칸 아닌가` 그것뿐이었다. **약한 검사기를 통과한 것은 아무것도 증명하지 않는다** — 번들 문자열 grep이 양방향으로 고장 난 계측기인 것과 같은 종류의 착각이다. T-218에 남겼다.

**표시 이름과 ID를 분리했다.** 사람이 보는 이름은 **홈트가어렵나**(콘솔에서 수정 가능), 설정의 `appName`은 **`home-workout-hard`**(수정 불가). 아티팩트도 `home-workout-hard.ait`가 된다.

**아이콘 크기도 틀렸다.** 콘솔 「앱 로고」는 **600×600**을 요구하는데 렌더 기본값에 600이 없었고, `assets/icon/README.md`·`plan.md`에는 *"콘솔에는 `icon-1024.png`를 올린다"* 고 적혀 있었다 — **올려 본 적이 없으니 확인된 적도 없는 문장**이었다. 600을 기본값에 넣고 문서를 고쳤다. 원본이 벡터라 크기 요구가 바뀌어도 `-Sizes`에 숫자만 넣으면 된다.

## 2026-08-25 · 앱 아이콘 — 벤치에서 흘러내리는 덤벨

**의도**: 콘솔 등록에 아이콘이 필요하다. 앱 이름이 던진 질문에 홈 화면이 *"네, 어렵습니다"* 로 답하는데, **아이콘도 같은 말을 하게** 했다.

**AI 생성을 접고 SVG로 직접 그렸다.** HiggsField로 뽑아 봤더니 **출력 PNG에 워터마크가 픽셀로 박혀** 나왔다(다운로드해서 확인). 가격 페이지 전문 14,533자에 "watermark"라는 단어가 한 번도 없어서 **유료 전환이 이걸 푸는지조차 확인이 안 된다.** 월 $19를 내고 나서야 아는 건 나쁜 거래다. 형태 자체가 곡선 하나에 도형 몇 개라 AI가 필요한 그림도 아니었고, 벡터로 가니 워터마크·크레딧·해상도 문제가 한꺼번에 사라졌다.

**시안 15개를 거치며 오독을 세 번 만났다.** 헤드폰(봉이 위로 굽으면 아치+원판이 이어캡) → 웃는 얼굴(곡선을 뒤집으니 눈 둘에 미소) → 말굽자석(깊게 처지니 봉과 원판이 U자 덩어리로 뭉침). **셋 다 좌우대칭에서 나왔고**, 비대칭 구도로 가니 한 번에 풀렸다. 판별 기준은 [`assets/icon/README.md`](assets/icon/README.md)에 남겼다 — 다시 손댈 때 같은 길을 또 걷지 않도록.

**「봉을 휘면 덤벨이 아니게 된다」는 평면에서만 참이었다.** 3D에서는 명암이 재질을 알려줘서, 봉이 휘어도 원판이 원통 형태를 지키면 「같은 물체가 늘어지는 중」으로 읽힌다. 평면에서 얻은 결론을 3D에 그대로 옮긴 게 성급했다.

**PNG는 산출물이고 단일 출처는 `icon.svg` 하나다.** `scripts/render-icon.ps1`이 헤드리스 Chrome으로 6개 크기를 뽑고 **실제 픽셀 크기까지 검증**한다 — Chrome은 실패해도 종료코드가 0인 경우가 있어서다.

## 2026-08-25 · 배포 준비 — 앱 이름 확정 「홈트가어렵나」

**의도**: 광고를 재려면 앱이 실기기에 올라가 있어야 하고, 올리려면 콘솔 등록이 먼저다. 등록에 필요한 것부터 치우고, **키 없이 확인할 수 있는 데까지 미리 확인해 뒀다.**

**빌드 체인은 키 없이 전부 통과한다** — `npm run build` → `ait build` → `홈트가어렵나.ait`(109KB). 막히는 지점은 `ait deploy` 하나뿐이고 그건 콘솔 API 키가 필요하다. ~~한글 `appName`이 그대로 통과하는 것도 실측으로 확인했다.~~ ← **이 문장은 틀렸다.** 빌드만 통과할 뿐 deploy에서 깨진다. 아래 「appName은 표시 이름이 아니다」 항목이 정정본이다.

**이름이 던진 질문에 앱이 답한다.** 「홈트가 어렵나?」 → **"네, 어렵습니다."** 응원이 아니라 동의다. 그리고 제일 어려운 부분(뭘 할지 정하기)은 이미 정해뒀다고 말한다 — 이 앱이 실제로 하는 일이 그거다. 기록이 없을 때만 뜬다. 매일 보이면 농담이 아니라 잔소리가 된다.

**`npm run release`로 배포 2단계를 묶었다.** `ait build`는 `dist/`를 있는 그대로 싸기 때문에 빌드를 빼먹으면 옛 번들이 그대로 올라간다 — BookTimer T-150이 정확히 그 사고였다.

**당겨서 새로고침을 껐다.** 진행 중인 세션은 React state에만 있어서 새로고침 한 번에 그날 기록이 통째로 날아간다. 운동 중에 화면을 당기는 건 흔한 동작이다. 세션을 저장소에 넣기 전까지 이 설정 한 줄이 유일한 방어다.

**함정 1건**: `sed -i`가 CRLF 파일을 LF로 뒤집었는데 **훅이 지시한 `git diff --numstat` 검증이 정상으로 나왔다.** 이 레포는 `core.autocrlf = true`라 git이 diff 전에 줄끝을 정규화해버려서, numstat은 EOL 뒤집힘을 **원리적으로** 못 본다. 바이트로 확인해야 한다 (T-217).

## 2026-08-25 · Phase 2 — 화면 4개 (광고 없이 먼저)

**의도**: 광고가 없어도 앱이 온전해야 광고가 실패했을 때도 온전하다. 그래서 광고 자리를 **일부러 비워둔 채** 완성했다.

보유 기구 / 오늘의 루틴 / 운동 진행 / 기록 네 화면과 하단 탭. Phase 0.5 실측 도구는 기록 화면 아래 임시 입구로 살려 뒀다 — 실기기 측정이 아직 안 끝났다.

**화면을 만들자 데이터 결함 3건이 드러났다 — 로직 테스트로는 절대 안 잡혔다**

첫 화면을 열자 기구가 하나도 없는데 **풀업 3종**을 추천하고 있었다. 원본이 "자기 체중을 든다"는 뜻으로 풀업·행잉을 `body only`로 적어 뒀기 때문이다. 기준은 자기 체중이냐가 아니라 **매달 데가 있느냐**다. 9개를 `pullupBar`로 옮기고 회귀 테스트를 걸었다.

다음 루틴은 어깨였는데 3종목 중 2개가 **목 아이소메트릭**이었다. 홈트 루틴에 목 운동을 넣는 앱은 없다. 제외했다.

그 다음은 팔이었는데 **5종목이 전부 삼두**였다. 맨몸으로 이두를 할 수 없어 근육 버킷이 하나뿐이라 그렇다. 한 근육에 3개 상한을 넣었다 — 한 근육에 15세트는 과훈련이고, **그런 루틴은 사람을 떠나게 해서 광고 슬롯도 함께 사라진다.** 부위 최소 운동 수(3)도 같이 넣었다. 1종목짜리 "루틴"은 루틴이 아니다.

그 결과 맨몸 로테이션은 코어·팔·하체·가슴 4개로 돈다. **등은 0개, 어깨는 1개** — 매달 데 없이 등 운동은 불가능하니 정직한 결과이고, 덤벨 하나로 등 6개·어깨 35개가 열린다. 해금 훅에 이빨이 생겼다.

**돌연변이 하나가 살아남아 공허한 테스트를 잡았다**

`todayKey`의 KST 지정을 통째로 지웠는데 테스트가 통과했다. **개발 기계가 이미 한국 시간**이라 지정이 있으나 없으나 같은 값이 나왔기 때문이다. 시간대를 인자로 빼고 다른 시간대에서 답이 달라지는지를 보게 고쳤다. T-216으로 남겼다 — 로케일·통화·인코딩에 그대로 되풀이되는 구조다.

**측정**: 맨몸 3종목 = 9세트 → 휴식 8회. 덤벨 5종목 = 15세트 → **휴식 14회.** 이것이 세션당 광고 슬롯 수다.

**검증**: 테스트 140건. 돌연변이 16종 중 15종 사살 + 살아남은 1종은 원인을 고쳐 사살. 브라우저에서 전체 흐름(9세트 → 완료 → 기록 저장 → localStorage 확인)을 걸었고, 모바일 375×812에서 가로 넘침 0·글자 잘림 0·버튼 높이 51~55px를 수치로 확인했다. **스크린샷은 없다** — 브라우저 패널이 표시되지 않아 합성이 멈췄다(CLAUDE.md 함정 ①).

---

## 2026-08-25 · Phase 1 — 운동 데이터 정제 + 순수 로직 4종

**의도**: 실기기 광고 실측(Phase 0.5)을 기다리는 동안, 실측 결과와 무관한 부분을 전부 끝내 둔다.

**장비를 단일 값이 아니라 집합으로 바꿨다**

원본(free-exercise-db)의 `equipment`는 값이 하나뿐이라 **덤벨 벤치프레스가 벤치를 요구한다는 사실을 표현할 수 없다.** 그대로 쓰면 덤벨만 가진 사람에게 벤치프레스를 추천한다. `requires: []` 집합으로 바꿔 덤벨 27 · 바벨 27 · 밴드 1개의 벤치 의존을 살렸다. 밴드 보조 풀업(밴드+풀업바)도 같은 방식으로 표현된다.

**제외를 기본값으로 뒀다**

`other` 122개는 아틀라스 스톤·썰매·타이어·TRX 같은 잡동사니다. 하나씩 분류하는 대신 **홈트에서 되는 것만 골라 담고 나머지는 버렸다**(122 → 33). 잘못 뺀 운동은 안 보일 뿐이지만, 집에서 아틀라스 스톤을 추천하면 앱 전체를 못 믿게 된다. 머신·케이블 148개는 통째로 뺐다.

873 → **647개 채택**. 근력 415개 기준 해금 수치는 **맨몸 81 → 덤벨 176 (+95, 2.17배)** 로, 설계가 가정한 2.1배가 정제 후에도 살아남았다. 덤벨이 단일 최대 해금 장비라는 것도 그대로다 — 첫 쉐어링크 추천의 근거다.

**한글화는 통째 번역이 아니라 토큰 사전으로 했다**

한국 헬스 용어는 대부분 영어 음차이고 어순도 같다("Incline Dumbbell Curl" → "인클라인 덤벨 컬"). 647개를 손으로 옮기면 같은 단어가 파일마다 다르게 적히지만, 사전은 한 번 정하면 끝까지 일관된다. **미매핑 토큰이 하나라도 있으면 빌드가 실패한다** — 영어가 조용히 섞여 나가는 게 최악이라서.

**순수 함수 4종을 TDD로 만들었다**

`filterByEquipment` · `unlockGain` · `pickRoutine` · `adPlan`. 모두 실패 테스트를 먼저 쓰고 Red를 확인한 뒤 구현했다.

`adPlan`이 이 앱의 심장이다 — 휴식 40초 미만은 안 틀고(규율이 곧 수익 구조다), 미충전이 연속되면 그만큼 슬롯을 쉬고, 3회 연속이면 남은 세션을 조용히 포기한다. **광고를 못 틀어도 앱은 정상 동작해야 한다.**

`pickRoutine`의 seed는 날짜다. 같은 날 재접속해도 루틴이 안 바뀐다. 부위 로테이션은 「가장 오래 전에 한 부위」로, 균형은 부위 안에서 근육별 라운드로빈으로 잡았다 — 그냥 섞어 뽑으면 대퇴사두만 5개 나오는 날이 생긴다.

**검증**: 테스트 84건 통과. **돌연변이 10종을 주입해 전부 사살**(생존 0) — 필터 조건 뒤집기, 경계 밀기, 백오프·균형·시드 무력화 등. 주입이 실제로 일어났는지는 `git diff --numstat`으로 따로 확인했다.

**안 한 것**: `instructions`(운동 설명문)를 데이터에서 뺐다. 647 × 5문장 번역은 크고, 설계상 운동 진행 화면은 세트 기록 + 타이머라 설명문 자리가 없다. 이미지 2장이 동작을 보여준다. 필요해지면 그때 넣는다.

---

## 2026-08-25 · 아이템 확정 — 홈트 + 휴식 시간 광고 (restfit)

**의도**: 「가격 이력 검증」을 실물 확인 후 기각하고, 접었던 홈트 안을 **새 수익 모델로 부활**시켰다.

**가격 이력 검증을 기각한 이유** — 전제가 사실과 달랐다

토스쇼핑 상품 상세에 **「최대할인가」 표시와 「가격 비교하기」 버튼이 이미 있었다.** 눌러보면 네이버·11번가·쿠팡·G마켓 크로스몰 비교가 뜨고 해당 몰로 이동까지 된다. *"커머스 사업자는 자기 할인이 가짜라고 말 못 한다"* 는 핵심 전제가 틀렸다 — **이 회사는 자기 매출을 깎으면서까지 타 몰로 보내주며 가격 정직성을 브랜드로 쓰고 있다.** 남은 자리(시간축)는 비어 있었지만 해자가 60일뿐이라, 플랫폼이 채울 자리에 밖에서 들어갈 이유가 없었다.

**홈트를 부활시킨 것은 기능이 아니라 수익 모델이다**

세트 사이 휴식 타이머 동안 전면 광고를 재생한다. 보통 리워드 광고는 "참으세요"라 손해지만, **운동 중 휴식은 어차피 기다리는 시간**이라 마찰이 없다. 한 세션 12세트면 광고 슬롯이 12개다.

**Fitbod·플랜핏은 구독 모델이라 이걸 못 한다** — 구독자에게 운동 중 광고를 12번 트는 건 브랜드 자살이다. 기술이 아니라 수익 모델이 충돌해서 못 하는 자리라, 이 프로젝트에서 **판정 기준 ②를 통과한 첫 차별점**이다. 앱인토스 검색으로 휴식 시간에 광고를 트는 앱이 없음도 확인했다.

접을 때 지적했던 구멍(*"기구를 다 사면 수익 0"*)도 광고가 메운다. 쉐어링크는 기구 해금 자리에 보조 수익으로 남는다.

**그 밖에**

- 판정 기준에 **⓪ 선점 확인을 설계보다 먼저** 추가했다. 두 번 다 설계를 끝낸 뒤 경쟁자를 발견한 대가다
- 「백원마켓」(리뷰 3,730)을 뜯어보다 **리워드 광고가 실제로 작동하는 수익 모델**임을 확인했다. 어필리에이트는 구매까지 가야 하지만 광고는 시청만으로 수익이 난다
- **서버 0대**가 됐다 — 운동 데이터는 번들에, 상태는 localStorage, 쉐어링크는 미리 발급해 정적 JSON. EC2도 DB도 필요 없다
- 레포·폴더를 `pricelog` → `restfit`으로 정리했다

---

## 2026-08-25 · 설계 개정 — 웹사이트에서 앱인토스 미니앱 단독으로

**의도**: 실행 환경이 앱인토스 미니앱으로 확정되면서 초판 설계의 전제가 무너졌다.

**결과**

- **SEO 설계를 전부 폐기**했다 — 사이트맵·JSON-LD·URL 슬러그·"상품 하나당 페이지 하나로 롱테일"·Next.js 서버 모드·60일 SEO 대기론까지. 미니앱은 검색엔진에 색인되지 않는다. 트래픽은 토스 앱 내 노출에서 온다
- 미니앱 스택은 선택지가 없다: Vite + React **18** + `@apps-in-toss/web-framework` + `@toss/tds-mobile`. BookTimer 미니앱 실물로 확인했다 (React 19는 TDS peer 제약으로 불가)
- 미니앱이 정적 SPA라 **API 서버가 필수**가 됐다. 이 때문에 "이 프로젝트엔 API 서버 자리가 없다"는 초판 논거가 무효가 됐고, Spring이 후보로 되살아났다 → **Spring Boot + PostgreSQL** 채택. BookTimer로 검증된 Spring + 미니앱 패턴을 재사용한다
- 수집기는 별도 cron이 아니라 `@Scheduled`로. 하루 1회 작업에 별도 배포를 늘릴 이유가 없다
- **미니앱 심사 리젝 우려는 근거가 없었다** — 외부 링크 정책의 허용 예시에 *"각 제품을 소개·추천한 뒤 최저가 구매 플랫폼으로 이동"* 이 명시돼 있다. 우리 서비스 그 자체다

**최우선 과제가 바뀌었다**

미니앱에서 쉐어링크를 열면 앱 복귀가 불안정하다는 제보가 확인됐고(토스 "개선 논의 중"), 제보자는 결국 토스쇼핑 링크를 빼고 쿠팡을 쓰고 있다. **수익 전부가 그 링크 하나에 달려 있어** 다른 설계보다 먼저 잰다. Open API 승인 없이 수동 발급 링크로 검증 가능하다.

---

## 2026-08-25 · 프로젝트 신설 — 토스쇼핑 가격 이력 검증 서비스

**의도**: 토스 쉐어링크를 적극 활용하는 서비스를 찾는 과정에서, 여러 후보를 검토·기각한 끝에 아이템을 확정하고 프로젝트를 세웠다.

**결과**

- 아이템 확정: **토스쇼핑 상품의 가격 이력을 매일 쌓아 할인의 진위를 판정하는 웹사이트.** 수익은 쉐어링크 어필리에이트 10%
- 선정 근거: 커머스 사업자는 자기 셀러의 할인이 가짜라고 표시할 수 없다 → 토스가 구조적으로 못 만드는 기능 = 영구 해자. 그리고 다나와가 커버 못 하는 오픈마켓 롱테일이 우리 자리다
- 검토했다 접은 안들과 **접은 이유·되살릴 조건**을 `docs/ideas.md`에 보존. 특히 홈트 헬퍼는 채택했다가 같은 날 경쟁 조사(Fitbod·플랜핏)로 기각했고, 그 과정에서 얻은 판정 기준 3개를 문서에 남겼다
- 설계 문서 `docs/2026-08-25-design.md` — 아키텍처(EC2 1대 + Next.js 서버 모드 + SQLite + cron), 데이터 모델, 수집기 우선순위, 판정 로직 명세
- 작업 추적 3종 + troubleshooting 분할 시스템 셋업

**주요 판단 두 가지**

- 인프라를 **EC2 + Elastic IP**로 정했다. 쉐어링크 API가 출발지 IP 화이트리스트를 요구하는데 가정용 회선은 유동 IP라 조용히 깨진다
- 초기 설계의 "서버 0대 정적 사이트"를 뒤집었다. 상품 하나당 페이지 하나로 수천 페이지가 나오는 SEO 사이트라 매일 전체 재빌드가 비현실적이다 → 서버 모드 + SQLite
