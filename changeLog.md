# changeLog

완료 기록 (역순). 앞으로 할 일은 [plan.md](plan.md), 함정은 [claude-docs/troubleshooting.md](claude-docs/troubleshooting.md).

코드 세부는 PR·커밋에 있으니 여기엔 **왜 / 무엇을**만 적는다.
⚠️ PR 번호는 적지 않는다 — 미래에 확정되는 값이라 stale이 쌓인다. PR을 찾으려면 제목으로 `git log --grep`.

---

## 2026-09-04 · 검수 제출 (`20260904-33`) — 밀기/당기기 교대를 실어 보냈다

**의도**: 밀기/당기기 교대를 실기기에서 확인하고 production 번들로 검수에 올린다. **지난 노트의 오타 2건도 이번 노트에서 바로잡는다.**

**결과**: 개발 번들 `20260904-32`로 사용자가 실기기 확인 → production `20260904-33` 제출(`REVIEWING`). 라이브는 `20260903-31` 그대로라 사용자 영향은 없다. 올리기 전 번들 실물을 마커 11종으로 대조했다 — 새 문구 5종 있음, **옛 문구 「고른 운동만 매일 그대로 나옵니다」 없음**(새 빌드라는 증거), 기존 기능 3종 있음(계측기가 죽지 않았다는 증거), 개발용 입구 없음(production 확정).

**읽을 것**: 「팔굽혀펴기」·「뜨고」가 제대로 나갔다. 지난 「흥러요」까지 셋 다 정리됐다. 다만 **이번에 바뀐 건 철자가 아니라 순서다** — T-247의 근인은 오타가 아니라 「확정과 동시에 고칠 수 없다」였다.

`bundle_set_release_note`는 `CREATED`에서만 되는 대신 **몇 번이든 다시 쓸 수 있다.** 그래서 노트를 먼저 박고 → 서버본을 되받아 원본과 바이트 대조하고(709자, 동일) → `bundle_submit_review`에 `releaseNotes`를 **안 넘겨서** 제출했다. 그 인자는 필수가 아니라 생략하면 검사를 통과한 값이 그대로 쓰인다 — **두 번 타이핑할 일이 없어지니 오타가 들어갈 자리가 사라진다.** 되돌릴 수 없는 호출을 피할 수 없다고 단정하기 전에 같은 일을 하는 다른 호출을 찾아본 것이 답이었다([T-247](claude-docs/troubleshooting/T-247.md) 갱신).

---

## 2026-09-04 · 밀기/당기기를 하루씩 번갈아 — 「내 운동」에 회복 게이트가 생겼다

**의도**: 「상체는 당기는 운동과 미는 운동으로 나누더라. 풀업과 푸시업도 그렇게 나누면 일정 짤 때 유용하겠다」는 제보. Phase 11이 남긴 구멍을 사용자가 스스로 짚었다 — 둘 다 상체라 상/하체 로테이션이 못 봐 주는 자리다.

**결과**: 밀기와 당기기를 **함께 고르면 하루씩 번갈아** 나간다. 홈이 「오늘은 당기는 날 · … · 다음은 미는 날」로 말한다. 한쪽만 골랐으면 종전대로 매일 전부 나간다 — 나눌 것이 없는데 나누면 이틀에 하루는 운동이 아예 없는 날이 되기 때문이다.

**읽을 것**: **분류를 새로 만들 일이 아니었다.** 데이터에 `force: 'push' | 'pull'`이 처음부터 있었는데(408종 중 398종이 값을 가졌다) 타입 선언에만 있고 어디서도 안 쓰고 있었다. 새 필드를 파기 전에 있는 것을 먼저 본 것이 이 PR의 절반이다.

교대는 **날짜 홀짝이 아니라 기록**으로 굴린다. 화·목만 운동하는 사람은 홀짝이 같아 한쪽만 두 번 하고 다른 쪽은 영영 안 나온다 — 「마지막으로 한 쪽」을 보면 건너뛴 날이 교대를 밀어내지 않는다. 돌연변이 4종(나누기 끄기·동점 뒤집기·기록 무시·정적 운동 떨구기)을 넣어 검사가 전부 잡는 것을 실측했고, 그 과정에서 **치환이 조용히 안 먹어 초록이 나온** 건을 [T-248](claude-docs/troubleshooting/T-248.md)로 남겼다.

---

## 2026-09-03 · 승인·출시 (`20260903-31`) — 「내 운동」이 라이브로 갔다

**의도**: 검수 결과를 확인하고, 사용자가 콘솔 웹에서 출시한 것을 실측으로 확인해 기록한다.

**결과**: **승인 → 출시(17:02:09).** `bundle_get_live_version`의 `deployed: true`·`reviewStatus: APPROVED`와 `miniapp_release_status`의 `liveBundleVersionName: 20260903-31`로 확인했다. **제출(16:38)에서 출시까지 24분**이고 반려는 없었다. 이제 사용자가 자기 앱에서 턱걸이·푸시업을 골라 매일 그대로 할 수 있다 — 이 기능을 만든 이유가 그거였다.

**읽을 것**: 광고 관련 반려(T-245)를 겪은 직후라 이번에도 걸릴 수 있다고 봤는데 한 번에 통과했다. **예고 문구를 이미 실은 번들 위에 기능만 얹었기 때문**이다 — 광고 동작을 안 건드리는 기능 추가는 그 자체로는 심사 위험이 아니라는 뜻이고, 릴리즈 노트에 「휴식 광고는 이전 버전과 동작이 같습니다」를 명시한 것도 같은 방향으로 작동했을 것이다.

⚠️ 릴리즈 노트의 깨진 한글 2건(「팔굴혀퐀기」·「뜼고」)은 **승인·출시된 번들에 그대로 남았다.** `APPROVED`에서도 노트는 못 고친다 — 다음 번들 노트에서 바로잡는다([T-247](claude-docs/troubleshooting/T-247.md)).

---

## 2026-09-03 · 검수 제출 (`20260903-31`) — 「내 운동」을 실어 보냈다

**의도**: 「내 운동」 직접 고르기를 실기기에서 확인하고, production 번들로 검수에 올린다.

**결과**: 개발 번들 `20260903-30`으로 테스트 푸시해 사용자가 실기기 확인 → production `20260903-31`(187,992바이트)을 새로 뽑아 업로드(HTTP 200 · 보낸 바이트 = 로컬 크기) → 테스트 푸시 → 검수 요청 → `REVIEWING`. 올리기 전 번들 실물에서 새 문구 9종과 기존 기능 문구(동작 보기·광고 예고 2종)가 **소스 리터럴 그대로** 있고 개발용 입구는 없는 것을 확인했다. 라이브는 `20260902-29` 그대로다.

**읽을 것**: 함정 둘을 새로 남겼다. ① **`.ait`는 평범한 zip이 아니다** — `AITBUNDL` 헤더 뒤에 zip이 붙은 컨테이너라 그냥 열면 **엔트리 0개가 예외 없이** 나오고, 그 상태로 마커를 대조하면 전부 「없음」이 된다. 대조군 셋(새 문구·기존 문구·개발용 입구)을 같이 내지 않았으면 멀쩡한 번들을 다시 뽑을 뻔했다([T-246](claude-docs/troubleshooting/T-246.md)).

⚠️ ② **릴리즈 노트에 깨진 한글 2건**(「팔굴혀퐀기」·「뜼고」)이 나갔다. 한글을 `\uXXXX` 이스케이프로 손수 쓴 탓이고, 유효한 코드포인트라 아무 데서도 안 걸린다. 검수 요청과 **같은 호출**에서 확정되는 값이라 `REVIEWING`에서는 고칠 수도 없다 — 다음 번들 노트에서 바로잡는다. 긴 한글 인자는 파일로 먼저 쓰고 눈으로 대조한다([T-247](claude-docs/troubleshooting/T-247.md)).

---

## 2026-09-03 · 운동 직접 고르기 — 추천이 안 주는 운동을 하려는 사람을 위한 문

**의도**: 「턱걸이와 푸시업을 꾸준히 해서 몸의 변화를 보고 싶은데 운동을 이 둘로 설정할 수가 없다」(사용자 제보). 추천만 있는 앱은 **추천이 안 주는 운동을 하려는 사람을 통째로 잃는다** — 그게 리텐션이고, 리텐션이 곧 광고 슬롯이다.

**결과**: 「내 운동」 화면을 새로 만들어 근력 408종을 부위 칩 + 검색으로 고르게 했다. 고른 것이 있으면 홈이 **그것만, 고른 순서 그대로, 매일** 보여 준다(`customRoutine`). 비우면 자동으로 추천으로 돌아가서 켬/끔 스위치가 필요 없다. 입구는 오늘의 목록 **바로 아래** — 「이거 말고 딴 걸 하고 싶다」는 생각이 드는 자리에 문이 없었던 게 제보의 원인이다.

**읽을 것**: 두 가지가 이 화면의 절반이다. ① **검색 별칭** — 데이터 이름은 원어 음차(「풀업」)인데 사람은 우리말로 찾는다(「턱걸이」·「푸쉬업」). 0건이면 그 운동은 앱에 없는 것과 같아 제보 상태로 그대로 되돌아간다. ② **막지 않고 순서로 답한다** — 안 가진 기구는 「풀업바 필요」 딱지로 말만 하고 고를 수 있게 두되(막으면 제보가 재발), 가진 것으로 되는 운동을 위로 올린다.

⚠️ 유닛 로테이션이 하던 **회복 게이트가 이 모드에는 없다.** 매일 같은 부위가 나가는 것은 고른 사람의 몫이고, 화면이 「고른 운동만 매일 그대로 나옵니다」로 그 사실을 먼저 말한다.

---

## 2026-09-02 · 승인·출시 (`20260902-29`) — 4차 반려를 하루 안에 닫았다

**의도**: 재제출한 번들의 검수 결과를 확인하고, 사용자가 콘솔 웹에서 출시한 것을 실측으로 확인해 기록한다.

**결과**: 검수 요청 7분 뒤 **APPROVED**(15:16). 사용자가 콘솔 웹 「앱 출시」에서 눌렀고, `miniapp_release_status`의 `liveBundleVersionName`과 `bundle_get_live_version`으로 **라이브 = `20260902-29`** 확인. 반려(13:57) → 대응 머지 → 재제출(15:09) → 승인(15:16) → 출시까지 **같은 날**이다. 이 번들에 「동작 보기」(칩·시트·사진 2장·한글 단계 목록)와 휴식 광고 예고(문구 2곳 + 3초 유예)가 함께 실렸다.

**읽을 것**: 반려 사유가 「누르게 하라」가 아니라 「오는 걸 알게 하라」였다는 판단이 맞았다 — 안내 문구만으로 통과했고 자동 노출은 그대로다. 다만 다음에 같은 사유가 오면 그때는 CTA 쪽으로 읽는다(plan의 ⚠️). 릴리즈 노트 오타(「흥러요」)는 승인된 번들에 남았다 — 다음 번들 노트에서 바로잡는다.

---

## 2026-09-02 · 재제출 (`20260902-29`) — 광고 예고를 실은 번들

**의도**: 4차 반려 대응(광고 예고 문구 2곳 + 3초 유예)이 main에 합쳐졌다. 같은 날 바로 재제출한다 — 광고 지면의 23일 무노출 시계는 반려와 무관하게 돈다.

**결과**: production `20260902-29`(`npm run release`, 186,539바이트) → 업로드 HTTP 200 + 보낸 바이트 = 로컬 크기 → `bundle_test_push`(제출 전 필수) → `bundle_submit_review` → `reviewStatus REVIEWING`. 올리기 전에 번들 실물에서 예고 문구 두 개가 **소스 리터럴 그대로** 있고 개발용 입구 문구는 없는 것을 확인했다(양성·음성 대조군 동반). 릴리즈 노트는 맨 앞에 [휴식 광고 예고] 절을 두어 심사자가 수정 지점을 먼저 보게 했고, 그 뒤에 [동작 보기]를 그대로 실었다.

⚠️ 릴리즈 노트에 오타가 하나 있다(「흘러요」가 「흥러요」로) — 앱 문구는 맞고 노트만 틀렸다. `bundle_set_release_note`는 REVIEWING 상태에서 못 고치므로(취소하면 그 배포는 재검수 불가) 결과가 나온 뒤 처리한다.

**라이브는 `20260829-26` 그대로다.** 승인돼도 자동 출시되지 않는다 — 콘솔 웹 「앱 출시」에서 눌러야 반영된다.

---

## 2026-09-02 · 광고가 오기 전에 말한다 — 4차 반려 대응

**의도**: 4차 검수가 한 줄로 반려됐다 — *「유저가 예상하기 어려운 시점에 광고가 노출돼요. 광고 노출 전에 유저가 인지할 수 있도록 CTA 문구나 UI를 추가해 주세요.」* 그런데 **광고 코드는 Phase 3 이후 한 줄도 안 바뀌었고, 같은 동작으로 검수를 세 번 통과했다.** 회귀가 아니라 **기준이 바뀌었거나 심사자가 다르다**는 뜻이다. 그리고 그 판정 자체는 맞다 — 「세트 완료」를 누르면 휴식 화면이 뜨고 광고 로드가 끝나는 대로(수 초 안, 시점 불규칙) 전면 광고가 덮는데, 그 순간을 예측할 단서가 화면에 하나도 없었다. 실제로 앱 어디에도 사용자에게 보이는 광고 예고가 없었다(소스에서 「광고」는 주석뿐).

**결과**: **광고는 지금처럼 자동으로 뜬다.** 사용자가 눌러야 나오는 CTA로 바꾸면 노출이 통째로 무너져 수익 슬롯이 사라진다 — 반려가 요구한 것은 「누르게 하라」가 아니라 **「오는 걸 알게 하라」**다. 그래서 말을 두 번 건다. 세트 화면의 「세트 완료」 바로 위에 *「세트를 마치면 휴식 중에 광고가 나와요」*, 휴식 화면의 「휴식」 캡션 아래에 *「잠시 후 광고가 나와요 · 휴식 시간은 그대로 흘러요」*. 둘 다 **다음 슬롯에 광고가 실제로 나올 때만** 뜬다 — 판단은 `adPlan` 하나가 하고, 안 나올 광고를 예고하는 것도 거짓말이기 때문이다.

그리고 **예고를 띄운 뒤 3초가 지나야 노출한다**(`AD_NOTICE_MS`). 로드는 즉시 시작하므로 늦춰지는 것은 **노출뿐**이고, 대기는 절대 시각 비교라 백그라운드 스로틀에서도 3초가 줄지 않는다. 3초 뒤에도 **아직 휴식 중일 때만** 띄운다 — 그 사이 건너뛰었으면 노출을 포기한다(세트 도중에 전면 광고가 덮는 것이 바로 반려가 말한 그 시점이다).

**안 바뀐 것**: 자동 노출 · 휴식 타이머(`restEndsAt` 절대 시각이라 광고가 시간을 훔치지 않는다) · `adPlan`/`nextAdState`/40초 컷 · 백오프·포기 규칙 · 광고 지면. **노출 조건·빈도는 그대로다** — 로드를 늦추지 않으므로 슬롯당 성공률도 그대로고, 늦춰지는 것은 노출 시점 3초뿐이다. 다만 **로드가 끝나기 전에 휴식을 건너뛴 슬롯의 노출은 포기한다** — base에서는 그 광고가 **세트 화면 위에** 떴다.

경위와 재발방지는 [T-245](claude-docs/troubleshooting/T-245.md).

---

## 2026-09-02 · 「동작 보기」 검수 제출 (`20260902-28`)

**의도**: 「동작 보기」 1차(칩·시트·사진 2장)와 2차(한글 단계 목록)가 main에 합쳐졌고, 사용자가 실기기(토스 앱)에서 확인을 마쳤다. 한 번들로 묶어 UPDATE 검수를 넣는다.

**결과**: 실기기 확인은 **개발 번들 `20260902-27`**(`release:dev`, 기록 탭 개발자용 줄 포함)로 했고, 제출은 **production 번들 `20260902-28`**(`npm run release`)을 새로 뽑아 올렸다 — 둘이 같은 `dist/`를 쓰므로 개발 번들을 그대로 올리지 않는다는 규칙 그대로다. 올리기 전에 production `dist/`에 개발용 입구 문구가 0건인 것을 확인했고, 업로드는 HTTP 200 + 보낸 바이트 = 로컬 파일 크기(186,270)로 검증했다. `bundle_test_push`(제출 전 필수) → `bundle_submit_review` → `reviewStatus REVIEWING`. 릴리즈 노트는 [동작 보기] 한 절 — 전문은 `bundle_list`의 `releaseNote`.

**라이브는 `20260829-26` 그대로다** — 승인돼도 자동 출시되지 않고, 콘솔 웹 「앱 출시」에서 눌러야 반영된다.

---

## 2026-09-02 · 동작을 글로도 말한다 — 「동작 보기」 시트의 한글 단계 목록

**의도**: 사진 두 장은 「무엇을」까지만 말한다. 팔꿈치를 고정하는지, 어디서 숨을 내쉬는지는 글이 있어야 한다. 원문(free-exercise-db, 영어 4.2단계·41만 자)을 그대로 붙이면 사용자가 못 읽으므로, **짧은 한글 단계**로 옮겨 사진 아래에 얹는다.

**결과**: 시트를 열면 그 운동의 단계가 번호 목록으로 뜬다. 번역은 Claude 배치가 80개씩 8묶음으로 만들고, `scripts/merge-instructions.mjs`가 **원본과 대조해 합친다** — 단계 수가 원문과 같거나(마지막이 「권장 횟수만큼 반복한다」류면) 정확히 1 적어야 하고, 빈 단계·80자 초과·영문 잔재(예외는 SMR·kg·cm)가 하나라도 있으면 아무것도 쓰지 않는다. 커밋된 결과는 `instructions.test.ts`가 같은 형식으로 다시 지킨다.

**번역 규칙**: 단계 수 유지 · 평서형 「~한다」 · 한 단계 60자 목표(80자 상한) · 용어는 화면 어휘(`MUSCLE_KO`·`EQUIPMENT_KO`·운동명)를 따른다. 횟수 지시 단계만 뺀 이유는 **횟수는 앱이 정하기 때문**이다. 결과 635키 · 2483단계(평균 3.9 · 최장 73자).

**설명이 0단계인 운동 5개는 키 자체가 없다** — 화면은 사진만 보여 준다. 로딩 중과 「설명 없음」이 같은 화면이라 스피너도 빈 상자도 없다.

**첫 로드는 그대로다** — 데이터는 동적 import로 떨어진 별도 청크(268KB · gzip 59KB)라, 시트를 여는 사람만 받고 한 번 받으면 캐시된다. 비동기라 결과가 닫힌 뒤·다음 운동으로 넘어간 뒤에도 도착하는데, 그 늦은 답은 버린다 — 안 버리면 다음 운동 시트에 이전 운동 설명이 뜬다.

---

## 2026-09-02 · 모르는 운동에 답하는 자리 — 세트 화면 「동작 보기」 시트

**의도**: 추천 풀이 640개라 **이름만으로는 모르는 운동**이 자주 나온다(「조트만 컬」·「케틀벨 윈드밀」 — 사용자 제보). 지금 세트 화면에는 88px 사진 한 장과 이름·부위뿐이라, 모르는 운동을 만나면 앱 밖에서 찾아봐야 한다. 다양성을 줄이는 것은 답이 아니라서, **설명을 필요한 사람만 열어 보게** 한다. 시안 3×3(설명 방식 × 디자인) 비교에서 **칩 「동작 보기」 + 바텀시트**로 확정됐다.

**결과**: 세트 화면 헤더의 이름·부위 아래에 칩 「▶ 동작 보기」가 생겼고, 누르면 바텀시트에 운동 이름 · 주동근 · 기구 · 난이도와 **시작·끝 자세 사진 두 장**이 나란히 뜬다. **`images[1]`(끝 자세)을 처음 쓴다 — 데이터 변경은 0이다**: 640개 전부 이미 CDN에 두 장이 있는데 지금껏 첫 장만 썼다. 닫는 길은 둘(「닫기」·배경 탭)이고, 운동이 바뀌면 저절로 닫힌다 — 안 닫으면 다음 운동에 이전 운동 설명이 떠 있다.

**세트 화면에만 둔다.** 휴식 화면은 광고 자리이고 완료 화면은 기록 자리다. 홈 루틴 목록에도 안 뒀다 — 요구는 「운동을 시작해 횟수를 정하는 단계」에서 나왔다. 시트가 「세트 완료」를 가리는 것도 의도다: 읽고 닫아야 기록한다.

**시트는 화면을 갈아 끼우지 않는다** — 세트 화면 위에 겹쳐 떠서, 입력하던 무게·횟수가 안 날아간다. `History`의 시트 스타일을 `ui.dim`·`ui.sheet`로 승격해 두 화면이 같은 것을 쓴다(값 변경 없음).

**2차에서 단계 번역을 얹는다** — 원문 설명(영어 4.2단계·41만 자)을 짧은 한글 단계 목록으로 옮겨 사진 아래에 붙인다. 첫 로드가 무거워지지 않게 별도 청크로 떼어낸다. 시트 틀이 이미 서 있어 두 단계가 서로를 기다리지 않는다.

---

## 2026-08-29 · 몸이 화면보다 먼저 안다 — 휴식 마지막 10초 준비 신호

**의도**: 휴식이 끝나면 화면은 자동으로 다음 세트로 넘어가는데, 폰을 내려놓고 쉬는 사람은 그 순간을 놓친다. 화면을 계속 쳐다봐야만 알 수 있는 전환은 「쉬는 시간」이 아니다. 시안 화면 3종 × 소리 3종 비교에서 **V1(배경 전체가 물든다) + S1(미리 알림형)로 확정**(사용자 결정).

**결과**: 남은 10초부터 배경이 기본 회색에서 진홍으로 램프하고, 4초부터는 글자가 흰색으로 뒤집히며, 마지막 3초는 숫자가 초당 한 번 뛴다. 소리는 −10초 두 음 알림(딩–동) 한 번 → 조용 → −3·−2·−1초 틱 → 0초 시작음이다.

**소리가 안 나도 앱은 온전하다.** 웹뷰·무음 스위치·기기 정책 어느 쪽으로도 소리는 막힐 수 있어서, **화면이 같은 정보를 전부 전달한다**(색 램프 · 펄스 · 「다음 세트 준비」 문구). 음원 파일 대신 웹 오디오 합성음이라 번들은 0바이트 늘었고, 오디오가 없는 환경에서는 조용히 넘어간다 — 광고와 같은 규율이다(신호는 기능이지 뼈대가 아니다).

**놓친 신호는 몰아서 내지 않는다.** 백그라운드에 다녀와 12초가 통째로 지났으면 딩동·틱·틱·틱·시작음이 한꺼번에 터지는 대신 **시작음 하나만** 난다. 판정은 순수 함수 하나(`restCue.ts`)가 전부 쥐고 있어서 소리와 화면이 같은 값에서 나온다.

**펄스는 초당 1회다** — 3Hz 이상 점멸은 광과민성 위험이라 주기를 더 줄이지 않고, 움직임을 줄여 달라고 한 사람에게는 펄스를 생략한다(색 램프는 모션이 아니라 유지).

**타이머 로직·광고 판단·탭바는 한 줄도 안 건드렸다** — 신호는 소리와 색만 얹는다. 신호가 광고에 양보하지도 않는다: 전면 광고를 늦게 닫으면 닫는 순간 이미 물든 화면이 보인다.

---

## 2026-08-29 · 이름이 내용을 말하게 한다 — 설정 입구 개편

**의도**: 홈 오른쪽 위 칩의 라벨이 **현재 목적 이름 그대로**(예: 「근육 키우기」)인데, 누르면 보유 기구·운동 목적·운동 경험·불편한 부위 4종이 다 든 한 페이지가 열렸다. 라벨이 내용의 4분의 1만 말한다 — 기구를 바꾸러 「근육 키우기」를 누를 사람은 없다(사용자 진단). 시안 2종(자리마다 입구 / 설정 허브) 비교에서 **A안 자리마다 입구**로 확정됐다.

**결과**: 설정을 한 페이지에 모으는 대신 **입구를 그 설정이 생각나는 자리에** 뒀다. 홈 칩은 「목적 · {현재 목적} ›」 — 접두어가 버튼의 정체를, 현재 값이 상태를, ›가 눌림을 말한다. 기구는 기구 탭 맨 위 요약 카드 「내 보유 기구 N개 + 바꾸기」로 옮겼다: 기구를 바꿔야겠다는 생각이 드는 화면이 기구 탭이고, 아래 추천이 무엇을 기준으로 뽑혔는지도 그 한 줄이 말한다.

**허브는 기각했다** — ⚙ 하나가 두 줄짜리 목록을 여는 구조는 화면 하나 값을 못 하고, 기구가 다시 「설정 뒤에 숨는」 지금 문제의 약화판이 된다.

**페이지는 둘로 쪼갰다**(`Settings.tsx` 삭제 → 「보유 기구」·「운동 목적」). 경험·불편 부위는 목적과 함께 뒀다 — 목적(횟수·휴식)·경험(난이도)·부위(제외) 셋 다 **루틴을 몸에 맞추는 같은 축**이고, 기구(무엇으로 하느냐)만 다른 축이다. 화면 내용·문구는 옮기기만 했지 새로 그린 것이 없다.

**닫으면 보던 탭으로 돌아온다.** 라우팅을 `settings: boolean` → `config: 'equipment' | 'goal' | null`로 바꿔 탭 상태를 안 건드리므로, 기구 탭에서 열면 기구 탭으로 복귀한다 — 고치러 들어간 사람이 매번 탭을 다시 찾지 않는다.

**빈 루틴 화면의 「설정 열기」 1버튼은 2버튼으로 나눴다** — 짚어 준 원인이 둘(기구·불편 부위)인데 갈 곳이 하나면, 도착한 페이지에 나머지 하나가 없어 짚어만 주고 못 고치게 두는 셈이다.

**탭바는 한 줄도 안 건드렸다** — 2회 반려된 표면(T-222·T-224)이라 이 개편의 사정거리 밖에 뒀다.

⚠️ 릴리즈 전 확인: 스토어 스크린샷 「홈」 장에 구 칩(목적 이름 단독)이 보이면 재촬영 대상(T-222) — 캘린더 개편의 「기록」 장 재촬영과 같은 회차로 묶는다.

---

## 2026-08-28 · 기록은 달력이 된다 — 월간 캘린더 + 플로팅 카드

**의도**: 기록 화면이 역순 카드 리스트라, 사람이 실제로 찾는 것 — **며칠 쉬었나 · 지난주엔 언제 했나** — 을 스크롤로 재구성해야 했다. 그건 리스트가 못 답하는 질문이다. 시안 3종(주간 스트립 / 월간 캘린더 / 타임라인) 비교에서 월간 캘린더로 확정됐다(설계 문서 참조).

**결과**: 화면의 주인이 달력이 됐다. 마커 2종(파란 점 = 운동, 초록 점 = 눈바디)에 오늘은 파란 테두리, ‹ › 로 달을 자유 이동하고, 그 밑에 월 요약 한 줄. 내용 조회는 날짜를 눌렀을 때 뜨는 딤 + 바텀시트가 담당한다 — 그날 부위 칩·눈바디 사진·운동별 세트를 통째로, 같은 날 레코드가 여럿이면 전부.

**세는 단위는 「날」이다.** 「12일 운동」이 레코드 수가 되면 같은 날 두 번 한 사람이 이틀 운동한 게 된다 — 달력이 칠하는 근거와 요약이 세는 근거가 같아야 한다.

**내용이 없는 날은 버튼이 아니다.** 눌러도 아무 일 없는 버튼을 만드는 대신 처음부터 못 누르게 뒀다 — 빈 카드가 뜨는 것보다 안 열리는 편이 정직하다.

**「오늘 찍기」를 걷었다 — 촬영 입구는 완료 화면 하나로 일원화한다**(운동한 날만 찍는다, 사용자 결정). 구 설계의 「기록 탭 카드 = 안 뛴 날의 유일한 촬영 입구」 근거는 그래서 폐기했다. 비교로 가는 문은 살아 있어 슬림 로우(최신 썸네일 + 장수 + 「비교」)로 축소 유지하고, 사진이 0장이면 로우 자체가 없다.

**저장소는 한 줄도 안 건드렸다** — `storage.ts`·`photoStore.ts` 무변경. 읽기 전용 개편이라는 보증을 테스트가 아니라 diff가 말한다.

⚠️ 이 개편이 나가면 **스토어 스크린샷의 「기록」 장을 다시 찍는다**(T-222 규율).

---

## 2026-08-28 · 손가락이 미끄러져도 화면은 안 커진다 — 확대 금지

**의도**: 사용자 제보 — 쓰다 보면 **의도치 않게 화면이 확대돼 잘린다.** 미니앱에 확대가 필요한 화면은 하나도 없는데, 들어오는 문은 둘이다: 핀치, 그리고 세트 +/− 를 빠르게 연타할 때 웹뷰가 이를 더블탭 확대로 오인하는 것. 후자가 특히 나쁘다 — 운동 중 가장 많이 누르는 버튼이 확대 트리거다.

**결과**: 두 문을 각각 닫았다. 뷰포트 메타에 `maximum-scale=1.0, user-scalable=no`(핀치), `html, body`에 `touch-action: manipulation`(더블탭). 후자는 뷰포트 메타를 무시하는 웹뷰까지 덮는 두 번째 방어선이기도 하다. 스크롤과 일반 탭은 건드리지 않는다.

**입력칸 포커스 확대는 손대지 않았다** — 글자가 17px이라 iOS의 자동 확대 기준(16px 미만)에 애초에 안 걸린다. 없는 문제를 막는 코드는 넣지 않았다.

**막는 코드가 두 파일에 흩어져 있어 되돌려도 조용해진다** — 그래서 소스를 직독해 두 방어선이 다 서 있는지 확인하는 테스트로 잠갔다. CSS 쪽은 「파일 어딘가」가 아니라 **`html, body` 블록 안**임을 잠근다.

---

## 2026-08-28 · 맨몸에서 모자란 사람에게도 말을 건다 — 미달 안내를 맨몸까지

**의도**: 사용자 제보 — 맨몸 운동에서 **횟수가 너무 적을 때의 문구가 없다.** 기구에는 목표 하단 미달 안내가 있는데 맨몸엔 사다리 안내(25회 이상, 「너무 많이 했다」 쪽)뿐이라, 목표에 못 미친 사람은 화면에서 아무 말도 못 듣는다. 정작 격려가 필요한 쪽이 침묵을 만난다.

**결과**: 미달 판정에서 맨몸 가드를 걷었다. 경계 규칙은 기구와 **똑같다**(목표 하단 미만, 경계는 범위 안) — 자리도 회색도 그대로다. 초과 안내는 그대로 기구 전용으로 남긴다(맨몸엔 얹을 무게가 없다).

**갈리는 것은 둘째 줄뿐이다.** 기구는 「힘들면 무게를 낮춰서 횟수를 채워보세요」, 맨몸은 「힘들면 조금 쉬었다가 나눠서 채워보세요」 — 맨몸엔 낮출 무게가 없어서 **할 수 있는 행동 자체가 다르다.** 한 문구로 묶으면 한쪽엔 못 할 조언이 뜬다.

**사다리(25회 이상)와의 배타는 코드로 만들지 않았다** — 모든 목적의 목표 하단이 12 이하라 겹칠 값이 애초에 없다. 없는 충돌을 막는 분기를 넣는 대신 그 사실을 주석과 테스트로 잠갔다.

---

## 2026-08-28 · 왜 그런지를 먼저 말한다 — 세션 안내 문구 3종 교체

**의도**: 사용자 제보 — 맨몸 사다리 안내 「더 어려운 동작에 도전할 때예요」가 **응원인지 경고인지 모호하다.** 「더 해라」로도 「그만 늘려라」로도 읽혀서, 읽고 나서 뭘 해야 하는지가 남지 않는다. 같은 문제가 기구 안내의 둘째 줄에도 있었다 — 「무게를 올려볼 때예요」·「무게를 조금 낮춰보세요」는 행동은 있는데 **왜 그래야 하는지**가 없다.

**결과**: 세 문구를 **이유 + 행동** 구조로 교체했다. 맨몸은 한 줄이라 이유를 넣을 자리가 없어 **두 줄로 늘렸다** — 「여기서 횟수를 더 늘려도 효과가 잘 안 늘어요 / 다음엔 더 어려운 동작으로 바꿔보세요」. 기구는 둘째 줄만 갈았다 — 초과는 「무게를 올려야 효과가 계속 늘어요」, 미달은 「힘들면 무게를 낮춰서 횟수를 채워보세요」(미달 쪽은 나무람이 아니라 **선택지**로 읽히게 조건절을 앞에 뒀다).

**고친 것은 문구뿐이다.** 색(초과·사다리는 파랑, 미달은 회색)·자리(횟수칸 바로 아래)·트리거 조건(25회 컷, 기구 전용 범위 판정)은 하나도 건드리지 않았다. 옛 문구가 화면 어디에도 남지 않는다는 부재 단언을 새로 세웠다 — 두 줄을 넣고 옛 한 줄을 지우는 걸 잊으면 세 줄이 뜨는데, 존재 단언만으론 그걸 못 잡는다.

---

## 2026-08-28 · 맨몸도 타이핑하는 순간 말해 준다 — 사다리 안내를 입력 기준으로

**의도**: 사용자가 실기기에서 맨몸 워킹 런지에 30을 입력했는데 아무 문구도 안 떴다. 사다리 안내(「더 어려운 동작에 도전할 때예요」)가 **지난번 저장 기록**을 보고 있어서, 오늘 처음 25회를 넘긴 사람은 다음 세션까지 아무 말도 못 듣는다. 직전 작업에서 기구는 입력 즉시 반응하게 됐는데 맨몸만 한 박자 늦은 상태였다.

**결과**: 컷 판정을 입력값 기준으로 바꿔(`repsNum >= 25`, 경계 포함) 기구 안내와 **같은 자리**(횟수칸 아래)에 같은 톤으로 띄운다. 25 미만으로 고치면 바로 사라진다. 색은 파랑(`--blue-dark`) — 초과를 알리는 지적이 아니라 진급 신호라서, 회색이면 나무라는 말로 읽힌다.

**옛 상단 「지난번 기록 기준」 안내는 지웠다 — 제거가 곧 통합이다.** 입력 기본값이 지난 기록에서 오므로(`suggestNext`) 지난 기록이 25 이상이면 첫 렌더의 입력값도 25 이상이라 실시간 안내가 그 경우를 전부 덮는다. 남겨두면 같은 문구가 한 화면에 두 번 뜬다.

**입력칸 색은 안 바꾼다.** 기구 초과의 파란 테두리는 「무게 칸을 봐라」는 신호와 묶인 것인데, 맨몸엔 무게 칸이 없다.

---

## 2026-08-28 · 목표보다 많이 들었으면 그 자리에서 말해 준다 — 반복 범위 이탈 안내

**의도**: 졸업 프리필은 **다음번** 입력값을 고쳐 주는 장치라, 지금 20회를 치고 있는 사람은 그 세트를 마칠 때까지 자기가 목표(6~12회) 밖이라는 사실을 모른다. 무게를 올릴 타이밍이 한 세션씩 늦게 온다.

**결과**: 세트 진행 화면 입력칸 아래에 두 줄 안내. 초과면 파랑(`--blue-dark`)으로 「목표(6~12회)보다 많아요 / 무게를 올려볼 때예요」, 미달이면 회색(`--text-sub`)으로 「…보다 적어요 / 무게를 조금 낮춰보세요」. 구간은 `GOALS[goal].reps`에서 읽는다 — 목적마다 6~12 · 12~20 · 8~15로 달라서 박아 두면 남의 목표가 뜬다.

초과일 때는 횟수 입력칸도 함께 바뀐다 — **테두리뿐 아니라 입력 텍스트 색까지** 파랑이다(시안엔 테두리만 있었다). 안내 문구가 파랑인데 정작 그 문구가 가리키는 숫자만 검게 남으면 둘이 한 신호로 안 읽힌다.

**「안내지 검문이 아니다」가 규율이다** — 범위 밖이어도 「세트 완료」는 그대로 눌린다. 저장을 막으면 오늘 한 운동이 기록에서 사라지는데, 잘못된 것은 기록이 아니라 무게다.

**맨몸엔 안 뜬다.** 얹을 무게가 없는 사람에게 「무게를 올려볼 때예요」는 줄 수 없는 조언이라, 그 자리는 기존 25회 사다리 안내가 맡는다. 두 줄을 **각각 별도 요소**로 쪼갠 것도 의도다 — 한 줄로 흘리면 375px에서 어절 한복판이 꺾인다.

---

## 2026-08-28 · 눈바디 실기기 검증 완료 — 인라인 프리뷰·고스트·영속 확인

아이폰 실기기(토스 iOS 앱 · 번들 `20260828-18`)에서 화면 안 실시간 프리뷰와 고스트 오버레이, 그리고 앱 재시작·번들 교체를 넘어선 사진 영속이 사용자 확인으로 잡혔다. 화면 녹화로 드러났던 두 결함 — 카메라 중단과 인라인 프리뷰 전체화면 강탈 — 은 각각 중단 감지·재취득 복구 로직과 `webView` 인라인 재생 설정으로 해결된 상태다. 남은 항목(중단 복구 재현 · 캡처 화질 · 권한 거부)은 미확인으로 둔다. 눈바디 검수 제출은 진행 중인 개인화 심사가 끝난 뒤로 결정했다.

---

## 2026-08-28 · 카메라는 켜졌는데 우리 화면에 안 보인다 — 인라인 재생 웹뷰 설정

**의도**: 실기기(아이폰 · 토스 iOS 앱)에서 촬영 화면의 카메라를 켜면 **버튼도 고스트도 없는 네이티브 전체화면 플레이어**가 뜨고, 그걸 닫으면 앱의 인라인 `<video>`는 **정지 프레임 한 장**만 보여줬다. 기준 사진을 반투명으로 겹쳐 구도를 맞추라고 만든 고스트 오버레이가 **통째로 무용지물**이었다 — 프리뷰가 안 움직이는데 겹쳐 봐야 맞출 것이 없다.

**결과**: `apps-in-toss.config.ts`의 `webView`에 `allowsInlineMediaPlayback: true`와 `mediaPlaybackRequiresUserAction: false` 두 줄. WKWebView는 앞엣것이 **기본 false**라 `video.play()`를 전체화면으로 승격시키고, 뒤엣것이 없으면 인라인 자리는 생겨도 재생이 사용자 조작을 기다리다 정지 프레임에 머문다 — 두 줄이 한 쌍이다. 화면 코드의 `<video autoPlay playsInline muted>`는 **처음부터 맞았다**: 막은 것은 웹 코드가 아니라 컨테이너 설정이었다.

**「사진은 찍히는데 프리뷰만 죽었다」가 유일한 감별 신호였다.** `getUserMedia`는 열리고 스트림도 살아 있어서 `drawImage` 캡처는 정상으로 성공한다 — **스트림이 있느냐를 재는 어떤 신호도 이 버그를 못 잡는다.** 카메라 프로브의 「켜짐 = 성공」 판정이 이걸 그대로 통과시켰다: 프로브의 성공 기준에는 「열리는가」뿐 아니라 **「어디에 보이는가」**가 들어가야 한다.

**이 축은 테스트가 못 잡는다** — jsdom에는 웹뷰가 없고, 문제는 웹뷰 컨테이너 설정이다. 다만 키 이름과 값 타입은 프레임워크 `config.d.ts`가 리터럴로 타이핑해서 **`tsc`가 진짜 계측기 노릇을 한다**(오타 키를 넣으면 `TS2561`, 문자열 값을 넣으면 `TS2322`로 실제로 막히는 것을 확인했다). `appName`이 `string`이라 검증이 없던 T-218과 갈리는 지점이다 — 「빌드 통과 ≠ 유효성」은 **필드마다 다르다**.

---

## 2026-08-28 · 얼어붙은 프리뷰를 살아 있는 척 두지 않는다 — 카메라 중단 복구

**의도**: 실기기(아이폰 · 토스 iOS 앱)에서 촬영 화면을 연 채 **화면 녹화를 시작하면 프리뷰가 얼어붙고 끝까지 안 살아났다.** 다른 앱이 카메라를 물거나 앱이 잠깐 백그라운드로 밀리면 트랙이 `ended`/`mute`가 되는데, 화면은 스트림을 **최초 1회만** 붙이고 있었다 — 복구 경로가 없어 화면을 닫았다 다시 여는 것이 유일한 해결이었다. PR 리뷰가 「visibilitychange 미처리 — 실기기에서만 판별 가능」으로 미리 지목했던 구멍이 그대로 터졌다.

**결과**: 끊기면 「카메라가 중단됐어요」로 **상태를 바꾼다.** 얼어붙은 마지막 프레임을 그대로 두는 것은 사용자에게 카메라가 살아 있다고 거짓말하는 것이고, 그 화면에서 셔터를 누르면 정지 화면이 오늘의 사진으로 저장된다. 앱에서 돌아오면(`visibilitychange`) 트랙 상태를 직접 보고 죽어 있으면 **기존 취득 경로를 그대로 다시 탄다** — 옛 스트림 정리는 원래 있던 effect cleanup이 그대로 맡는다(「만든 곳이 놓아준다」). 자동 재취득이 실패하면(녹화 앱이 카메라를 쥐고 있는 동안이 그렇다) 「다시 연결」 버튼으로 사람이 고른다.

**`readyState`만 보면 이 버그는 안 잡힌다.** iOS의 화면 녹화 경로는 트랙을 죽이지 않고 **음소거**한다 — `readyState`는 `'live'`인 채로 프레임만 안 온다. 그래서 살아 있음 판정은 `readyState`와 `muted`를 **둘 다** 본다(한쪽만 재는 테스트가 반쪽 구현을 통과시키지 않도록 두 경로를 각각 잰다).

**캡처가 빈손으로 돌아오는 것도 중단 신호다.** 실기기에서 얼어붙은 프리뷰가 정확히 「사진을 찍지 못했어요」로 나타났다 — 인라인 안내 한 줄로 끝내면 사용자는 **죽은 트랙에 대고 영영 셔터만 누른다.** 그 문구를 지우고 재연결 흐름에 태웠다(mute 리스너와 함께 이중 그물).

**자동 재연결은 딱 한 번이다** (리뷰가 잡은 치명 1건). 판정 effect가 `cam`에 의존하는데 재연결이 그 `cam`을 바꾸므로, **다시 취득한 스트림마저 죽어 있으면**(iOS 녹화 중에는 실제로 muted 트랙이 온다) 판정 → 재취득 → 판정으로 영영 돈다 — 실측 3초에 `getUserMedia` **3951회**, 그동안 「다시 연결」 버튼은 **0개**로 수동 폴백이 있으나 마나였다. 두 번째부터는 사람에게 넘긴다(녹화를 끄는 것은 우리가 못 하는 일이다). 살아 있는 스트림을 다시 잡으면 자동이 도로 풀린다 — 한 세션에서 녹화를 두 번 켜는 사람이 있다.

**캡처가 빈손인 경로 셋 중 둘은 카메라가 멀쩡하다.** 0×0 프레임 · 2D 컨텍스트 없음 · `toBlob` 실패 — 이걸 무조건 중단으로 보내면 진입 직후 셔터를 빠르게 누른 사용자의 **멀쩡한 세션을 철거하고 틀린 안내까지** 띄운다. 「사진을 찍지 못했어요」를 되살리고 **죽은 트랙일 때만** 중단으로 넘긴다.

**확인 화면에서는 아무것도 안 한다.** 사진은 이미 손에 있고, 그 위에 중단 안내를 띄우면 방금 찍은 사진이 그대로 증발한다 — 「다시 찍기」로 프리뷰에 돌아가는 순간에 비로소 다시 연다. 카운트다운 중에 끊기면 카운트다운은 그 자리에서 죽는다.

---

## 2026-08-28 · 기준 사진 옆에 오늘을 놓는다 — 눈바디 진입점과 비교 화면

**의도**: 3부작의 ③이자 완결 — 저장소(①)와 촬영 화면(②) 위에 **들어가는 문과 보는 자리**를 붙인다. 지금까지 촬영 화면은 만들어져 있었지만 여는 곳이 없었다(의도된 순서였다 — 임시 버튼은 다음 PR에서 도로 지울 코드다).

**결과**: 문이 둘이다. 운동 완료 화면의 한 줄(「📷 오늘의 눈바디 남기기」)과 기록 탭의 눈바디 카드. 완료 화면은 「매일 운동하고 찍는다」와 정확히 겹치는 순간이지만 **운동한 날만** 뜨고, 기록 탭 카드는 안 뛴 날에도 찍을 수 있는 유일한 입구이자 비교로 가는 유일한 문이다. 홈 카드는 「오늘 운동 시작」 CTA와 경쟁해서(이탈은 곧 광고 슬롯 소멸이다), 넷째 탭은 이미 두 번 반려된 표면이라 둘 다 안 만들었다.

**완료 화면에서는 기록 저장이 먼저 돌고 촬영이 나중에 열린다.** 촬영을 먼저 열면 그 화면에서 사용자가 뒤로 가거나 앱이 죽는 순간 **방금 한 운동이 통째로 사라진다.** 기록은 광고보다 귀하고 사진보다도 귀하다 — 그래서 순서 자체를 테스트가 잠근다(호출 순서 대조).

**비교는 기준 고정이다.** 왼쪽은 남아 있는 가장 오래된 사진, 오른쪽만 날짜를 넘긴다. 양쪽 다 움직이면 「무엇 대비 무엇인가」가 흐려져 그건 더 이상 비교가 아니다. 사진이 한 장뿐이면 좌우에 같은 사진을 거는 대신 「내일 또 찍으면 비교할 수 있어요」라고 말한다 — 같은 사진 두 장은 「변화가 없다」는 거짓말이다.

**삭제한 뒤에는 목록을 다시 읽는다.** 로컬 배열에서 한 장 빼는 것으로 대신하면 삭제가 조용히 실패해도 화면은 지워진 것처럼 보이고, 사용자는 지워졌다고 믿는다. 프라이버시 기능에서 그 거짓말이 제일 비싸다.

**촬영 화면 리뷰 지적 다섯 건도 함께 정리했다**: 저장 중 「다시 찍기」가 눌리던 창을 잠갔고, 프리뷰로 돌아가도 남던 실패 문구를 걷었고, 「사진은 이 기기에만 저장된다」 고지를 **권한 거부·로딩 화면까지** 넓혔다(심사자가 가장 먼저 눌러 보는 화면에 그 문장이 없었다). IDB 연결을 안 닫아 쌓이던 문제는 **「열고·읽고·닫는다」를 훅 하나로 모아** 세 화면이 같은 방식을 쓰게 했다. 테스트 전용으로 뚫려 있던 prop 하나도 모듈 목으로 갈음해 지웠다.

**리뷰가 둘을 더 잡았다.** ① 왼쪽 사진을 거는 조건이 「사진이 여럿인가」였는데, 그러면 선택이 기준 자신일 때(이전 날짜를 끝까지 넘겼을 때) **좌우에 같은 사진이 걸린다** — 코드 주석이 스스로 금지한 상태였다. 조건을 「고른 것이 기준 자신이 아닌가」로 바꿨다. 그 자리를 「이전 날짜 비활성」으로 막지는 않는다 — 기준을 바꾸는 유일한 길이 기준을 골라 지우는 것이라, 못 가게 하면 기준이 영영 고정된다. ② **진입 배선 자체가 무보증이었다**: 찍기 버튼을 비교 화면에 잘못 이어도 화면 테스트 430건이 전부 초록이고 타입 검사도 안 걸린다(둘 다 인자 없는 함수라 타입이 같다). 진입점이 이 작업의 주제인데 그 연결만 아무도 안 보고 있었다 — App 배선 테스트를 새로 뒀다.

**돌연변이 하나가 살아남아 계측기를 고쳤다.** 「기준이 선택을 따라 움직인다」로 부쉈는데 테스트가 통과했다 — 가짜 blob URL이 모든 사진에 같은 문자열이라 **날짜 글씨만 보고 있었다.** 날짜는 그대로인데 사진만 어긋나는 것이 이 화면의 실제 실패 모드다. 사진마다 다른 URL을 주고 각 자리를 재도록 고쳤다.

---

## 2026-08-28 · 거울을 보며 찍었으면 저장본도 거울이어야 한다 — 눈바디 촬영 화면

**의도**: 3부작의 ② — 저장소(①) 위에 **사람이 실제로 쓰는 화면**을 얹는다. 첫 촬영은 격자 + 실루엣으로 기준 구도를 잡고, 이후에는 라이브 프리뷰 위에 **기준 사진(남아 있는 가장 오래된 것)**을 반투명 고스트로 겹쳐 사용자가 스스로 몸을 맞춘다. 자세 인식 AI는 없다 — 고스트가 그 일을 사람 눈에 맡긴다.

**결과**: 촬영 화면 하나(`screens/BodyPhoto.tsx`)와 캡처 로직(`logic/capture.ts`). 임시 프로브였던 `cameraProbe.ts`는 로직 그대로 `camera.ts`가 됐고(판정이 끝났으니 이름이 사실과 맞아야 한다), 설정 화면의 프로브 버튼은 컴포넌트째 지웠다 — **본 기능이 같은 것을 더 잘 잰다.**

**미러 일관성이 이 PR의 심장이다.** 전면 프리뷰는 거울처럼 좌우를 뒤집어 보여주는 것이 자연스러운데, **저장본이 정방향이면 다음 촬영에서 고스트와 내 몸이 좌우로 어긋나 영영 안 맞는다.** 게다가 그 사실은 사진이 여러 장 쌓인 뒤에야 드러나서, 알아챘을 때는 이미 기준 사진이 오염돼 있다. 그래서 변환을 캡처 함수 한 곳에서 내보내고, **프리뷰 CSS와 캔버스 변환이 같은 방향인지를 테스트가 직접 대조**한다 — 한쪽만 고치는 순간 빨간불이 뜬다.

**「저장하지 못했어요」와 「공간이 부족해요」는 서로에게 거짓말이다.** 그래서 `savePhoto`의 반환을 boolean에서 3상(`ok`/`quota`/`fail`)으로 바꿨다(저장소 리뷰에서 나온 결정). 아무 실패에나 「오래된 사진을 지워 주세요」를 띄우면 공간이 멀쩡한 사람에게 사진을 지우라고 시키는 것이고, 반대로 뭉개면 정말 꽉 찬 사람에게 **영영 되는 일 없는 재시도**를 권하게 된다. 원인은 `tx.error`의 이름이 정한다 — 버려지던 값이다.

**심사자는 권한을 거부부터 눌러 본다.** 카메라 실패 세 갈래(미지원 · 권한 거부 · 무응답)가 각각 다른 안내로 갈리고, 어느 쪽이든 닫기로 멀쩡히 돌아온다. 에러 원문은 작은 글씨로 병기한다 — 문의가 왔을 때 무엇이 막혔는지 알 유일한 단서다. 「사진은 이 기기에만 저장되며 어디로도 전송되지 않습니다」는 화면에 상시로 적었다. 사실이고(서버는 0대다), 검수와 사용자 양쪽에 같은 문장으로 답한다.

**전체 실행에서만 빨간불이 뜬 경합을 하나 잡았다.** 저장소가 열리기 전에 저장을 누르면 **아무 일도 안 일어난다** — 조용한 무반응은 실패 안내보다 나쁘다. 파일 단독 실행은 계속 초록이었고 전체 실행에서만 드러났다. 준비가 끝날 때까지 셔터를 안 여는 쪽으로 고쳤다.

**함정 하나를 새로 적었다(T-232)**: jsdom + fake-indexeddb에서는 Blob이 왕복하며 평범한 객체가 되어 저장소의 어휘 검증에 통째로 걸린다 — 저장은 `ok`인데 목록은 **항상 빈다.** node 환경의 저장소 테스트는 초록이라 저장소가 무죄로 보이는 것이 고약하다. 화면 테스트는 목록을 목으로 두고, 진짜 왕복은 node 환경이 잰다.

**진입점은 일부러 없다.** 이 화면을 여는 버튼과 비교 화면은 ③의 몫이라, 지금은 화면과 배선만 서 있다 — 임시 버튼을 달면 그건 다음 PR에서 도로 지울 코드다(방금 프로브 UI를 그렇게 지웠다). 테스트 360 → 401, **돌연변이 30종 전수 사살**(주장이 아니라 전수 실행 로그로).

## 2026-08-28 · 날짜를 키로 삼으면 「하루 1장」에 코드가 안 든다 — 눈바디 사진 저장소

**의도**: 실기기에서 카메라 프리뷰가 된다는 판정이 나와 눈바디 사진을 고스트 오버레이 UX로 확정하고 설계를 마쳤다([docs/2026-08-28-body-photo-design.md](docs/2026-08-28-body-photo-design.md)). 그 3부작의 ① — **화면이 없는 저장소 계층부터** 간다. 사진(Blob, 장당 ~150KB)은 localStorage(문자열, 통상 5MB)에 못 담아 IndexedDB가 필요하고, 저장이 성립하는지가 나머지 두 PR의 전제다.

**결과**: `photoStore.ts` 하나. `storage.ts`의 세 관례를 그대로 옮겼다 — 방어적 읽기(DB가 안 열리면 빈 목록, 화면은 돈다) · 주입 가능(`IDBFactory`를 인자로 받아 테스트가 가짜를 넣는다) · 어휘 검증(형태가 어긋난 레코드는 화면까지 안 간다).

**날짜(`YYYY-MM-DD`)가 키다.** 같은 날 다시 찍으면 `put`이 덮어써서 「하루 1장」에 분기가 0줄 든다 — 규칙을 코드로 쓰는 대신 **스키마가 규칙이 되게** 했다. 키가 사전순이라 정렬 코드도 없다(`getAll`이 스펙상 키 오름차순이다). 보관 상한 365장은 넘길 때 가장 오래된 것부터 지우는데, **저장과 같은 트랜잭션 안**에서 한다 — 나누면 저장이 실패했는데 삭제만 남는 순서가 생긴다.

**`storage.ts`와 딱 하나 다르다: 저장 실패를 삼키지 않는다.** 조용한 write는 계산된 값을 다시 만들 수 있는 기록에는 맞지만 사진에는 안 맞는다 — 방금 찍은 그 장면은 두 번 없고, 소리 없이 증발하면 사용자는 **한 달 뒤 비교 화면에서야** 안다. `savePhoto`가 boolean을 돌려주고 확인 화면이 "저장하지 못했어요"를 띄운다.

**어휘 검증은 읽기·쓰기 양쪽에 있다.** 읽기만 막아도 깨진 레코드는 화면에 안 뜨니 충분해 보이는데, **프루닝은 원시 키를 센다** — 목록에 안 보이는 유령이 상한 한 자리를 먹고, 정원이 찬 날 **기준 사진(가장 오래된 유효 사진)이 대신 지워진다.** 쓰기 경계에서 한 줄로 막으면 프루닝은 손댈 것이 없다.

**테스트는 `fake-indexeddb`로 실제 IDB 경로를 태운다**(devDependency 1개 추가). 트랜잭션·이벤트 루프를 몇 줄로 흉내 내면 그 흉내를 테스트하는 꼴이 된다. 16건 · **돌연변이 17종 중 16 사살, 생존 1건.** 생존자(「IDB 없음 가드 제거」)는 규명했다 — 아래 try/catch가 같은 결과를 내는 **이중 그물**이라 그렇고, 둘 다 걷으면 깨진다. 그래도 가드를 남긴 이유는 **「IDB가 없는 환경」이 예외가 아니라 설계된 경로**라서다. 걷으면 `undefined.open()`의 TypeError에 기대는 꼴이 되어 의도가 코드에서 사라진다.

**「전부 사살」이라고 먼저 적었다가 리뷰에서 틀린 것이 드러났다.** 실측은 14종 중 13 사살이었는데 헤드라인이 로그와 어긋났고, 더 나쁘게는 실패 목이 `tx.onabort`만 발화시켜 **`tx.onerror` 경로가 아예 안 덮여 있었다** — 실전에서 반환값을 정하는 건 먼저 오는 `onerror` 쪽인데도(발화 순서 `error → abort`, 재실측). 목을 「어떤 이벤트가 발화하는가」로 파라미터화해 `onerror`만 / `onabort`만 / 둘 다 셋으로 갈랐다. **계측기를 자칭하는 것과 계측이 되는 것은 다르다** — 사살 주장은 전수 실행 로그로만 한다.

## 2026-08-28 · 카메라가 켜지는지부터 재고 본다 — 눈바디 사진 프로브

**의도**: 눈바디 사진(매일 같은 구도로 몸 사진)의 이상적 UX는 **라이브 프리뷰 위에 지난 사진을 반투명으로 겹치는 것**인데, 그게 되는지가 **토스 웹뷰가 `getUserMedia`를 허용하는가**에 통째로 달려 있고 문서로는 확답이 없다. 화면을 다 만든 뒤에 알게 되면 통째로 버리게 된다 — Phase 0.5에서 광고를 먼저 8회 찔러 본 것과 같은 순서로, 최소 프로브를 먼저 넣는다.

**결과**: `probeCamera` 하나 + 설정 화면 맨 아래 임시 버튼. 성공하면 라이브 프리뷰가 뜨고, 실패하면 **에러 원문이 그대로 화면에 남는다.** 네이티브 권한(`camera`)도 함께 선언했다 — 안 하면 막혔을 때 「권한 미선언」과 「웹뷰 미지원」이 구별되지 않아 실측 자체가 성립하지 않는다.

**감별해야 할 실패가 셋이라 결과도 셋이다.** `unsupported`(mediaDevices 자체가 없다) · 에러 원문(`NotAllowedError` 권한 거절 / `NotFoundError` 카메라 없음) · `timeout`(프롬프트를 삼켜 영영 pending). 셋의 다음 수가 각각 달라서 「실패했습니다」로 뭉개면 프로브를 돌린 의미가 없다. 타임아웃이 없으면 마지막 실패 모드에서 화면이 「확인 중」으로 굳는다.

**타임아웃 뒤에 늦게 도착한 스트림은 트랙을 끈다.** 판정은 이미 나갔으니 붙일 화면이 없는데, 그냥 두면 **카메라가 켜진 채로 남아** 다시 눌러도 점유가 안 풀린다. 정리 지점을 화면과 프로브가 같은 함수(`stopStream`)로 공유해 재시도·언마운트도 같은 경로를 탄다.

**임시 코드라고 적는 대신 구조로 임시로 만들었다** — `History.tsx`의 개발용 입구와 같은 `DEV_TOOLS` 상수 뒤에 뒀다. 릴리스 번들에는 **코드째 없고**(production 0건 / development 1건 / 같은 파일의 사용자 문구 1건이 대조군), 실기기 판정용은 `npm run release:dev`로 뽑는다. 주석의 「나중에 지운다」는 약속은 지켜진 적이 드물지만, 트리셰이킹은 매번 지킨다.

## 2026-08-28 · 어제 낸 검수를 물리고 최신 번들로 다시 냈다 (`20260828-14`)

**의도**: 검수 중이던 `20260827-10`(기구 선택 그리드 개편)이 하루 넘게 IN_PROGRESS였다. 그 사이 개인화 4종(프로필 온보딩·2분할·세션 피드백·졸업 프리필)이 담긴 `20260828-14`가 테스트까지 끝나 있어서, 승인을 기다렸다 또 한 번 검수를 도는 것보다 물리고 한 번에 내는 쪽이 빠르다.

**결과**: `20260827-10` 검수 취소 → `20260828-14`로 재제출(REVIEWING 전환 확인). 취소한 배포는 **영구히 재검수 불가**지만(콘솔 규정 — CANCELED에서는 재요청이 안 되고 CREATED/REJECTED만 가능) 그 변경분이 최신 번들에 전부 포함돼 있어 잃는 것이 없다.

**출시 노트는 취소분까지 묶어서 썼다.** 검수 단위가 아니라 **라이브(`20260826-9`) 이후의 사용자 체감 변경 전부**가 노트의 범위다 — 심사자와 사용자가 보는 것은 「이 번들과 라이브의 차이」이지 「이 번들과 직전 제출의 차이」가 아니다. 개인화 4종 + 기구 선택 화면 개편 + 기구 추천 확충(케틀벨·밴드·풀업바, 무게 필터)을 담았다.

승인돼도 자동 출시는 안 된다 — 콘솔 웹 「앱 출시」의 「출시하기」를 눌러야 라이브다.

## 2026-08-28 · 전 세트를 다 채우면 다음번엔 2.5kg가 얹혀 있다 — 졸업 프리필

**의도**: 입력칸에 지난번 값을 그대로 띄우고 있었다. 편해 보이지만 **정체가 기본값**이 된다 — 목표 상단(예: 8~15회의 15회)을 세 세트 다 채운 다음 날에도 같은 무게·같은 횟수가 떠 있어서, 점진적 과부하가 전적으로 사용자의 의지에 맡겨진다. 앱이 기록을 다 갖고 있으면서 아무 말도 안 하는 셈이다.

**결과**: 지난번 **모든** 세트가 목표 상단에 닿았으면 다음 값을 한 단 올려서 띄운다(double progression). 기구는 **+2.5kg에 횟수를 대역 하단으로 되돌리고**, 맨몸은 **횟수를 하나** 올린다. 아니면 지난 마지막 세트 그대로다. 문헌(Plotkin 2022)과 실무(StrongLifts +2.5kg)가 만나는 지점이라 새로 발명한 규칙이 아니다.

**「모든 세트」와 「횟수 되돌리기」가 규칙의 전부다.** 「한 세트라도」로 느슨하게 하면 첫 세트만 잘 나온 날에 승급해서 다음 주 내내 못 드는 무게가 기본값으로 떠 있는다. 횟수를 안 되돌리면 무게만 단조 증가해 곧 벽에 박는다 — 「무게↑ → 횟수를 다시 쌓아 올림」의 왕복이 이 방식이 double인 이유다. 상단 값은 목적표에서 읽는다(근육 12 · 체지방 20) — 상수로 박으면 같은 12회 기록이 두 목적에서 같은 답을 낸다.

**맨몸은 얹을 무게가 없어서 다르게 처리한다.** 상단 초과를 허용하고 횟수만 올리되, 25회에 닿으면 「더 어려운 동작에 도전할 때예요」를 한 줄 띄운다 — 반복 25~30회를 넘기면 근비대 효율이 급락해서, 그 지점부터는 동작을 바꾸는 것이 유일한 진행 수단이다. 기구엔 안 띄운다(무게를 얹으면 된다). 종목 자동 스왑은 안 만들었다 — 경험 승급이 티어 선호를 통해 같은 효과를 이미 낸다.

**루틴 선발과 세션 상태 기계는 한 줄도 안 건드렸다.** 프리필은 입력칸 기본값일 뿐이라 「같은 날 같은 입력이면 같은 루틴」이 그대로여야 한다. 그 보증은 루틴·세션 스냅샷 대조로 지킨다 — 다만 **그 대조 자체는 항진명제다.** 두 기록의 부위가 같아 선발이 받는 인자가 애초에 동일하니, 같은 인자로 같은 함수를 두 번 부르는 셈이다. 그게 정확히 요점이라 남겨 뒀다: 선발 인자에 세트 기록이 **아예 없다**는 구조적 사실의 트립와이어이고, 누군가 그 인자를 히스토리로 늘리는 날 깨진다.

**그 앞에 세운 계측기는 처음에 신호가 0이었다.** 「두 기록의 프리필이 다르다」로 썼는데, 두 기록은 마지막 세트부터가 12회 vs 11회라 **졸업 규칙을 통째로 무력화해도 여전히 달랐다** — 리뷰가 그 돌연변이를 심어 세 테스트가 다 통과하는 것을 실측했다. 「다르다」가 아니라 **값을 못 박아야**(22.5kg × 6회 / 20kg × 11회) 졸업이 일어난다는 사실이 관측된다. 계측기라고 이름을 붙였다고 계측이 되는 것이 아니다.

## 2026-08-28 · 쉬웠다고 세 번 답하면 난이도가 올라간다 — 세션 피드백 루프

**의도**: 온보딩의 자가평가는 **첫날의 오류를 영구 보존한다.** 체력 테스트를 안 만들기로 한 대가를 지불할 장치가 필요했다 — 매 세션 1비트씩 받아 스스로 교정하는 쪽을 택했다(설계 §3.5). 상급은 온보딩 질문(예/아니요)으로는 도달할 수 없어서, 여기가 생기기 전까지 **닿을 수 없는 값**이었다.

**결과**: 완료 화면에 「오늘 운동 어땠나요?」 3버튼(쉬움·적당·힘듦). **선택 사항이다** — 필수로 만들면 응답률이 아니라 기록 저장률이 깎인다. 3연속 쉬움이면 한 단계 올리고 2연속 힘듦이면 한 단계 내린 뒤, 결과를 그 자리에서 프로필에 반영하고 한 줄로 알린다. 「올려 드릴까요?」로 묻지 않는 이유는 탭이 하나 늘면 다음 세션부터 아무도 안 누르기 때문이다.

**승급(3)과 강등(2)이 비대칭인 것은 의도다.** 대칭이 예뻐 보여도 두 오류의 값이 다르다 — 너무 쉬운 루틴은 지루할 뿐이지만 너무 어려운 루틴은 이탈과 부상으로 간다. 같은 이유로 **답을 건너뛴 세션과 「적당」이 스트릭을 끊는다**: 무응답을 쉬움으로 세면 아무 말도 안 한 사람이 올라간다. 그래서 `recentFeels`는 `recentUnits`와 달리 빈자리를 **거르지도 압축하지도 않는다** — 그 구멍이 신호 자체다.

쿨다운은 두지 않았다. 계속 쉽다는 사람은 세션마다 한 단계씩 올라 초급→상급 최속 4세션이다 — 계속 쉬우면 빨리 올리는 게 맞고, 상태 하나를 아낀다.

**프로필이 없는 사람에게는 피드백만 저장하고 판정은 안 돈다.** 여기서 프로필을 대신 만들면 「경험을 안 고른 사람」이 조용히 사라져 홈의 안내 칩까지 함께 증발한다. 저장소 쪽에서는 어휘 밖 feel이 들어와도 **레코드는 살리고 feel만 버린다** — 옛 버전이 남긴 값 하나 때문에 그날의 세트를 통째로 잃을 이유가 없다. 값을 버린 자리는 「건너뛴 세션」과 같아져 판정에서만 조용히 빠진다.

## 2026-08-27 · 무릎이 아프면 스쿼트를 안 준다 — 프로필을 루틴에 연결

**의도**: 프로필(운동 경험·불편 부위)을 받아서 저장만 해 뒀다. 물어봐 놓고 안 쓰는 값은 마찰만 남긴다 — 이제 `pickRoutine`이 실제로 먹게 한다.

**결과**: 불편 부위는 풀 구성 단계에서 **하드로 뺀다.** 기구 하한·부위 하한과 결정적으로 다른 지점이다 — 저건 「금지가 아니라 선호」라 전부 걸러지면 있는 것 중에서라도 주지만, 통증 부위는 **안전 문제**라 「그래도 하나쯤은」이 성립하지 않는다. 전부 걸러지면 빈 루틴을 주고, 그 화면이 「불편 부위 설정」을 짚어 준다.

매핑은 **부위마다 다르고 셋 다 실측으로 정했다**(근력 408종 기준). 무릎은 주동근만으로 50종 — 스쿼트·런지류가 전부 대퇴사두 primary라 충분하다. 어깨도 주동근만 83종인데, **보조근까지 걸면 가슴 47종 중 43종이 전멸한다** — 벤치류의 어깨 관여가 secondary에 기재돼 있어서다. 그건 필터가 아니라 상체 몰살이다. 반대로 허리는 주동근만 보면 **3종뿐**이라 필터가 사실상 아무 일도 안 해서, 여기만 보조근까지 본다(43종). 세 줄이 다르게 생긴 것은 실수가 아니라 원본 데이터가 부위마다 다른 곳에 근육을 적어 뒀기 때문이다.

훈련 수준은 **제외가 아니라 선발 순서**다. 근육 큐를 티어 순으로 정렬하면 풀이 넉넉할 때는 필터처럼 동작하고(상급 26/408은 초보에게 사실상 안 나온다) 마르면 자연히 다음 티어로 넘어간다 — **폴백이 규칙에 내장된다.** 하드 필터였다면 맨몸 어깨(쓸 수 있는 것이 상급 1종뿐)처럼 풀이 통째로 비는 자리마다 폴백 규칙을 따로 지어야 했다.

**프로필이 없으면 둘 다 건너뛴다** — 안 채운 기존 사용자의 오늘 루틴은 배열 단위로 안 바뀐다. 이 보증의 검증법이 하나 특이하다: 「프로필 없이 호출한 것과 `null`을 넘긴 것이 같은가」는 **자기 자신과의 비교**라, 개인화가 프로필과 무관하게 늘 켜져 있어도 그대로 통과한다. 그래서 2분할 시점의 실제 출력을 고정값으로 박아 대조한다 — 실제로 null 가드를 뺀 돌연변이가 이 테스트에서만 죽었다.

화면 둘: 프로필을 안 채운 사람 중 **기록이 있는 사람**에게만 홈에 한 줄 안내 칩이 뜬다(신규 사용자는 온보딩에서 이미 답했다 — 방금 답한 걸 또 조르면 잔소리다). 프로필이 채워지면 조건이 무너져 사라지므로 「닫음」을 저장할 자리가 없다. 그리고 설정의 「바꾸면 오늘의 루틴에 바로 반영됩니다」를 이제 쓴다 — 받아서 저장만 하던 시점에는 그 문장이 거짓이라 일부러 뺐던 것이다.

## 2026-08-27 · 오늘은 가슴이 아니라 상체다 — 로테이션을 2분할로

**의도**: 6부위 로테이션은 부위당 주간 빈도가 **0.33~0.5회**였다. 주 2회 쓰는 사람은 같은 부위를 **3주에 한 번** 만난다 — 근단백 합성 창(24~72h) 대비 무자극 기간이 11일을 넘고, 빈도 문헌이 비교한 대역(주 1~3회)의 **바깥**이다. 분할을 상체/하체 2유닛으로 바꿔 빈도를 연구 범위 안으로 되돌린다.

**결과**: 로테이션 단위가 부위에서 **유닛(상체=가슴·등·어깨·팔 / 하체=하체·코어)**으로 올라갔다. 헤드라인도 "오늘은 가슴"에서 **"오늘은 상체"**가 된다. 유닛 안에서는 **부위→근육 2단 라운드로빈**으로 뽑는다 — 근육을 평평하게 섞으면 상체 근육 버킷이 9개라 "사실상 등 데이"가 나서, 부위당 빈도가 보장이 아니라 확률이 된다. 바깥 라운드 0에서 부위마다 하나씩 나가게 두면 상체 4종목이 4부위를 **전부** 커버한다.

다만 첫 라운드가 부위마다 주는 것은 「기회」가 아니라 **확정 슬롯**이라, 종목이 하나뿐인 부위가 큐에 끼면 그 한 종목이 이틀에 한 번 반드시 나간다. 맨몸 사용자의 어깨가 정확히 그랬다 — 쓸 수 있는 맨몸 어깨 운동이 **상급 「핸드스탠드 푸시업」 1종뿐**이라 초보의 상체 세션마다 이 동작이 고정 배급됐다(리뷰 실측 60/60). 그래서 **종목 2개 미만인 부위는 바깥 큐에서 뺀다.** 구 6분할의 부위 하한이 막던 자리가 로테이션 단위가 유닛으로 올라가며 비어 있었다.

종목 수는 셋 다 4로 통일했다(근육 키우기·건강 유지 3→4). **근거는 광고가 아니라 커버리지다** — 3종목이면 매 상체 세션에서 한 부위가 결번이라 그 부위의 주간 세트가 최소선(주 4세트)을 밑돈다. 휴식 슬롯이 8→11로 는 것은 그 결정의 부수 효과다. 대가로 근육 키우기 세션이 ~25분에서 ~32분으로 늘었다(문헌 표준 30~60분 안).

**회복 게이트는 여전히 안 만들었다.** 유닛이 둘뿐이라 「가장 오래된 유닛 우선」이 격일 교대를 구조적으로 강제한다 — 어제 유닛은 재선정될 수 없어서 24h 하드 컷을 항상 웃돈다. 전신 분할을 기각한 결정적 이유가 이것이다(전신은 연속일 사용 시 24h 안에 겹쳐 게이트 코드가 필요해진다). 이 보장은 테스트로 못 박고 `routine.ts` 주석에 「덧붙이지 마라」를 남겼다.

기록의 `group` 어휘는 새 필드를 파지 않고 **넓혔다** — 구 부위와 신 유닛이 둘 다 통과한다. 한쪽만 받으면 배포 당일에 지난 기록이 통째로 사라진다. 로테이션이 읽을 때 `unitOf`가 한 줄로 흡수해서, **어제 「가슴」을 한 사람에게 오늘 하체가 나간다**(연속성 유지). 기록 화면은 저장된 이름을 그대로 보여 준다 — 구 기록 「가슴」과 새 기록 「상체」가 섞여 보이는 것은 사실 그대로라 허용한다. 대신 `legs` 라벨을 「하체」에서 **「다리」**로 바꿨다. 유닛 `lower`가 「하체」를 가져갔는데 둘 다 「하체」로 두면 기록에서 구·신이 구별되지 않는다 — 구 기록의 그것은 다리만 한 날이고 새 기록의 「하체」는 다리+코어라, 같은 말로 적으면 거짓이 된다.

## 2026-08-27 · 몸 상태를 묻기 시작했다 — 아직 쓰지는 않는다

**의도**: 루틴이 기구·목적만 보고 짜여서, 무릎이 아픈 사람에게도 스쿼트가 나가고 처음 하는 사람에게 상급 동작이 나간다. 개인화의 첫 조각인 **운동 경험과 불편한 부위**를 받는다.

**결과**: 온보딩을 2단계에서 3단계로 늘려(기구 → 몸 상태 → 목적) 경험 질문 하나(예/아니요)와 불편 부위 칩(「없음」 기본)을 받고, 설정에서도 바꿀 수 있게 했다. **받아서 저장만 하고 루틴에는 안 먹인다** — 이 번들만 배포하면 기존 사용자의 오늘 루틴이 한 톨도 안 바뀐다. 알고리즘 소비는 이후 PR이다(로드맵은 [plan.md](plan.md) Phase 6).

저장 규칙 하나가 특이하다: `experience`가 어휘 밖이면 **프로필 전체를 버린다.** 기구 상세처럼 필드별로 걸러 반쪽을 남기면 「부위는 먹는데 난이도는 안 먹는」 상태가 조용히 생겨, 나중에 루틴이 이상하다는 제보를 받았을 때 원인을 짚을 수가 없다. 불편 부위 안내 두 줄(의료 면책 포함)은 온보딩과 설정이 같은 컴포넌트를 써서 갈라질 수 없게 묶었다 — 한쪽에만 붙으면 다른 쪽이 치료 효능을 주장하는 화면이 된다.

## 2026-08-27 · 쉐어링크에서 돌아오는 길을 한 줄로 알려 준다

**의도**: 실기기 테스트에서 **상품을 열면 뒤로 가기로 앱에 못 돌아오는** 것이 드러났다. 토스쇼핑이 자기 스택으로 떠서 뒤로 가기가 그 안에서만 움직인다.

**결과**: **고치지 않고 안내로 덮었다.** 상품 목록 **위**에 한 줄 — 누른 뒤에 읽히면 늦어서 아래가 아니라 위다.

> 상품을 열면 뒤로 가기로는 못 돌아와요. 토스 홈에서 다시 열어주세요.

**고칠 방법이 우리에게 없다**([T-228](claude-docs/troubleshooting/T-228.md)). `Device.openURL`은 URL 문자열 하나만 받아 복귀 경로를 줄 인자가 없고, 외부 주소를 미니앱 안에서 여는 SDK도 없다(웹뷰 API는 `openPDFViewer` 하나, PDF 전용). 토스 공식 답변도 *"토스 내 다른 서비스로 이동하는 별도의 SDK / API는 제공해드리고 있지 않습니다"* 다. **토스가 내부 개선 중**이므로 고쳐지면 이 안내를 지운다.

⚠️ **이 문제가 한 번 걸러진 적이 있다.** plan.md에 「복귀 검증 통과 — 우리 앱에는 해당하지 않는다」고 적혀 있었는데, **그 검증은 다른 것을 봤다** — 8/26에 본 것은 *돌아왔을 때 앱 상태가 유지되는가*였고, 이번 것은 *뒤로 가기가 어디로 가는가*다. 둘 다 「복귀」 한 단어로 적혀 있어 같은 것으로 읽혔다. 그래서 앞으로는 **무엇을 눌러 무엇이 일어났는지**로 적는다.

안내는 **경고가 아니라 길 안내**로 보이게 했다 — 빨간색·아이콘을 붙이면 「위험한 링크」로 읽혀 누르기를 망설이게 된다. 배경은 `--bg`(흰색)인데, 이 안내가 앉는 카드가 이미 `--bg-sub`라 같은 색이면 경계가 사라지기 때문이다. 대비 **4.62:1**(12px AA 4.5:1 통과), 모바일 375px에서 **2줄 52px** — 첫 문안(55자)은 3줄 70px이라 줄였다.

---

## 2026-08-27 · 쉐어링크 98건 테스트 번들 배포 (`20260827-13`)

**의도**: 5종이 다 찬 쉐어링크를 실기기에서 눌러 보려고 테스트 푸시를 보냈다. **심사 제출은 안 했다** — 라이브는 `20260826-9` 그대로고 `20260827-10`이 검수 중이라 사용자 영향이 없다.

**올리기 전에 번들에 들어갔는지 확인했다**([T-224](claude-docs/troubleshooting/T-224.md)). 마커는 소스에 따옴표째 실린 리터럴만 골랐다:

- `toss.im/_m/` **98건** — 코드의 개수와 일치
- 한글 마커 7종(`세라밴드 2M 오리지널 옐로우/레드/그린` · `2중 잠금 · 문틀 폭 80~102cm` · `루프 6종 세트` · `큰 루프 · 폭 38mm · 풀업 보조` · `아디다스 문틀철봉 도어짐`) 전부 존재
- **대조군** — 개발용 입구 `온보딩 다시 보기`는 소스에 1건, 번들에 **0건**(트리셰이킹). 같은 검색이 위 한글 7종은 찾으므로 계측기가 죽어서 0이 나온 게 아니다
- 업로드 검증은 HTTP 200 + **보낸 바이트 = 로컬 파일 크기**(116,489)

⚠️ **`npm run release`(production)로 뽑았다** — `release:dev`와 같은 `dist/`를 쓰므로 개발 번들을 만든 뒤 그대로 올리면 개발용 입구가 딸려 나간다.

**설치 방식은 안 채우기로 했다** (사용자 결정). 풀업바 `note` 7건이 비어 있는데, **설치 방식은 상품에 딸려 오는 설명서가 말한다** — 사진을 봐서까지 채우면 상품이 이미 하는 말을 한 번 더 하는 것이고, 틀리면 우리 쪽에만 책임이 남는다. 벤치와 다른 점은 거기서는 사진이 **담을지 말지**를 갈랐다는 것이다.

---

## 2026-08-27 · 풀업바 10건 — 쉐어링크 5종이 다 찼다

**의도**: 마지막으로 남은 기구였다. `문틀철봉`·`도어짐`으로 뽑아 온 11건을 반영했다.

**결과**: **10건.** 링크 중복은 없었고, 뺀 1건은 「아리프 듀얼락 풀그립 문틀 철봉」 — 첫 줄과 **이름이 사실상 같아** 화면에 같은 줄이 둘 뜬다. 옵션(문틀 폭)이 적힌 쪽만 남겼다.

**푸쉬업바는 한 건도 안 섞였다.** 검색어를 「풀업바」로 쓰지 않으면 그 함정은 애초에 안 생긴다는 것이 실측으로 확인됐다.

⚠️ **설치 방식을 상품명이 안 말한다.** 원룸에서는 문틀에 구멍을 뚫을 수 있느냐가 살지 말지를 가르는데, 10건 중 방식을 짐작할 수 있는 이름은 **3건뿐**이었다(`듀얼락`·`더블락`·`흠집방지`). 그래서 `note`에는 **이름에 실제로 쓰인 말만** 옮기고 나머지 7건은 비웠다 — 「압착식」이라 적힌 것을 믿고 산 사람이 나사를 박아야 하는 물건을 받으면 그 한 줄이 사고가 된다. 채우려면 상품 사진이 필요해 ⏸로 남겼다(방식을 몰라도 담을지 말지는 안 갈려서 급하지 않다).

**이로써 `SHOP_CANDIDATES` 5종이 다 찼다** — 덤벨 29 · 케틀벨 24 · 밴드 23 · 벤치 12 · 풀업바 10, 합 **98건.** 여기부터는 새로 뽑는 일이 아니라 분기마다 살아 있는지 보는 일이다.

---

## 2026-08-27 · 밴드 23건 — 검색이 주는 비율을 뒤집었다

**의도**: 밴드 링크가 0건이었다. 검색어 셋(`루프밴드`·`저항밴드`·`튜빙밴드`)으로 뽑아 온 52건을 반영했다.

**결과**: 중복 5건을 걷어 **고유 47건**, 그중 **23건**을 담았다(튜빙 9 · 평밴드 6 · 루프 6 · 풀업 보조 2).

**종류 비율이 거꾸로였다.** 앱의 밴드 운동 23개 중 **20개가 긴 밴드**(튜빙·평밴드)를 쓰고, 짧은 루프(힙밴드)로 되는 것은 **3개뿐**이다(몬스터 워크·힙 어덕션·힙 리프트). 그런데 검색이 주는 것은 정반대다 — **여는 운동은 긴 밴드 20 : 루프 3인데, 뽑아 온 링크는 19 : 22**였다. 그대로 담으면 목록의 절반이 3개짜리 물건이 된다 — **긴 밴드를 앞에, 루프는 대표만.**

⚠️ **앱은 이 차이를 모른다.** 루프만 산 사람에게도 23개가 다 열리지만 실제로 할 수 있는 건 3개다. 벤치는 앱이 평/조절식을 갈라서 알기라도 하는데(`BENCH_OPTIONS`), 밴드는 그것조차 없어 **고르는 단계에서만** 막을 수 있다.

**뺀 것**: 스텝퍼 전용 부속·8자 스트레칭밴드(단독으로 근력 운동이 안 된다) · 색상만 다른 중복(3컬러 요가밴드 3건 → 0건, 아이워너 계열 5건 → 1건). **세라밴드 3색은 남겼다** — 색이 곧 강도인 표준(옐로우 < 레드 < 그린)이라 셋이 서로 다른 선택지다.

**밴드는 무게 축이 없어 칩이 안 뜬다** — 목록이 통째로 나열되므로 개수가 곧 화면 길이다. 그래서 47건을 다 담지 않았다.

---

## 2026-08-27 · 벤치 12건 — 18건 중 6건을 걸러냈다

**의도**: 벤치는 덤벨을 가진 순간 +12가 되어 추천에 나타나는데(`MIN_GAIN`) 링크가 0건이었다.

**결과**: 벤치 **12건, 전부 각도 조절식.** 무게 축이 없어 칩도 배지도 안 뜨므로 **접이식·다기능을 앞에** 세웠다 — 원룸에서 접히는지가 가장 큰 차이다.

**뽑아 온 18건 중 6건을 걸러냈다.** 평벤치를 권하면 **사용자가 산 물건이 인클라인·디클라인 34개를 못 연다.**

- 이름으로 4건: 프로스펙스 폴더블 플랫벤치 · 핏분 플랫 벤치 B-FB100 · 반석스포츠 스케일 평벤치 · 평벤치 JF-107
- 사진으로 2건: 멜킨 볼튼(등판이 평평하고 붙은 것은 각도가 아니라 **바벨랙**) · 아디다스 싯업 디클라인(패드가 **한 판**이라 등판이 안 세워진다)

**두 단계로 가른다.** ① 이름에 「인클라인·디클라인·각도조절」이 있으면 담고, 「플랫·평벤치」뿐이면 뺀다. ② 셋 다 없으면 **상품 사진**을 본다 — 판정 기준은 **「시트와 등판이 갈라져 있는가」** 다. 갈라져 있고 등판 뒤에 계단식 조절 홈이 보이면 조절식이다.

⚠️ **「벤치프레스」·「멀티」·「14in1」은 다기능처럼 들려도 각도 조절을 뜻하지 않는다** — 실제로 그 셋 중 둘은 조절식이었고 하나는 아니었다. 이름으로는 못 가른다.

---

## 2026-08-27 · 덤벨 28건 + 아이폰 메모에서 링크를 자동으로 가져오는 길

**의도**: 덤벨은 추천 1순위인데 링크가 「가벼움」 1건뿐이라, 보통·무거움·조절식을 고른 사람에게는 보여줄 게 없었다.

**결과**: 덤벨 **29건**(신규 28 + 기존 1)으로 **네 구간이 다 찼다** — 화면에 무게 구간 칩이 뜬다. 경계는 5kg·15kg(온보딩 `WEIGHT_OPTIONS.dumbbell`과 같다).

**둘을 걸러냈다.** 「MMC 미니어처 덤벨 세트」는 무게 표기가 없는 장식품으로 보였고, 「노르드 덤벨 랙」은 거치대라 **여는 운동이 0개**다. 케틀벨 때 푸쉬업바를 걸러낸 것과 같은 판단이다.

**옮겨 적는 노동을 없앴다.** 폰에서 뽑은 링크를 카톡으로 하나씩 보내고 PC에서 하나씩 복사하던 것을, **아이폰 메모 → 크롬의 iCloud 메모 탭 → 클립보드**로 바꿨다. 사람이 하는 일은 「공유 → 메모에 추가」와 「덤벨 넣어줘」 두 개뿐이다.

⚠️ **어드민 자동화는 막혔다.** `sharelink.toss.im`이 금융 도메인이라 브라우저 확장의 안전 제한에 걸린다 — **링크 발급은 사람이 한다.**
⚠️ 메모 본문은 canvas라 DOM으로 안 읽힌다([T-227](claude-docs/troubleshooting/T-227.md)).

---

## 2026-08-27 · 링크 목록을 기구 문서에도 두고 테스트로 코드와 묶음

**의도**: 점검은 문서를 보고 하는데 정작 문서에 링크가 없어서 매번 코드를 열어야 했다. 기구 문서가 「그 기구의 링크를 관리하는 곳」이 되려면 목록이 거기 있어야 한다.

**결과**: 각 기구 문서에 「반영된 링크」 표(구간 · kg · 상품명 · `note` · 링크). 케틀벨 24건 · 덤벨 1건. 수집함은 「아직 반영 안 된 것」, 반영된 링크는 「화면에 뜨고 있는 것」으로 자리가 갈린다.

**같은 데이터가 두 벌이 되는 값은 테스트로 치른다.** `기구 문서 ↔ shareLinks.ts` 5건이 문서의 링크 목록과 코드를 **순서까지** 대조한다 — 손으로 맞추면 반드시 어긋나고, **어긋난 쪽이 문서면 죽은 링크를 영영 못 찾는다.**

⚠️ **「반영된 링크」 절만 센다.** 수집함까지 세면 링크를 붙여넣는 순간 빨간불이 켜져 계측기를 못 쓰게 된다.

---

## 2026-08-27 · 쉐어링크 작업대를 기구별 파일로 쪼갬

**의도**: 한 장짜리 시트가 케틀벨 24건을 넣는 동안 이미 길어졌고, 남은 4종을 채우면 더 길어진다. 덤벨 하나 하려고 다섯 기구의 함정을 전부 스크롤해 지나야 했다.

**결과**: [`docs/sharelinks/`](docs/sharelinks/README.md) — 공통 규칙(뽑는 법 · `weight`/`kg`/`note` 역할 · 점검 절차)은 README 한 곳에, **기구마다 파일 하나**(덤벨·케틀벨·벤치·밴드·풀업바). 기구 파일에는 그 기구에만 해당하는 것만 둔다: 검색어 · 그 기구의 함정 · 구간 경계 · 수집함 · 점검 기록.

**개수는 README 표 한 곳에서만 센다.** 기구 파일에 또 적으면 두 숫자가 반드시 어긋난다 — PR 번호를 여기 안 적는 것과 같은 이유다.

**「점검 기록」이 새로 생겼다.** 링크를 마지막으로 확인한 날짜는 코드에도 대시보드에도 안 남는다. 언제 봤는지 모르면 분기 점검이 매번 처음부터가 된다.

---

## 2026-08-27 · 케틀벨 24건 전부 + 무게 구간 필터

**의도**: 앞서 24개 중 4개만 골라 넣었는데, 그러면 **고르는 일을 우리가 대신해 버린다.** 「내 무게에 맞는 게 없는」 사람이 생긴다. 많이 담고 거를 수단을 주는 쪽으로 뒤집었다.

**결과**: 케틀벨 **24건 전부** + 펼친 목록 위에 **무게 구간 칩**(전체 / 가벼움 10 / 보통 7 / 무거움 2 / 조절식 4). `Product.weight`가 필터 축이고, 어휘는 앱이 이미 쓰는 것(`WeightBand`)에 조절식을 더한 것이다 — 온보딩에서 고른 말과 같아야 사용자가 대응시킨다.

**`note`의 역할이 바뀌었다.** 구간은 칩이 말하고 무게는 이름에 있으니, `note`에는 **그 둘이 못 하는 말만** 적는다(`소프트 · 바닥 보호` · `철제` · `저소음` · `덤벨 겸용` · `5단계`). 안 그러면 필터를 걸었을 때 24줄이 전부 같은 말로 시작한다.

**칩은 구간이 둘 이상일 때만 뜬다.** 하나뿐이면 「전체」와 결과가 같아 누를 이유가 없다 — 덤벨(가벼움 1건)에는 칩이 없다. 빈 구간의 칩도 안 만든다(`productBands`). 눌러도 아무것도 안 나오는 버튼은 고장으로 읽힌다.

**필터 초기화 코드는 0줄이다.** 목록을 컴포넌트로 빼고 `key={기구}`를 줬더니, 다른 기구를 펼치면 새로 마운트되면서 저절로 「전체」로 돌아온다.

**⚠️ 접근성 두 가지를 실측으로 잡았다.** ① 선택 칩 배경이 `--blue`면 흰 12px 글씨와 **3.71:1**로 WCAG AA(4.5:1) 미달이라 `--blue-dark`(5.41:1)로 바꿨다. ② `ui.chip`의 세로 여백이 얇아(27px) 손가락에 작았다 — 누르는 자리에서는 35px로 키웠다. 원래 이 스타일은 **누르지 않는 라벨**용이었다.

**⚠️ 콘솔에서 진짜 버그를 잡았다.** `ui.chip`이 `border` shorthand를 쓰는데 호출부가 `borderColor`만 덮으면, React가 리렌더에서 그 값을 지운다(*"Removing borderColor"*). **첫 렌더에는 멀쩡하다가 리렌더에서만 색이 풀려** 눈으로는 못 잡는다. `ui.chip`을 `borderWidth`/`Style`/`Color`로 쪼개 근본을 고쳤다 — **홈 화면의 목적 칩도 같은 버그를 갖고 있었고** 함께 나았다.

**무게 배지를 왼쪽에 세웠다.** 목록이 무게순인데 숫자가 이름 끝에 묻혀 있어 정렬돼 있다는 게 눈에 안 들어왔다. 이름에서 kg을 떼어(`앳플리 소프트 케틀벨 4kg` → 배지 `4kg` + 이름 `앳플리 소프트 케틀벨`) 오른쪽 정렬 + `tabular-nums`로 세우니 자릿수가 달라도 kg이 맞아떨어진다. **조절식만 이름에 무게를 남겼다** — 배지가 「조절식」이라 떼어 내면 「바이줌 조절 케틀벨」 둘이 구분되지 않는다.

**상품 썸네일은 못 넣는다.** 쉐어링크 운영정책이 상품 이미지 사용을 **사전 확인 대상**으로 집는데, 우리 케이스가 그 목록에 그대로 걸린다 — 이미지를 수집해 저장하는 것, 이미지 URL을 외부 앱에서 직접 표시하는 것, **추천 영역의 썸네일로 반복 노출하는 것**. 상품 이미지에 셀러·제3자의 권리가 얽혀 있어 토스가 일괄로 허락하지 못한다. 게다가 썸네일 URL을 얻을 경로도 없다(발급 API 응답에 이미지가 없고, 발급된 링크를 프로그램으로 열어 긁는 건 무효 클릭 금지에 걸린다). 넣으려면 `sharelink@toss.im` 문의가 먼저다.

**확인**: 칩 클릭 → 목록이 정확히 그 개수로 좁혀짐, 기구 전환 시 「전체」 복귀, 모바일 375px에서 칩이 2줄로 접히고 가로 스크롤 없음, 배지 24개가 모두 같은 x좌표에서 끝남(오른쪽 91px), 가장 긴 이름도 1줄, `aria-pressed` 정확, 새 탭 콘솔 에러 0건. 테스트 214건(신규 17건).

## 2026-08-27 · 케틀벨 쉐어링크 4건

**의도**: 작업 시트를 만들자마자 첫 기구를 채웠다.

**결과**: 케틀벨 **4건**. 어드민에서 24개를 뽑아 왔고 그중 **무게 구간이 겹치지 않는 것만** 골랐다 — 가벼움(4kg 소프트) · 보통(10kg 저소음) · 무거움(16kg), 그리고 **조절식 하나**(트위스트벨 7단계). 마지막 하나가 중요한데, 이 앱은 「모르겠어요」가 1급 선택지라(`equipSpec`) 자기 무게 구간을 못 고르는 사람에게 줄 답이 필요하다.

**같은 구간을 둘 놓으면 고르는 기준이 사라진다.** 24개를 다 넣으면 목록이 아니라 잡음이 된다. 남은 20개는 버렸다 — 대체가 필요하면 다시 검색하는 게 예비 목록을 관리하는 것보다 싸고, 발급 이력은 어드민 「링크 관리」에 남아 있다.

**⚠️ 푸쉬업바가 하나 섞여 들어왔다.** 작업 시트에 적어 둔 바로 그 함정이다(「풀업바」로 검색하면 섞인다). 푸쉬업바를 쓰는 운동은 데이터에 0개라 넣어도 화면에 안 뜬다 — 걸러냈다.

덤벨 `note`도 앱 라벨과 맞췄다(`가벼움 · 입문용` → `가벼움 · 5kg 이하`). 시트에 「앱이 쓰는 구간 라벨을 그대로 쓴다」는 규칙을 세워 놓고 기존 항목이 어긋나 있었다.

**확인**: dev 서버 기구 탭에서 케틀벨 「상품 4개」가 펼쳐지고 네 줄의 note가 구간별로 갈리는 것까지 실측했다.

## 2026-08-27 · 쉐어링크 작업 시트 신설

**의도**: 링크 채우기는 한 번에 끝나는 일이 아니라 틈틈이 나눠 하는 노가다다. 매번 「어디서 뽑더라 / 뭘 검색하더라 / 어디까지 했더라」를 다시 찾지 않게 한 장에 모았다.

**결과**: `docs/sharelink-worksheet.md`(뒤에 기구별 파일로 쪼개졌다 → [docs/sharelinks/](docs/sharelinks/README.md)). 뽑는 절차, **기구 5종별 검색어와 함정**, `note`에 쓸 어휘(앱의 무게 구간 라벨과 같은 말), 임시 수집함, 점검 방법이 들어 있다.

**필요한 건 5종뿐이라는 걸 명확히 했다** — `SHOP_CANDIDATES`(덤벨·케틀벨·벤치·밴드·풀업바). 나머지 기구는 링크를 넣어도 추천에 안 떠서 화면에 안 나온다. 모르면 11종을 다 채우려 든다.

**수집함은 「아직 반영 안 된 것」만 담는다.** 옮긴 줄은 지운다 — 링크가 두 곳에 살면 어느 쪽이 맞는지 알 수 없게 된다.

**⚠️ 링크 생존 확인을 스크립트로 하지 않기로 했다.** 운영정책이 *"무효 클릭, 자동 실행"* 을 금지하는데, 추적 링크에 자동 요청을 보내면 클릭으로 집계될 수 있고 토스 쪽에서는 점검과 어뷰징을 구분할 방법이 없다. 대신 **실적 대시보드에서 「클릭은 있는데 전환이 0」인 기구를 1차 신호로 삼고**, 의심되는 것만 사람이 눌러 본다. 죽은 링크는 앱이 못 잡는다 — 주소가 있으면 버튼도 뜨고 404를 열어 주며 수익만 조용히 0이 된다.

## 2026-08-27 · 쉐어링크 Open API는 안 쓰기로 — 조사하고 접었다

**의도**: 상품 링크를 손으로 채우는 대신 API로 일괄 수집할 수 있는지 알아봤다.

**결과**: **API는 있고 명세도 공개돼 있다**([sharelink-docs.toss.im](https://sharelink-docs.toss.im/)) — plan.md에 적어 뒀던 *"공개 문서에 명세가 없다"* 는 낡은 서술이었다. 그런데도 **안 쓰기로 했다.** 심사제라 연동 신청서를 넣어야 하고, 신청서가 요구하는 「브라우저로 열리는 서비스 URL」을 미니앱(`intoss://home-workout-hard`)은 댈 수 없다. 결정적으로 **키워드 검색 API가 없어서** 「덤벨」로 상품을 찾을 수가 없고 — 조회는 카테고리 베스트·하루특가·베스트·상품 상세뿐이다 — 운영정책은 *"Open API 데이터로 별도 상품 DB를 구성하는 경우"* 를 제한 대상으로 집는데 `shareLinks.ts`가 정확히 그것이다.

**이 API의 대상은 「매일 갈리는 편성」이다.** 문서가 *"상품 목록을 받아 게시글·구좌를 구성"* 이라 쓰고 랭킹이 시간마다 바뀌니 캐싱하라고 안내한다 — 딜 카페·오픈채팅방 봇처럼 손 복사가 물리적으로 불가능한 곳을 위한 것이다. 고정 40건을 한 번 박는 우리는 애초에 대상이 아니었다. 실사용 사례는 웹에서 하나도 못 찾았다(출시 공지조차 없다).

**대신 발급 경로가 하나 짧아졌다.** 어드민 웹(`sharelink.toss.im`)의 「링크 → 상품 조회」에서 상품 카드의 「링크 발급」을 누르면 된다 — **토스쇼핑 앱을 켤 필요가 없다.** 옛 절차를 그대로 따르고 있었다.

## 2026-08-27 · 개발용 입구를 배포 번들에서 걷어냄

**의도**: 출시된 앱의 기록 탭 아래에 개발자용 버튼 두 개가 그대로 노출돼 있었다. 배포판에서는 진입 경로를 막아야 한다.

**결과**: 둘을 다르게 처리했다. **`개발자용 · Phase 0.5 실측`은 삭제** — 실측은 2026-08-26에 끝났고(광고 8/8 · 쉐어링크 복귀 정상) 코드 주석에도 "실측이 끝나면 걷는다"고 적어 뒀던 것이라, 조건부로 살려 두는 것보다 지우는 쪽이 싸다. `Probe.tsx`·`returnTracker`·`verifyLog`가 함께 나갔다(테스트 22건 감소, 219 → 197). **`온보딩 다시 보기`는 남기되 `import.meta.env.MODE !== 'production'` 뒤로 넣었다** — 폰에서는 localStorage를 손댈 방법이 없어 이 버튼 없이는 온보딩을 실기기에서 두 번 볼 수 없다.

**숨긴 게 아니라 번들에 없다.** `vite build`의 기본 모드가 `production`이라 조건이 상수로 접히고 블록이 트리셰이킹으로 사라진다. 새 config·env 파일·런타임 플래그는 하나도 늘지 않았다 — Vite가 이미 주는 값 한 줄이다.

**검증은 양방향으로 했다.** production 번들에서 「온보딩 다시 보기」 0건, development 번들에서 1건, 그리고 **같은 production 파일에서 일반 사용자 문구는 1건**이 잡혔다. 세 번째가 대조군이다 — 0건만으로는 「제거됐다」와 「검사 문자열이 틀렸다」가 구분되지 않는다.

**⚠️ 검수 중인 `20260827-10`에는 아직 개발용 입구가 들어 있다.** 이 변경은 다음 번들부터 적용된다. 실기기 확인용으로는 `npm run release:dev`를 새로 뒀는데, **둘이 같은 `dist/`를 쓰므로** 개발 번들을 만든 뒤 그대로 `ait deploy` 하면 개발용 입구가 딸려 나간다.

## 2026-08-27 · 첫 출시 🎉 그리고 기구 선택 화면 번들 제출 (`20260827-10`)

**의도**: 승인된 번들을 라이브로 올리고, 곧바로 새 기구 선택 화면을 다음 버전으로 넣는다.

**결과**: `20260826-9`가 **2026-08-27 12:28에 출시**됐다. 사용자가 콘솔 웹에서 눌렀고 `bundle_get_live_version`으로 `deployed: true`를 확인했다. 등록 후 첫 출시까지 검수 3회 왕복이 걸렸고, 세 번 모두 사유는 **탭바 하나**였다.

이어서 `20260827-10`(2열 정사각 기구 그리드 + 아이콘 8종 + 「다음」 하단 고정)을 올려 UPDATE 검수를 넣었다 — `REVIEWING`. 라이브는 `20260826-9` 그대로라 쓰는 사람에게는 영향이 없다.

**올리기 전에 번들에 수정이 실제로 들어갔는지 확인했다.** 2차 반려 때 이 확인을 건너뛸 뻔했던 경험(T-224) 때문인데, 마커는 **소스에 따옴표째 실린 리터럴만** 골랐다 — 런타임에 조립되는 값은 번들에 리터럴로 없어서 마커 구실을 못 한다. 업로드는 HTTP 200과 함께 **보낸 바이트가 로컬 파일 크기와 같은지**로 검증했다.

**콘솔의 「미니앱 주소가 바뀌었으니 서버에 허용해 달라」 안내는 우리와 무관하다.** 파트너가 자기 백엔드를 둔 경우의 이야기인데 이 앱은 서버가 0대다. 소스에 `tossmini.com` 주소가 한 곳도 없고, 바깥 호출은 운동 이미지 CDN과 쉐어링크뿐이라 둘 다 우리 서버가 아니다.

## 2026-08-27 · 3차 검수 승인 — 탭바 사유가 세 번 만에 풀렸다

**의도**: 검수 결과를 확인하고 출시까지 진행하려 했다.

**결과**: 3차 검수(`reviewRequestId 159653` · 번들 `20260826-9`)가 **APPROVED**. 1·2차를 막았던 「플로팅 형태」 사유는 `safe-area`에 바닥값(`max()`)을 주고 좌우를 가이드 비율(30px)에 맞춘 것으로 풀렸다 — **문제는 끝까지 탭바 하나였다.**

**출시는 MCP로 못 한다.** `releaseDecision: ALLOWED`는 「콘솔 웹 출시하기 버튼이 눌리는 상태」라는 뜻일 뿐이고, 라이브 전환 도구가 아예 없다. `bundle_rollback`은 과거에 라이브였던 번들로만 되돌리는 것이라 첫 출시에는 못 쓴다(이 앱은 `liveBundleVersionName: null`). 콘솔 웹에서 사용자가 눌러야 한다.

**승인된 번들에 오늘 만든 기구 선택 화면은 없다.** 반려 3번을 지나온 만큼 일단 출시해 앱을 살리고, 새 화면은 다음 번들로 UPDATE 검수를 넣기로 했다.

## 2026-08-27 · 기구 선택을 2열 정사각 버튼으로 — 아이콘 8종 추가

**의도**: 기구 목록이 한 줄짜리 체크 항목이라 무엇을 고르는지 눈에 안 들어왔다. 칸을 크게 만들고 기구마다 앱 전용 아이콘을 붙였다.

**결과**: 170×170 정사각 2열. 칸 안에 44px 아이콘 + 이름 + 해금 수치가 들어가고, 상세 칩은 그 줄 아래 전폭으로 펼친다. 고른 칸은 테두리·바탕·체크 셋이 같이 바뀐다 — 색 하나에만 기대지 않기 위해서다. 온보딩과 「보유 기구」가 같은 컴포넌트라 양쪽이 함께 바뀌었다.

칸이 10개면 세로가 약 900px이라 「다음」이 두 화면 아래로 밀린다. **온보딩에서 다음을 못 찾는 건 그대로 이탈**이라 하단에 고정했다.

아이콘은 바벨·밴드·풀업바·벤치·짐볼·폼롤러·메디신볼·복근롤러 8종. **밴드만 면으로 채웠다** — 꼬리로 갈수록 가늘어 보이려면 잉크가 실제로 얇아져야 하는데 선으로는 안 된다(굵기는 path 하나에 하나뿐). 채우면 굽은 데 안쪽이 제 살을 파고들어서, 겹침 지수를 재며 굵기를 찾는 생성기를 함께 넣었다. 경위는 [T-225](claude-docs/troubleshooting/T-225.md).

## 2026-08-26 · 3차 검수 제출 (`20260826-9`)

**의도**: 탭바 여백·화면 아이콘·스토어 스크린샷이 모두 반영된 번들로 다시 심사를 넣었다.

**결과**: 앱정보 검토는 `APPROVED`, 번들은 `REVIEWING`. 3차도 같은 문구로 반려되면 추측을 접고 토스에 문의한다 — 수치가 가이드 그림 환산값과 맞는데도 막힌다면 우리가 못 읽는 기준이 따로 있다는 뜻이다.

## 2026-08-26 · 스토어 스크린샷 재촬영 (새 탭바 + 새 아이콘)

**의도**: 탭바 여백과 화면 아이콘이 연달아 바뀌어 콘솔에 올린 이미지가 전부 낡았다. 옛 화면이 찍힌 채로 재제출하면 심사에서 보는 것과 스토어에 뜨는 것이 다르다.

**결과**: `raw/` 4장을 다시 찍고 `render-screenshots.ps1`로 `store/` 5장(세로 636×1048 ×4, 가로 썸네일 1932×828)을 다시 냈다. 콘솔 「앱 정보」에도 반영했다. 문구는 그대로 두고 화면만 갈았다.

## 2026-08-26 · 2차 반려 — 탭바 여백에 바닥값을 줬다

**의도**: 플로팅으로 고쳐 재제출했는데 **글자 하나 다르지 않은 같은 사유로 또 반려**됐다. 수정이 번들에 들어갔는지부터 확인했고(머지 22:23 → 번들 업로드 22:24:25), 들어가 있었다 — 즉 고친 화면을 보고도 미달로 판정한 것이다.

**결과**: 원인은 `safe-area`에 **더하기만 한** 것이었다. `env(safe-area-inset-bottom)`이 0으로 오는 환경에서는 `+12px`이 그대로 띄운 거리가 되어 「밑변에 붙은 바」로 읽힌다. 개발 중 보던 브라우저가 정확히 그 환경이라 끝까지 안 보였다.

`max()`로 바닥값을 보장하고(`--tab-lift`), 좌우 여백은 가이드 그림에서 비율로 환산해 30px로 넓혔다(`--tab-side`). 실측 캡슐 폭 **330px** — 가이드 그림 환산값 330.6px과 맞는다. 경위는 [T-224](claude-docs/troubleshooting/T-224.md).

**탭 아이콘은 일부러 안 건드렸다** — 사유가 「플로팅 형태」라 아이콘과 무관하고, 둘을 같이 바꾸면 3차에서 또 반려됐을 때 원인을 못 가른다.

## 2026-08-26 · 이모지를 걷어내고 앱 전용 아이콘 6종을 그렸다

**의도**: 화면의 이모지가 기기·OS마다 다른 그림으로 뜨는 데다, 어느 앱에나 있는 남의 그림이라 화면이 「아무거나 갖다 붙인 것」처럼 보였다.

**결과**: 앱 아이콘([`assets/icon/icon.svg`](assets/icon/icon.svg))과 같은 언어 — **24 그리드 · 2px 라운드 스트로크** — 로 6종을 그려 `components/Icon.tsx` 한 곳에 모았다. 색은 `currentColor`라 탭이 활성이면 파랑, 아니면 회색으로 **부모가 정한다**(이모지로는 못 하던 것이다).

바뀐 자리는 일곱이다 — 하단 탭 3(20px) · 목적 카드 3(28px) · 홈 목적 칩(14px) · 운동 완료(48px).

**모티프를 하나 바꿨다**: 근육 키우기는 💪가 아니라 **원판**이다. 이두박근 실루엣은 20px에서 반드시 뭉개지고, 원판은 앱 아이콘에서 쏟아지는 그 흰 원판과 같은 물건이라 계보가 이어진다. `✓`·`＋`(기구 선택)는 컬러 이모지가 아니라 문자 기호라 그대로 뒀다.

시안은 세 안(가는 선 / 채운 면 / 두툼한 획)을 실제 렌더 크기로 놓고 골랐다. **40px 시안에서만 판정했다가 세 개가 20px에서 무너진** 경위는 [T-223](claude-docs/troubleshooting/T-223.md).

## 2026-08-26 · 심사 반려 — 하단 탭을 플로팅 캡슐로 고쳤다

**의도**: 첫 심사가 반려됐다. 사유는 **하나뿐**이었다 — 탭바가 토스 브랜딩 가이드의 플로팅 형태가 아니라는 것. 기능·광고·쉐어링크는 지적이 없었다.

**결과**: 탭바를 좌우·아래로 띄운 흰 캡슐로 바꾸고, 옛 탭바가 찍힌 스토어 스크린샷도 다시 찍었다.

- **원인은 「모바일 앱의 상식」이었다**([T-222](claude-docs/troubleshooting/T-222.md)). 밑변에 붙이고 윗선을 긋는 건 네이티브 앱의 표준이지만, 미니앱은 토스 앱 **안에** 얹히는 화면이라 토스 자체 하단 탭과 형태가 겹친다 — 사용자가 지금 어디에 있는지 헷갈리는 것이 가이드가 든 이유다
- ⚠️ **수치 스펙은 문서에 없다.** 가이드가 주는 건 그림 한 장뿐이라 거기서 형태를 읽어 냈다 — 흰 캡슐 + 그림자 + 사방 여백
- ⚠️ **반려 사유는 `review_get_feedback`에 있다.** `review_get`의 `rejectMessages`는 빈 배열이었다 — 그것만 보면 "사유가 안 왔다"고 오판한다
- **스토어 스크린샷 4장 중 3장이 같이 낡았다.** UI를 고치면 `assets/screenshots/raw/`도 함께 의심한다. 겸사겸사 기구 추천 장은 스크롤 끝에서 찍어 **대가성 문구가 탭에 가리지 않게** 했다
- **재제출까지 마쳤다** — 번들 `20260826-8`(`REVIEWING`) + 콘솔 이미지 5장 교체(`IN_REVIEW`). 번들 검수와 앱정보 검토는 **별개 트랙**이라 앱정보가 검토중이어도 번들 요청이 막히지 않는다

---

## 2026-08-26 · 심사 제출 — 스크린샷·썸네일을 만들어 올렸다

**의도**: 실측·광고 통합·실기기 확인이 모두 끝나 심사 전제가 풀렸다. 남은 건 콘솔에 올릴 이미지였다.

**결과**: 세로 스크린샷 4장(636×1048) + 가로 썸네일 1장(1932×828)을 만들어 등록하고, 번들 `20260826-7`을 심사에 넣었다(`REVIEWING`).

- **재생성 스크립트로 남겼다**([`render-screenshots.ps1`](scripts/render-screenshots.ps1)). ⚠️ 콘솔은 **1px만 달라도 거부**하는데 **Chrome은 실패해도 종료코드가 0**이라, 성공 판정을 산출 파일의 실제 픽셀로 한다
- ⚠️ **636×1048은 폰보다 뭉뚱하다**(1.65:1 vs 2.16:1). 앱 화면을 그대로 넣으면 아래가 텅 비어 앱이 빈약해 보인다 — 캔버스에 문구를 얹고 화면은 비율을 지켜 배치했다
- **기구 추천 장만 맨몸 상태로 찍는다.** 덤벨을 가진 상태면 덤벨이 추천에서 빠져 **이 화면의 핵심인 「+96개(2.7배)」가 안 보인다**
- **썸네일은 기능이 아니라 태도로 연다** — 「루틴을 대신 짜준다」는 흔한 약속이라 구미를 못 당긴다는 지적을 받아 *"네, 어렵습니다. / 그래서 만들어 봤습니다. 같이 해봅시다."* 로 바꿨다
- **상세 설명도 함께 고쳤다.** 기구 탭이 추천 화면이 되면서 옛 설명("기구 탭에서 체크해요")이 실제와 어긋나 있었다 — 그대로 올렸으면 심사에서 걸릴 자리였다
- ⚠️ **올린 이미지는 검토 승인 뒤에야 조회에 보인다**([T-220](claude-docs/troubleshooting/T-220.md)). 같은 요청의 텍스트는 즉시 반영되는데 이미지만 빈 배열이라 실패로 오인하기 쉽다
- ⚠️ **연령(`minAge` 19)은 우리가 못 바꾼다**([T-221](claude-docs/troubleshooting/T-221.md)). 딥시그널이 14세인 걸 근거로 「설정 가능한 값」이라 추론했는데, 그건 **게임이라 등급분류를 받은 것**이었다. 비게임은 토스가 정한다 — **조작 방법을 확인하기 전에 「바꾸세요」라고 안내한 것**이 실수였다

## 2026-08-26 · 기구 추천을 2단으로 + Phase 0.5 ② 통과

**의도**: 기구 카드를 누르면 특정 상품 하나로 곧장 나가 버려서, 앱 안에서 **고를 수가 없었다.** 상품 목록을 앱에 두되 해금 수치를 잃지 않는 구조가 필요했다.

**결과**: 1단은 「몇 개가 열리는지」, 펼치면 2단으로 「무엇을 사면 되는지」. 상품을 곧바로 늘어놓으면 **「+96개」가 상품 사이에 묻혀 사야 하는 이유를 잃는다** — 숫자로 이유를 먼저 말하고 그다음에 살 것을 보여준다.

- 한 번에 **하나만 펼친다** — 다 열면 다시 「쫘라락」이 되어 나눈 뜻이 없다
- ⚠️ **가격은 안 넣는다.** 정적 파일이라 바뀌어도 못 따라가고, **틀린 가격은 없는 것만 못하다**
- ⚠️ **미니앱 안에 토스쇼핑을 띄우는 방법은 플랫폼에 없다.** SDK도 딥링크도 WebView API도 없다는 게 앱인토스 개발자 커뮤니티의 [공식 답변](https://techchat-apps-in-toss.toss.im/t/topic/4102)이다 — 링크는 외부 브라우저로 열린다. **우리 구현의 한계가 아니라 플랫폼 제약**이라, 이걸 모르면 고치려다 시간을 버린다

**그리고 Phase 0.5 ②가 통과했다.** 기구 탭에서 토스쇼핑을 열고 돌아왔는데 **기구 탭 그대로였다** — 미니앱이 재시작되지 않았다는 뜻이다. 설계 때 「쉐어링크를 열면 복귀가 불안정하다」는 제보를 리스크로 달아 뒀는데 우리 앱에는 해당하지 않는다. 검증을 Probe 화면이 아니라 **실사용 경로에서** 한 것이라 더 강한 증거다. **이로써 Phase 0.5가 완전히 끝났다.**

## 2026-08-26 · 첫 쉐어링크(덤벨) + 대가성 문구를 공식 문장으로

**의도**: 추천 화면은 만들어 뒀는데 링크가 없어 버튼이 안 떴다. 덤벨 링크 하나를 받아 붙였다.

**결과**: 덤벨 카드에 「토스쇼핑에서 보기」가 뜬다. 나머지 넷은 링크가 없어 **버튼만 안 뜨고 추천은 그대로 보인다** — 설계한 대로다.

- ⚠️ **대가성 문구를 임의로 지어 놨던 것이 드러났다.** 토스가 쉐어링크를 발급할 때 **정확한 문장을 함께 준다**(*"✱ 이 포스팅은 토스쇼핑 쉐어링크 활동의 일환으로…"*). 뜻이 같아도 내가 쓴 문장은 규정 위반이라 `DISCLOSURE` 상수로 박고 화면은 그것만 참조한다
- ⚠️ **「풀업바」로 검색하면 푸쉬업바가 섞여 나온다** — 실제로 첫 링크가 그랬다. 푸쉬업바를 요구하는 운동은 데이터에 **0개**(푸시업 계열이 전부 맨몸으로 분류돼 있다)라 폼롤러와 같은 처지다. **손목을 편하게 해줄 뿐 할 수 있는 운동을 늘리지 않는다** — 앱의 추천 기준이 그대로 걸러낸 셈이다
- 5kg 고정 중량이지만 **`requires`는 무게를 안 보므로 덤벨 운동 123개가 전부 열린다.** +96개는 그대로다
- 문구가 하단 탭에 가려 보였는데 **실측하니 24px 여유가 있었다** — `fullPage` 스크린샷이 fixed 탭을 스크롤 끝에 그려서 생긴 착시였다(관측 경로를 먼저 의심하라는 그 경우다)

## 2026-08-26 · 기구 탭을 추천 화면으로 — 「막무가내로 보여주지 않는다」

**의도**: 홈의 목적 칩과 하단 「기구」 탭이 **같은 화면으로 갔다.** 진입점이 둘인데 목적지가 하나면 탭 하나가 낭비다. 기구 탭을 쉐어링크가 붙을 추천 화면으로 돌리고, 보유 기구·목적은 목적 칩에서 여는 전체화면으로 갈랐다.

**결과**: 안 가진 기구 중 **지금 못 하는 운동을 많이 열어주는 것**만 순서대로 보여준다. 설득은 "이거 사세요"가 아니라 **「+96개」라는 숫자**가 한다.

- **선별을 두 겹으로 했다** — 후보 목록(사람이 정한다)과 최소 해금 수(데이터가 정한다)
- ⚠️ **바벨을 뺀 건 데이터를 거스른 판단이다.** 운동 수 2위(+95)로 덤벨 바로 다음인데, 숫자만 믿으면 **원룸 사용자에게 200cm 봉과 랙을 권하게 된다.** 안전 문제도 혼자 드는 사람에겐 다르게 걸린다
- ⚠️ **폼롤러는 근력 운동이 +0이었다** — 사도 루틴이 한 개도 안 바뀐다. 스트레칭 도구지 근력 기구가 아니다. 메디신볼(+2)·복근롤러(+1)·짐볼(+9)과 함께 뺐다
- **`MIN_GAIN = 10` 하나가 벤치를 가른다** — 맨몸이면 +4라 안 뜨고, 덤벨을 가진 순간 +12가 되어 나타난다. 조건문 없이 「지금 뭘 가졌나」가 추천을 바꾸는 자리라 실제 데이터로 양쪽을 테스트했다
- ⚠️ **`recommend`는 근력 필터를 안에서 한다.** `unlockGain`은 호출부에 맡기는데, **이 함수의 첫 테스트가 그 함정에 그대로 걸렸다** — 맡기면 잊는다는 증거라 안으로 넣고 테스트로 못 박았다
- 배수는 **1.5배 이상일 때만** 띄운다. 화면에 「1.1배」가 셋씩 늘어서니 정보가 아니라 소음이었고, 「겨우 1.1배」로 읽혀 +18개라는 사실까지 깎아먹었다
- **쉐어링크는 아직 비어 있다**([`shareLinks.ts`](miniapp/src/data/shareLinks.ts)). 링크 없는 기구는 버튼만 안 뜨고 추천은 그대로 보인다 — 파일만 채우면 살아난다. 대가성 문구도 **링크가 하나라도 있을 때만** 뜬다
- 테스트 204 → **216건**

## 2026-08-26 · 휴식에 광고를 붙였다 — 8/8 실측 위에서

**의도**: Phase 0.5 ①의 답이 나왔다. **8회 시도, 8회 전부 노출 성공.** 이 아이템의 수익 전체가 걸려 있던 가정이 통과했으므로 광고를 실제 휴식에 붙인다.

**결과**: 휴식이 시작되면 `adPlan`이 판단하고, 틀기로 하면 load → show 한다. 광고는 **수익이지 기능이 아니라서**, 실패해도 사용자에게 아무것도 안 보인다.

- **실측 조건이 실사용보다 가혹했다** — 연속으로 눌러댄 8회다. 실제 휴식은 45~150초 간격이라 더 불리할 이유가 없다. frequency cap이 있었다면 애드몹 통상값인 3~5회에서 걸렸어야 하는데 안 걸렸다
- **배너 병행은 접었다.** plan에 「전면 광고 빈도가 낮게 나왔을 때만」이라는 조건부 항목이었고, 조건이 성립하지 않았다
- ⏸ **preload도 안 넣었다.** 순차(load→show)로 8회가 무손실이었고, 미리 받아 두면 마지막 세트처럼 **쓰지 않고 버리는 로드**가 생긴다. 광고가 눈에 띄게 늦게 뜨면 그때 넣는다
- **휴식 타이머는 한 줄도 안 고쳤다** — `restEndsAt`이 절대 시각이라 광고가 웹뷰를 가려 타이머가 스로틀돼도 남은 시간이 정확하다. 광고 자리를 비워 둔 채로 완성해 둔 설계가 값을 했다
- ⚠️ **미충전이어도 `slotsSinceLastTry`는 0으로 되돌린다** — 시도는 했기 때문이다. 안 그러면 백오프가 즉시 풀려 다음 휴식에 또 광고 서버를 두드린다. 이 전이는 테스트로 못 박았다
- 광고 상태는 `useRef` — 화면에 그릴 값이 아니라 state로 두면 광고 판단이 리렌더를 부른다
- **실기기 확인 완료**: 휴식에 광고가 뜨고, **닫으면 남은 휴식이 그만큼 줄어 있다.** 광고는 휴식 화면이 뜨고 **약 2초 뒤** 덮는다 — preload를 접은 판단이 여기서 확정됐다
- 테스트 198 → **204건**

## 2026-08-26 · 광고 지면 개설 + 이미지 CSP 리스크 해소

**의도**: 수익 모델 전체가 「휴식마다 광고를 틀 수 있는가」 하나에 걸려 있는데, 잴 지면이 없어 못 재고 있었다.

**결과**: 전면 광고 지면을 만들었다 — `groupId` **`ait.v2.live.f491615603f04220`**. 실측 도구는 이미 `adGroupId`를 입력받게 돼 있어 **코드 변경도 재배포도 필요 없었다.**

- 전면 광고는 `categoryId`가 **필수**다. 미니앱에 이미 붙어 있던 **생활 › 건강**(`3804`)을 그대로 썼다
- ⚠️ **문서가 예고한 `REGISTERING` 2시간 대기가 실제로는 없었다** — 생성 응답이 곧바로 `ENABLED`에 `groupId`까지 채워서 왔다. 「2시간 뒤에 재조회」를 전제로 일정을 잡을 필요가 없다
- ⚠️ 만든 순간부터 **23일 무노출 = 삭제 예정** 시계가 돈다
- **운동 이미지가 실기기에서 정상 표시됐다.** 설계 때부터 「미니앱 CSP가 막을 수 있다」고 달아 둔 미확인 리스크였고, 막혔으면 900장을 번들에 넣거나 호스트를 옮겨야 했다. jsDelivr CDN 그대로 간다

## 2026-08-26 · 기구 상세(무게·벤치 종류) — 그리고 평벤치 사용자에게 인클라인을 시키던 버그

**의도**: 기구를 「있다/없다」로만 물어서, 덤벨이 2kg인지 20kg인지 앱이 몰랐다. **「모름」을 1급 선택지로** 두는 게 전제였다 — 기준을 모르는 사람이 많고(밴드가 대표적이다), 모른다고 답해도 앱이 그대로 돌아야 한다.

**결과**: 기구를 고르면 그 아래 상세가 펼쳐진다. 무게는 구간(가벼움·보통·무거움·모름)으로 받고, 벤치는 평벤치/조절식/모름으로 받는다.

- ⚠️ **조사 중에 실제 버그가 드러났다.** 인클라인 28개 중 23개, 디클라인 12개 중 11개가 벤치를 요구하는데 데이터가 이걸 평벤치와 한 덩어리로 묶고 있었다 — **평벤치만 가진 사람에게 인클라인 벤치프레스가 나왔다.** 실제로 그날 첫 루틴이 그거였다
- 데이터 빌드에서 `benchAdjustable`을 갈랐다(`NEEDS_ADJUSTABLE = /incline|decline/i`). 승격은 `requiresOf` **끝**에서 한 번에 걸어 수동 예외(`Decline_Crunch` 등)까지 덮는다
- 조절식을 가진 사람은 평벤치 운동도 되므로 `effectiveOwned()`가 두 키를 함께 세운다. **「모름」은 안전하게 평벤치로 친다** — 모른다고 답한 사람이 못 하는 운동을 받는 쪽이 더 나쁘다
- **실측**: 벤치 「모름」 165개 → 「각도 조절」 **187개**(+22). 덤벨 「보통」을 고르면 운동 화면 첫 무게가 **0 → 10kg**으로 채워진다
- ⚠️ **무게 구간은 기구마다 다르다** — 덤벨 15kg은 무겁고 바벨 15kg은 가볍다. 한 테이블로 뭉치면 기본값이 엉뚱해진다
- **밴드 강도는 받되 앱이 쓰지 않는다.** 밴드 운동 23개는 강도와 무관하게 전부 된다 — 「여러 개 있어요」를 맨 위에 뒀다
- 풀업바·짐볼·폼롤러·복근롤러는 **아예 묻지 않는다.** 물어볼 스펙이 없고, 묻는 만큼 온보딩이 길어진다
- 테스트 168 → **198건**

## 2026-08-26 · 온보딩을 다시 볼 길을 냈다 + 콘솔 MCP로 배포 자동화

**의도**: 온보딩은 **한 번 끝내면 실기기에서 두 번 볼 수 없었다.** 폰에서는 localStorage를 손댈 방법이 없으니, 온보딩 화면을 고쳐도 확인할 길이 없다는 뜻이다. 배포도 매번 수동 웹 업로드였다.

**결과**: 기록 탭 개발자용 줄에 「온보딩 다시 보기」를 넣어 첫 진입 상태로 되돌린다. 그리고 앱인토스 콘솔 MCP를 붙여 번들 업로드·빌드 대기·테스트 발송을 Claude가 직접 한다.

- **`clearOnboarding()`은 목적만이 아니라 기구도 지운다** — 목적만 지우면 온보딩 1단계에 고른 기구가 남아 **첫 진입 경험이 재현되지 않는다.** 재현이 이 버튼의 존재 이유라 둘 다 지운다
- **운동 기록은 건드리지 않는다** — 온보딩을 다시 보려다 그동안 쌓은 기록이 날아가면 안 된다. 테스트로 못 박았다
- 배포 경로 확정: `bundle_upload` → 발급된 S3 URL에 PUT → `bundle_upload_complete` → 빌드 폴링 → `bundle_test_push`(테스트 푸시 + `privateLink`)
- ⚠️ **CLI용 API 키는 워크스페이스 왼쪽 「키」 메뉴에 있다** — 앱 안 개발 섹션이 아니라서 한참 못 찾았다. 결국 콘솔 MCP로 가서 키 자체가 불필요해졌다
- 테스트 164 → **168건**

## 2026-08-26 · 온보딩과 운동 목적 — 반복·휴식·종목 수를 목적이 정한다

**의도**: 첫 진입 사용자가 아무 맥락 없이 루틴부터 보게 돼 있었다. **기구 → 목적** 두 단계를 앞에 세우고, 고른 목적이 실제로 루틴을 바꾸게 했다.

**결과**: 체지방 감량 / 근육 키우기 / 건강 유지 세 목적이 **반복 횟수 · 휴식 길이 · 종목 수 상한**을 함께 정한다. 기구 탭에서 바꾸면 오늘의 루틴에 바로 반영된다(실측: 12~20회·60초·15분 ↔ 6~12회·150초·29분).

- ⚠️ **체지방 감량 휴식을 45초로 고른 이유** — 근거 권장은 30~60초인데 아래쪽을 쓰면 `MIN_REST_SECONDS`(40초)에 걸려 **광고가 통째로 사라진다.** 광고 때문에 늘리는 것도 금지라(「휴식은 운동이 정한다」), **권장 구간 안에서 40초를 넘는 값**을 골랐다. 이 불변식은 `goal.test`와 `session.test` 양쪽에서 못 박았다
- 짧게 쉬는 목적은 종목을 늘려 세션 길이를 지킨다 → **휴식 횟수는 체지방 감량이 더 많다**
- ⚠️ 종목 수는 **상한일 뿐이다.** `pickRoutine`이 근육 수와 `MAX_PER_MUSCLE`로 한 번 더 조여서, 가슴처럼 근육이 하나인 부위는 4를 넘겨도 3종목이 최대다. 화면 문구를 「최대 N종목」으로 쓴 이유
- 기구 선택 UI를 `EquipmentPicker`로 뽑아 온보딩과 기구 탭이 공유한다 — 갈라지면 고른 것과 보이는 것이 달라 보인다
- 테스트 140 → **164건**

## 2026-08-26 · 화면 시안을 Figma로 옮기고 PNG를 레포에 남겼다

**의도**: 화면을 코드 밖에서 눈으로 놓고 고칠 자리가 없었다. Figma 원격 MCP를 붙여 5개 화면을 옮기고, 리뷰·PR에서 바로 보이도록 PNG를 [`assets/mockups/`](assets/mockups/README.md)에 커밋했다.

**결과**: 아트보드 6장(오늘의 루틴 / 운동 진행 / 휴식 / **휴식·배너 광고안** / 기록 / 보유 기구). 색·간격은 `index.css` 변수를, 기구 해금 수치는 `exercises.json`에서 계산한 실제 값을 넣어 앱 화면과 같은 값이다.

- **배너 광고안 한 장만 코드에 없는 안**이다. ⚠️ 전면 광고는 시스템이 화면을 덮으므로 **레이아웃을 바꾸지 않는다** — 자리를 비워 둘 필요가 없다는 뜻이고, 배너를 실제로 쓸지는 Phase 0.5 실측에 달렸다
- 시안은 **코드의 복제본이지 단일 출처가 아니다.** 코드를 고쳐도 따라오지 않으니 그때마다 다시 뽑는다
- 폰트 함정 하나를 [T-219](claude-docs/troubleshooting/T-219.md)에 남겼다 — 목록에 있고 로드도 성공하는데 실재하지 않아 타이머가 통째로 안 그려졌다

## 2026-08-25 · 광고 정책 확인 — 수치 상한이 없다

**의도**: plan에 *"정책이 막으면 실측 이전에 끝이다"* 로 걸어 둔 항목이다. 콘솔 등록 신청이 들어가 심사를 기다리는 동안, **키 없이 확인되는 것**부터 치웠다.

**정책에 횟수 상한이 없다.** [인앱 광고 가이드](https://developers-apps-in-toss.toss.im/guide/monetization/in-app-ad)에는 *"사용자에게 광고가 너무 자주 노출되지 않도록 주의해 주세요"* 라는 권고만 있고 하루 몇 회·세션당 몇 회 같은 수치가 없다. **모델이 정책으로 죽지는 않는다** — 그러나 이건 *상한이 없다*는 뜻이 아니라 **상한이 정책이 아닌 다른 곳(fill rate)에 있다**는 뜻이다. Phase 0.5 ①의 실기기 실측은 여전히 유일한 답이고, 여전히 결정적이다.

**금지 조항은 우리 설계가 전부 통과한다.** 결제·계좌 개설 흐름 중 삽입 금지 / 종료·뒤로가기 방해 금지 / 필수 CTA 가리기 금지. 우리는 휴식 중에만 틀고, 광고가 도는 동안에도 휴식 타이머는 그대로 흐르며, 실패는 조용히 넘어간다. 제재가 **30일~영구 이용 제한 + 수익 환수**라 지킬 값이 크다. 앱인토스 인앱 광고는 **구글 애드몹 정책**을 따른다.

**측정 도구가 현행 API를 쓰는지도 확인했다.** `loadFullScreenAd`/`showFullScreenAd`는 현행이다(deprecated는 `TossAds.attach` → `attachBanner`, 그리고 `GoogleAdMob` v2 호환 별칭). **재는 도구가 낡았으면 측정값이 아니라 측정 자체가 틀린다** — 결정적 실측을 앞두고 확인할 값이었다.

## 2026-08-25 · appName은 표시 이름이 아니다 — `home-workout-hard`로 정정

**의도**: 콘솔 등록 화면이 `appName`을 *"수정할 수 없는 고유 ID"* 라고 표시했다. 우리 설정에는 한글 `홈트가어렵나`가 들어 있었으니, 되돌릴 수 없는 값을 넣기 전에 근거를 확인해야 했다.

**CLI 번들이 답을 갖고 있었다.** 검증 문구가 *"앱 이름은 케밥-케이스 형식이어야 합니다"* 이고, 무엇보다 `appName`이 **URL에 그대로 박힌다** — `intoss-private://<appName>?_deploymentId=…` 와 `…/bundles/<appName>/upload-start`. 한글이면 스킴의 host 자리에 한글이 들어간다.

**「빌드 통과」를 유효성의 증거로 삼은 게 틀렸다.** 이전 작업에서 한글 `appName`으로 `ait build`가 성공하는 걸 보고 "한글이 통과한다"고 적었는데, 그 검증은 `문자열인가 && 빈칸 아닌가` 그것뿐이었다. **약한 검사기를 통과한 것은 아무것도 증명하지 않는다** — 번들 문자열 grep이 양방향으로 고장 난 계측기인 것과 같은 종류의 착각이다. T-218에 남겼다.

**표시 이름과 ID를 분리했다.** 사람이 보는 이름은 **홈트가어렵나**(콘솔에서 수정 가능), 설정의 `appName`은 **`home-workout-hard`**(수정 불가). 아티팩트도 `home-workout-hard.ait`가 된다.

**아이콘 크기도 틀렸다.** 콘솔 「앱 로고」는 **600×600**을 요구하는데 렌더 기본값에 600이 없었고, `assets/icon/README.md`·`plan.md`에는 *"콘솔에는 `icon-1024.png`를 올린다"* 고 적혀 있었다 — **올려 본 적이 없으니 확인된 적도 없는 문장**이었다. 600을 기본값에 넣고 문서를 고쳤다. 원본이 벡터라 크기 요구가 바뀌어도 `-Sizes`에 숫자만 넣으면 된다.

## 2026-08-25 · 앱 아이콘 — 벤치에서 흘러내리는 덤벨

**의도**: 콘솔 등록에 아이콘이 필요하다. 앱 이름이 던진 질문에 홈 화면이 *"네, 어렵습니다"* 로 답하는데, **아이콘도 같은 말을 하게** 했다.

**AI 생성을 접고 SVG로 직접 그렸다.** HiggsField로 뽑아 봤더니 **출력 PNG에 워터마크가 픽셀로 박혀** 나왔다(다운로드해서 확인). 가격 페이지 전문 14,533자에 "watermark"라는 단어가 한 번도 없어서 **유료 전환이 이걸 푸는지조차 확인이 안 된다.** 월 $19를 내고 나서야 아는 건 나쁜 거래다. 형태 자체가 곡선 하나에 도형 몇 개라 AI가 필요한 그림도 아니었고, 벡터로 가니 워터마크·크레딧·해상도 문제가 한꺼번에 사라졌다.

**시안 15개를 거치며 오독을 세 번 만났다.** 헤드폰(봉이 위로 굽으면 아치+원판이 이어캡) → 웃는 얼굴(곡선을 뒤집으니 눈 둘에 미소) → 말굽자석(깊게 처지니 봉과 원판이 U자 덩어리로 뭉침). **셋 다 좌우대칭에서 나왔고**, 비대칭 구도로 가니 한 번에 풀렸다. 판별 기준은 [`assets/icon/README.md`](assets/icon/README.md)에 남겼다 — 다시 손댈 때 같은 길을 또 걷지 않도록.

**「봉을 휘면 덤벨이 아니게 된다」는 평면에서만 참이었다.** 3D에서는 명암이 재질을 알려줘서, 봉이 휘어도 원판이 원통 형태를 지키면 「같은 물체가 늘어지는 중」으로 읽힌다. 평면에서 얻은 결론을 3D에 그대로 옮긴 게 성급했다.

**PNG는 산출물이고 단일 출처는 `icon.svg` 하나다.** `scripts/render-icon.ps1`이 헤드리스 Chrome으로 6개 크기를 뽑고 **실제 픽셀 크기까지 검증**한다 — Chrome은 실패해도 종료코드가 0인 경우가 있어서다.

## 2026-08-25 · 배포 준비 — 앱 이름 확정 「홈트가어렵나」

**의도**: 광고를 재려면 앱이 실기기에 올라가 있어야 하고, 올리려면 콘솔 등록이 먼저다. 등록에 필요한 것부터 치우고, **키 없이 확인할 수 있는 데까지 미리 확인해 뒀다.**

**빌드 체인은 키 없이 전부 통과한다** — `npm run build` → `ait build` → `홈트가어렵나.ait`(109KB). 막히는 지점은 `ait deploy` 하나뿐이고 그건 콘솔 API 키가 필요하다. ~~한글 `appName`이 그대로 통과하는 것도 실측으로 확인했다.~~ ← **이 문장은 틀렸다.** 빌드만 통과할 뿐 deploy에서 깨진다. 아래 「appName은 표시 이름이 아니다」 항목이 정정본이다.

**이름이 던진 질문에 앱이 답한다.** 「홈트가 어렵나?」 → **"네, 어렵습니다."** 응원이 아니라 동의다. 그리고 제일 어려운 부분(뭘 할지 정하기)은 이미 정해뒀다고 말한다 — 이 앱이 실제로 하는 일이 그거다. 기록이 없을 때만 뜬다. 매일 보이면 농담이 아니라 잔소리가 된다.

**`npm run release`로 배포 2단계를 묶었다.** `ait build`는 `dist/`를 있는 그대로 싸기 때문에 빌드를 빼먹으면 옛 번들이 그대로 올라간다 — BookTimer T-150이 정확히 그 사고였다.

**당겨서 새로고침을 껐다.** 진행 중인 세션은 React state에만 있어서 새로고침 한 번에 그날 기록이 통째로 날아간다. 운동 중에 화면을 당기는 건 흔한 동작이다. 세션을 저장소에 넣기 전까지 이 설정 한 줄이 유일한 방어다.

**함정 1건**: `sed -i`가 CRLF 파일을 LF로 뒤집었는데 **훅이 지시한 `git diff --numstat` 검증이 정상으로 나왔다.** 이 레포는 `core.autocrlf = true`라 git이 diff 전에 줄끝을 정규화해버려서, numstat은 EOL 뒤집힘을 **원리적으로** 못 본다. 바이트로 확인해야 한다 (T-217).

## 2026-08-25 · Phase 2 — 화면 4개 (광고 없이 먼저)

**의도**: 광고가 없어도 앱이 온전해야 광고가 실패했을 때도 온전하다. 그래서 광고 자리를 **일부러 비워둔 채** 완성했다.

보유 기구 / 오늘의 루틴 / 운동 진행 / 기록 네 화면과 하단 탭. Phase 0.5 실측 도구는 기록 화면 아래 임시 입구로 살려 뒀다 — 실기기 측정이 아직 안 끝났다.

**화면을 만들자 데이터 결함 3건이 드러났다 — 로직 테스트로는 절대 안 잡혔다**

첫 화면을 열자 기구가 하나도 없는데 **풀업 3종**을 추천하고 있었다. 원본이 "자기 체중을 든다"는 뜻으로 풀업·행잉을 `body only`로 적어 뒀기 때문이다. 기준은 자기 체중이냐가 아니라 **매달 데가 있느냐**다. 9개를 `pullupBar`로 옮기고 회귀 테스트를 걸었다.

다음 루틴은 어깨였는데 3종목 중 2개가 **목 아이소메트릭**이었다. 홈트 루틴에 목 운동을 넣는 앱은 없다. 제외했다.

그 다음은 팔이었는데 **5종목이 전부 삼두**였다. 맨몸으로 이두를 할 수 없어 근육 버킷이 하나뿐이라 그렇다. 한 근육에 3개 상한을 넣었다 — 한 근육에 15세트는 과훈련이고, **그런 루틴은 사람을 떠나게 해서 광고 슬롯도 함께 사라진다.** 부위 최소 운동 수(3)도 같이 넣었다. 1종목짜리 "루틴"은 루틴이 아니다.

그 결과 맨몸 로테이션은 코어·팔·하체·가슴 4개로 돈다. **등은 0개, 어깨는 1개** — 매달 데 없이 등 운동은 불가능하니 정직한 결과이고, 덤벨 하나로 등 6개·어깨 35개가 열린다. 해금 훅에 이빨이 생겼다.

**돌연변이 하나가 살아남아 공허한 테스트를 잡았다**

`todayKey`의 KST 지정을 통째로 지웠는데 테스트가 통과했다. **개발 기계가 이미 한국 시간**이라 지정이 있으나 없으나 같은 값이 나왔기 때문이다. 시간대를 인자로 빼고 다른 시간대에서 답이 달라지는지를 보게 고쳤다. T-216으로 남겼다 — 로케일·통화·인코딩에 그대로 되풀이되는 구조다.

**측정**: 맨몸 3종목 = 9세트 → 휴식 8회. 덤벨 5종목 = 15세트 → **휴식 14회.** 이것이 세션당 광고 슬롯 수다.

**검증**: 테스트 140건. 돌연변이 16종 중 15종 사살 + 살아남은 1종은 원인을 고쳐 사살. 브라우저에서 전체 흐름(9세트 → 완료 → 기록 저장 → localStorage 확인)을 걸었고, 모바일 375×812에서 가로 넘침 0·글자 잘림 0·버튼 높이 51~55px를 수치로 확인했다. **스크린샷은 없다** — 브라우저 패널이 표시되지 않아 합성이 멈췄다(CLAUDE.md 함정 ①).

---

## 2026-08-25 · Phase 1 — 운동 데이터 정제 + 순수 로직 4종

**의도**: 실기기 광고 실측(Phase 0.5)을 기다리는 동안, 실측 결과와 무관한 부분을 전부 끝내 둔다.

**장비를 단일 값이 아니라 집합으로 바꿨다**

원본(free-exercise-db)의 `equipment`는 값이 하나뿐이라 **덤벨 벤치프레스가 벤치를 요구한다는 사실을 표현할 수 없다.** 그대로 쓰면 덤벨만 가진 사람에게 벤치프레스를 추천한다. `requires: []` 집합으로 바꿔 덤벨 27 · 바벨 27 · 밴드 1개의 벤치 의존을 살렸다. 밴드 보조 풀업(밴드+풀업바)도 같은 방식으로 표현된다.

**제외를 기본값으로 뒀다**

`other` 122개는 아틀라스 스톤·썰매·타이어·TRX 같은 잡동사니다. 하나씩 분류하는 대신 **홈트에서 되는 것만 골라 담고 나머지는 버렸다**(122 → 33). 잘못 뺀 운동은 안 보일 뿐이지만, 집에서 아틀라스 스톤을 추천하면 앱 전체를 못 믿게 된다. 머신·케이블 148개는 통째로 뺐다.

873 → **647개 채택**. 근력 415개 기준 해금 수치는 **맨몸 81 → 덤벨 176 (+95, 2.17배)** 로, 설계가 가정한 2.1배가 정제 후에도 살아남았다. 덤벨이 단일 최대 해금 장비라는 것도 그대로다 — 첫 쉐어링크 추천의 근거다.

**한글화는 통째 번역이 아니라 토큰 사전으로 했다**

한국 헬스 용어는 대부분 영어 음차이고 어순도 같다("Incline Dumbbell Curl" → "인클라인 덤벨 컬"). 647개를 손으로 옮기면 같은 단어가 파일마다 다르게 적히지만, 사전은 한 번 정하면 끝까지 일관된다. **미매핑 토큰이 하나라도 있으면 빌드가 실패한다** — 영어가 조용히 섞여 나가는 게 최악이라서.

**순수 함수 4종을 TDD로 만들었다**

`filterByEquipment` · `unlockGain` · `pickRoutine` · `adPlan`. 모두 실패 테스트를 먼저 쓰고 Red를 확인한 뒤 구현했다.

`adPlan`이 이 앱의 심장이다 — 휴식 40초 미만은 안 틀고(규율이 곧 수익 구조다), 미충전이 연속되면 그만큼 슬롯을 쉬고, 3회 연속이면 남은 세션을 조용히 포기한다. **광고를 못 틀어도 앱은 정상 동작해야 한다.**

`pickRoutine`의 seed는 날짜다. 같은 날 재접속해도 루틴이 안 바뀐다. 부위 로테이션은 「가장 오래 전에 한 부위」로, 균형은 부위 안에서 근육별 라운드로빈으로 잡았다 — 그냥 섞어 뽑으면 대퇴사두만 5개 나오는 날이 생긴다.

**검증**: 테스트 84건 통과. **돌연변이 10종을 주입해 전부 사살**(생존 0) — 필터 조건 뒤집기, 경계 밀기, 백오프·균형·시드 무력화 등. 주입이 실제로 일어났는지는 `git diff --numstat`으로 따로 확인했다.

**안 한 것**: `instructions`(운동 설명문)를 데이터에서 뺐다. 647 × 5문장 번역은 크고, 설계상 운동 진행 화면은 세트 기록 + 타이머라 설명문 자리가 없다. 이미지 2장이 동작을 보여준다. 필요해지면 그때 넣는다.

---

## 2026-08-25 · 아이템 확정 — 홈트 + 휴식 시간 광고 (restfit)

**의도**: 「가격 이력 검증」을 실물 확인 후 기각하고, 접었던 홈트 안을 **새 수익 모델로 부활**시켰다.

**가격 이력 검증을 기각한 이유** — 전제가 사실과 달랐다

토스쇼핑 상품 상세에 **「최대할인가」 표시와 「가격 비교하기」 버튼이 이미 있었다.** 눌러보면 네이버·11번가·쿠팡·G마켓 크로스몰 비교가 뜨고 해당 몰로 이동까지 된다. *"커머스 사업자는 자기 할인이 가짜라고 말 못 한다"* 는 핵심 전제가 틀렸다 — **이 회사는 자기 매출을 깎으면서까지 타 몰로 보내주며 가격 정직성을 브랜드로 쓰고 있다.** 남은 자리(시간축)는 비어 있었지만 해자가 60일뿐이라, 플랫폼이 채울 자리에 밖에서 들어갈 이유가 없었다.

**홈트를 부활시킨 것은 기능이 아니라 수익 모델이다**

세트 사이 휴식 타이머 동안 전면 광고를 재생한다. 보통 리워드 광고는 "참으세요"라 손해지만, **운동 중 휴식은 어차피 기다리는 시간**이라 마찰이 없다. 한 세션 12세트면 광고 슬롯이 12개다.

**Fitbod·플랜핏은 구독 모델이라 이걸 못 한다** — 구독자에게 운동 중 광고를 12번 트는 건 브랜드 자살이다. 기술이 아니라 수익 모델이 충돌해서 못 하는 자리라, 이 프로젝트에서 **판정 기준 ②를 통과한 첫 차별점**이다. 앱인토스 검색으로 휴식 시간에 광고를 트는 앱이 없음도 확인했다.

접을 때 지적했던 구멍(*"기구를 다 사면 수익 0"*)도 광고가 메운다. 쉐어링크는 기구 해금 자리에 보조 수익으로 남는다.

**그 밖에**

- 판정 기준에 **⓪ 선점 확인을 설계보다 먼저** 추가했다. 두 번 다 설계를 끝낸 뒤 경쟁자를 발견한 대가다
- 「백원마켓」(리뷰 3,730)을 뜯어보다 **리워드 광고가 실제로 작동하는 수익 모델**임을 확인했다. 어필리에이트는 구매까지 가야 하지만 광고는 시청만으로 수익이 난다
- **서버 0대**가 됐다 — 운동 데이터는 번들에, 상태는 localStorage, 쉐어링크는 미리 발급해 정적 JSON. EC2도 DB도 필요 없다
- 레포·폴더를 `pricelog` → `restfit`으로 정리했다

---

## 2026-08-25 · 설계 개정 — 웹사이트에서 앱인토스 미니앱 단독으로

**의도**: 실행 환경이 앱인토스 미니앱으로 확정되면서 초판 설계의 전제가 무너졌다.

**결과**

- **SEO 설계를 전부 폐기**했다 — 사이트맵·JSON-LD·URL 슬러그·"상품 하나당 페이지 하나로 롱테일"·Next.js 서버 모드·60일 SEO 대기론까지. 미니앱은 검색엔진에 색인되지 않는다. 트래픽은 토스 앱 내 노출에서 온다
- 미니앱 스택은 선택지가 없다: Vite + React **18** + `@apps-in-toss/web-framework` + `@toss/tds-mobile`. BookTimer 미니앱 실물로 확인했다 (React 19는 TDS peer 제약으로 불가)
- 미니앱이 정적 SPA라 **API 서버가 필수**가 됐다. 이 때문에 "이 프로젝트엔 API 서버 자리가 없다"는 초판 논거가 무효가 됐고, Spring이 후보로 되살아났다 → **Spring Boot + PostgreSQL** 채택. BookTimer로 검증된 Spring + 미니앱 패턴을 재사용한다
- 수집기는 별도 cron이 아니라 `@Scheduled`로. 하루 1회 작업에 별도 배포를 늘릴 이유가 없다
- **미니앱 심사 리젝 우려는 근거가 없었다** — 외부 링크 정책의 허용 예시에 *"각 제품을 소개·추천한 뒤 최저가 구매 플랫폼으로 이동"* 이 명시돼 있다. 우리 서비스 그 자체다

**최우선 과제가 바뀌었다**

미니앱에서 쉐어링크를 열면 앱 복귀가 불안정하다는 제보가 확인됐고(토스 "개선 논의 중"), 제보자는 결국 토스쇼핑 링크를 빼고 쿠팡을 쓰고 있다. **수익 전부가 그 링크 하나에 달려 있어** 다른 설계보다 먼저 잰다. Open API 승인 없이 수동 발급 링크로 검증 가능하다.

---

## 2026-08-25 · 프로젝트 신설 — 토스쇼핑 가격 이력 검증 서비스

**의도**: 토스 쉐어링크를 적극 활용하는 서비스를 찾는 과정에서, 여러 후보를 검토·기각한 끝에 아이템을 확정하고 프로젝트를 세웠다.

**결과**

- 아이템 확정: **토스쇼핑 상품의 가격 이력을 매일 쌓아 할인의 진위를 판정하는 웹사이트.** 수익은 쉐어링크 어필리에이트 10%
- 선정 근거: 커머스 사업자는 자기 셀러의 할인이 가짜라고 표시할 수 없다 → 토스가 구조적으로 못 만드는 기능 = 영구 해자. 그리고 다나와가 커버 못 하는 오픈마켓 롱테일이 우리 자리다
- 검토했다 접은 안들과 **접은 이유·되살릴 조건**을 `docs/ideas.md`에 보존. 특히 홈트 헬퍼는 채택했다가 같은 날 경쟁 조사(Fitbod·플랜핏)로 기각했고, 그 과정에서 얻은 판정 기준 3개를 문서에 남겼다
- 설계 문서 `docs/2026-08-25-design.md` — 아키텍처(EC2 1대 + Next.js 서버 모드 + SQLite + cron), 데이터 모델, 수집기 우선순위, 판정 로직 명세
- 작업 추적 3종 + troubleshooting 분할 시스템 셋업

**주요 판단 두 가지**

- 인프라를 **EC2 + Elastic IP**로 정했다. 쉐어링크 API가 출발지 IP 화이트리스트를 요구하는데 가정용 회선은 유동 IP라 조용히 깨진다
- 초기 설계의 "서버 0대 정적 사이트"를 뒤집었다. 상품 하나당 페이지 하나로 수천 페이지가 나오는 SEO 사이트라 매일 전체 재빌드가 비현실적이다 → 서버 모드 + SQLite
