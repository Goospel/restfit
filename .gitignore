node_modules/
.next/
out/
dist/
coverage/
*.tsbuildinfo

# 머신마다 다르다 (preview_start용)
.claude/launch.json

# 환경변수 — 쉐어링크 Access/Secret Key가 들어간다. 절대 커밋 금지
.env
.env.*
!.env.example

# ⚠️ 여기에 `data/`를 앵커 없이 적지 않는다 — 모든 깊이에 매치돼 `miniapp/src/data/`를 삼킨다 (T-215).
# 루트만 막을 거면 반드시 `/data/`.

# T-026 — PowerShell 5.1 한글 커밋 메시지 회피용 임시 파일
.commit-msg-tmp
.pr-body-tmp

# 스크래치
*.log
.DS_Store
Thumbs.db
