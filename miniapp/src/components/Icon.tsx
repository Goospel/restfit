/**
 * 앱 전용 아이콘.
 *
 * **이모지를 쓰지 않는다** — 기기·OS마다 다른 그림이 뜨는 데다, 어느 앱에나 있는
 * 남의 그림이라 화면이 「아무거나 갖다 붙인 것」처럼 보인다.
 *
 * 앱 아이콘(`assets/icon/icon.svg`)과 같은 언어다 — 24 그리드, 2px 라운드 스트로크.
 * 색은 `currentColor`라 부모가 정한다(탭 활성이면 파랑, 아니면 회색).
 *
 * ⚠️ **검수는 20px에서 한다.** 하단 탭이 그 크기라, 40px에서 예쁜 것보다 20px에서
 * 안 뭉개지는 게 중요하다. 획을 하나 더 넣고 싶어질 때 먼저 무너지는 것도 여기다.
 */

/**
 * 이름 → 그 아이콘을 이루는 path `d` 목록.
 *
 * ⚠️ 타입을 `Record<string, string[]>`로 넓히지 않는다 — 그러면 `IconName`이 그냥 `string`이 되어
 * 오타난 아이콘 이름이 컴파일을 통과하고, 화면에는 빈 사각형이 뜬다.
 */
export const ICONS = {
  /**
   * 덤벨. 앱 아이콘과 같은 판단이다 — **곧은 봉 + 옆에서 본 두꺼운 원판**이라야 덤벨로
   * 읽힌다. 원판을 얇은 원으로 그리면 양쪽에 공 달린 막대가 되고, 봉이 바깥 원판에
   * 안 닿으면 샤워기가 된다.
   */
  dumbbell: ['M6.5 7v10M17.5 7v10M3.5 10v4M20.5 10v4M6.5 12h11'],

  /**
   * 바벨. **덤벨과 갈라야 하는 게 전부다.** 봉을 원판 밖으로 끝까지 빼고 원판을 면으로
   * 채워 덤벨보다 무겁게 만들었다. 원판을 원으로 그린 안은 28px에서 안경이 됐다.
   */
  barbell: ['M2 12h20', 'M5.4 6.6h2.4v10.8H5.4z', 'M16.2 6.6h2.4v10.8h-2.4z', 'M9.8 9.8v4.4M14.2 9.8v4.4'],

  /**
   * 케틀벨. **손잡이 구멍이 생명이다** — 아치를 몸통 위에 그냥 얹으면 구멍이 메워져
   * 손가방이 된다. 그래서 몸통 상단을 낮게 깔고 아치를 그 위로 띄웠다.
   */
  kettlebell: [
    'M9.1 11.3V8.4a2.9 2.9 0 0 1 5.8 0v2.9',
    'M9.6 11.2c-2.4 1-4.2 3.2-4.2 5.9 0 1.4 1.1 2.5 2.5 2.5h8.2c1.4 0 2.5-1.1 2.5-2.5 0-2.7-1.8-4.9-4.2-5.9z',
  ],

  /**
   * 밴드. **여기만 선이 아니라 면이다**(`FILLED_ICONS`).
   *
   * 머리가 굵고 꼬리로 갈수록 얇아지는 띠인데, 선으로 그리면 겉모양만 좁아지고 **선 굵기는
   * 끝까지 2px 그대로**라 원근이 안 난다 — 꼬리가 「가늘어진 띠」가 아니라 「그냥 선」으로 보인다.
   *
   * ⚠️ **손으로 고치지 않는다.** 면으로 채운 띠는 반폭이 그 자리 곡률반경보다 커지는 순간
   * 안쪽이 제 살을 파고들어 덩어리가 되는데, 눈에는 「왠지 뚱뚱하다」로만 보인다.
   * 모양을 바꾸려면 `scripts/gen-band-icon.mjs`를 고쳐 다시 뽑는다 — 그 스크립트가
   * 겹침 지수(반폭÷곡률반경)를 재서 한도 안에서 최대 굵기를 찾아 준다. 지금은 한도 0.95,
   * 머리 두께 6.1 → 꼬리 0.53이다.
   */
  band: [
    'M7.9 7.6C7.9 8.7 9.7 8.2 10.5 8.4C11.3 8.7 12 9 12.7 9.2C13.4 9.5 14 9.8 14.5 10C15 10.2 15.4 10.5 15.6 10.6C15.9 10.8 16 10.9 16.2 11C16.3 11 16.4 10.9 16.4 11.1C16.3 11.2 16.3 11.5 16.1 11.8C15.8 12.1 15.4 12.5 14.8 12.9C14.3 13.3 13.5 13.7 12.8 14.2C12 14.6 11.1 15 10.3 15.4C9.4 15.8 8.5 16.2 7.7 16.7C7 17.1 6.2 17.5 5.7 17.9C5.1 18.4 4.7 18.9 4.6 19.4C4.6 19.9 4.9 20.5 5.4 20.9C5.9 21.3 7.2 21.9 7.6 22C8 22.1 8.1 21.8 7.8 21.5C7.5 21.2 6.2 20.7 5.8 20.4C5.4 20 5.4 19.8 5.4 19.6C5.5 19.3 5.8 19.1 6.3 18.8C6.7 18.5 7.5 18.1 8.3 17.8C9.1 17.5 10 17.1 10.9 16.8C11.8 16.5 12.7 16.1 13.6 15.8C14.4 15.5 15.2 15.1 15.9 14.8C16.6 14.4 17.3 14.1 17.8 13.6C18.3 13.2 18.8 12.7 19.1 12.1C19.4 11.5 19.5 10.6 19.4 10C19.3 9.3 18.9 8.6 18.5 8C18.1 7.4 17.5 6.9 17 6.4C16.4 5.9 15.7 5.4 15 4.9C14.4 4.4 13.6 3.9 12.8 3.5C12 3 11.2 1.3 10.4 2C9.5 2.7 7.9 6.6 7.9 7.6z',
  ],

  /** 풀업바. 문틀 + 위쪽에 걸린 바. 바를 가운데 두면 매달릴 공간이 안 보여 그냥 창틀이 된다. */
  pullupBar: ['M4.6 4.4v15.2M19.4 4.4v15.2', 'M3 7.8h18'],

  /** 벤치. 긴 패드 + 벌어진 다리. 다리를 곧게 세우면 탁자가 된다. */
  bench: ['M4.4 8.4h15.2a2.4 2.4 0 0 1 0 4.8H4.4a2.4 2.4 0 0 1 0-4.8z', 'M7.4 13.2 5.6 18M16.6 13.2 18.4 18'],

  /** 짐볼. 가장 큰 원 + 지구본 경선. 메디신볼과는 **크기와 선 방향**으로 가른다. */
  exerciseBall: ['M12 3.4a8.6 8.6 0 1 0 0 17.2 8.6 8.6 0 1 0 0-17.2z', 'M12 3.4a3.7 8.6 0 1 0 0 17.2 3.7 8.6 0 1 0 0-17.2z'],

  /** 폼롤러. 옆에서 본 원기둥. 앞면 타원이 없으면 그냥 알약이 된다. */
  foamRoller: ['M8 7.2h8a2 4.8 0 0 1 0 9.6H8', 'M8 7.2a2 4.8 0 1 0 0 9.6 2 4.8 0 1 0 0-9.6z'],

  /** 메디신볼. 작은 원 + 가죽 솔기. 짐볼보다 작게 그려야 「무거운 공」으로 읽힌다. */
  medicineBall: [
    'M12 5.2a6.8 6.8 0 1 0 0 13.6 6.8 6.8 0 1 0 0-13.6z',
    'M7.2 7.2c2.4 3 2.4 6.6 0 9.6',
    'M16.8 7.2c-2.4 3-2.4 6.6 0 9.6',
  ],

  /** 복근롤러. 가운데 바퀴 하나 + 좌우 손잡이. 바퀴가 둘이면 바벨이 된다. */
  abRoller: ['M12 8.6a5 5 0 1 0 0 10 5 5 0 1 0 0-10z', 'M3.6 13.6h3.4M17 13.6h3.4', 'M3.6 11.4v4.4M20.4 11.4v4.4'],

  /**
   * 쌓인 기록. 달력이 아니라 막대인 이유는, 이 앱의 기록이 「며칠에 했나」가 아니라
   * 「몇 세트 했나」라서다.
   */
  chart: ['M4.5 19.5h15', 'M8 19.5v-4.5M12 19.5v-9M16 19.5v-6.5'],

  /**
   * 불꽃(체지방 감량). **안쪽 불꽃이 빠지면 물방울로 보인다** — 바깥 윤곽만으로는
   * 눈물방울과 구별이 안 된다. 20px에서 안쪽이 점처럼 남아도 그 차이를 만든다.
   */
  flame: [
    'M12 2.8c3.4 4.4 6 6.5 6 9.9a6 6 0 0 1-12 0c0-3.4 2.6-5.5 6-9.9z',
    'M12 13.2c1.3 1.7 2.2 2.5 2.2 3.7a2.2 2.2 0 0 1-4.4 0c0-1.2.9-2 2.2-3.7z',
  ],

  /**
   * 원판(근육 키우기). 이두박근 실루엣은 20px에서 반드시 뭉개져서 원판으로 갔다 —
   * 앱 아이콘에서 쏟아지는 그 흰 원판과 같은 물건이다.
   */
  plate: ['M12 3.8a8.2 8.2 0 1 0 0 16.4 8.2 8.2 0 1 0 0-16.4z', 'M12 9.4a2.6 2.6 0 1 0 0 5.2 2.6 2.6 0 1 0 0-5.2z'],

  /** 새싹(건강 유지). 잎이 하나면 물음표처럼 보인다 — 크기가 다른 잎 둘이라야 새싹으로 읽힌다. */
  sprout: [
    'M12 20.5v-7.4',
    'M12 13.1c0-4.3 2.6-7.2 7.1-7.5.3 4.4-2.3 7.3-7.1 7.5z',
    'M12 16.6c-3 0-5-1.9-5.5-4.9 3 0 5 1.9 5.5 4.9z',
  ],

  /** 체크. 고른 기구 칸의 오른쪽 위에 붙는다 — **색만으로 상태를 알리지 않기 위해서다.** */
  check: ['M5.5 12.5 10 17 18.5 7'],

  /**
   * 재생(동작 보기). **면이다**(`FILLED_ICONS`) — 14px 칩 안에 들어가는데, 선으로 그리면
   * 2px 스트로크가 삼각형 안쪽을 거의 메워 「속이 빈 삼각형」이 아니라 검은 덩어리가 된다.
   */
  play: ['M8 5.5v13l10-6.5z'],
};

export type IconName = keyof typeof ICONS;

/**
 * 선이 아니라 **면**으로 그리는 아이콘.
 *
 * 굵기가 변해야 하는 그림은 선으로 못 그린다(stroke 굵기는 path 하나에 하나뿐이다).
 * ⚠️ 늘릴 때는 의식하고 늘린다 — 면 아이콘은 선 아이콘 사이에서 더 진해 보인다.
 */
export const FILLED_ICONS: ReadonlySet<IconName> = new Set<IconName>(['band', 'play']);

export function Icon({ name, size = 20 }: { name: IconName; size?: number }) {
  const filled = FILLED_ICONS.has(name);
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill={filled ? 'currentColor' : 'none'}
      stroke={filled ? 'none' : 'currentColor'}
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
      style={{ flexShrink: 0 }}
      aria-hidden="true"
    >
      {ICONS[name].map((d) => (
        <path key={d} d={d} />
      ))}
    </svg>
  );
}
